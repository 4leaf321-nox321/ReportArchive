# 종합보고 워크스페이스 기본 공유 — 설계

## 1. 배경 / 문제
- 보고서는 객체별 공유(`ContentGrant`)에 더해 **게시판 단위(`BoardGrant`)·폴더 단위(`FolderGrant`)** 기본 공유가 있어, 그 게시판/폴더에 게시된 보고서 전체가 자동 상속한다.
- 종합보고(`CompositeReport`)는 **객체별 `ContentGrant` 공유만** 있다. 폴더 개념도, 다중 게시(mount)도 없다.
- 게시판 단위 `BoardGrant`는 종합보고의 홈 워크스페이스에도 이미 적용되지만(`_content_board_slugs`가 홈 slug 반환), 이는 **보고서까지 함께** 묶는 게시판 설정이라 "종합보고 전용 기본값"이 아니다.
- → 워크스페이스(부서)마다 "이 부서 종합보고는 기본적으로 누구에게 열린다"를 한 번 설정하면 그 부서의 모든 종합보고가 상속하는 **종합보고 전용 기본 공유**가 필요하다.

## 2. 확정된 동작 (사용자 결정)
- **라이브 상속(소급 O)**: 기본 공유를 바꾸면 그 부서의 **기존 종합보고까지 즉시** 반영. 복사본 없음. 읽기 시점 계산.
- **추가만(additive)**: 부서 기본 공유는 최소 보장선. 개별 종합보고는 그 위에 **더 넓게만** 공유 가능하고, 기본분을 **빼서 더 좁게 닫을 수는 없음**(v1). → 개별 예외/제외 플래그 없음(단순).

## 3. 데이터 모델 (신규 테이블 1개)
`BoardGrant`를 본뜬 종합보고 전용 워크스페이스 기본 grant.

```
CompositeDefaultGrant
  id                int PK
  workspace_slug    str  FK workspaces.slug  ON DELETE CASCADE
  principal_type    enum GrantPrincipalType   (workspace | user | all_org)   ※ 기존 enum 재사용
  principal_ref     str|NULL   (workspace→부서 slug, user→user_id, all_org→NULL)
  level             enum GrantLevel           (view | edit)                  ※ all_org 은 view 고정
  created_by_user_id int|NULL
  created_at        datetime
  UNIQUE(workspace_slug, principal_type, principal_ref)
  INDEX(workspace_slug)
```
- 의미·상속 규칙은 `BoardGrant`/`ContentGrant`와 동일: 부서 grant는 그 부서 + **하위 부서까지 상속**, `all_org`는 사내 전체 **열람 전용**.
- 보고서 가시성엔 영향 없음(종합보고 판정 경로에서만 참조).

## 4. 가시성 계산 (백엔드)
`backend/app/modules/grants/services.py` 종합보고 판정에 홈 워크스페이스의 `CompositeDefaultGrant` 도달 여부를 OR 합산.

- 신규 헬퍼(기존 `_board_view_reaches`/`_board_edit_reaches`/`board_has_all_org` 대칭):
  - `_default_view_reaches(db, workspace_slug, actor, *, include_all_org)`
  - `_default_edit_reaches(db, workspace_slug, user_id)`
  - `default_has_all_org(db, workspace_slug)`
- 합산 지점(종합보고 한정):
  - `can_view` — 소유·sys_admin·user grant·부서 grant·게시판 grant·all_org **＋ default-grant(홈 ws)**
  - `can_edit_grant` — 위 edit 경로 ＋ default-grant edit(홈 ws)
  - `reachable_workspace_view`(부서 목록 노출 판정) ＋ default-grant 부서 도달
  - `has_all_org_grant` / `is_public_only_viewer` — default all_org 포함
- 종합보고 목록(`composites/services.list_in_workspace_tree` + 가시성 판정)도 같은 경로를 타므로 단건·목록 일관 적용.
- 홈 워크스페이스: `_content_board_slugs`처럼 `CompositeReport.workspace_slug` 사용.

## 5. API (게시판 공유와 대칭)
```
GET    /api/workspaces/{slug}/composite-default-shares          목록 (가시: 매니저/sys_admin)
POST   /api/workspaces/{slug}/composite-default-shares          추가/갱신 (매니저+sys_admin)
DELETE /api/workspaces/{slug}/composite-default-shares/{id}     제거 (매니저+sys_admin)
```
- 권한 가드: 기존 `_ensure_board_manager(db, actor, slug)` 재사용.
- 요청/응답 스키마: 기존 `GrantCreate`/`GrantRead` 재사용(또는 동형 신규). 라벨(`_label_for`)도 재사용.
- 검증: `all_org` → level 강제 `view`, `principal_ref` 규칙 검증, 자기 워크스페이스로의 무의미 grant 방지(선택).

## 6. 프론트엔드
- **재사용**: `frontend/src/shared/components/ShareEditor.jsx`(부서/사용자/전체공개 × 열람/편집) 그대로.
- **진입점**: `CompositesListPage` 상단에 "이 부서 종합보고 기본 공유" 팝오버(매니저/sys_admin 에게만). 게시판 공유 UI와 같은 자리/패턴.
- **API 클라이언트**: `frontend/src/shared/api/shares.js`에 `listCompositeDefaultShares(slug)`/`add`/`remove` 추가.
- **개별 종합보고 표시**: 각 종합보고 `SharePopover`에 "부서 기본 공유로 △△에 열려 있음(상속)"을 읽기 전용으로 함께 표기해, 상속분 vs 개별 추가분을 구분(편집은 부서 설정에서).

## 7. 마이그레이션
- Alembic: `CompositeDefaultGrant` 테이블 생성만. 기존 데이터 변경 없음. 롤백 = drop.

## 8. 엣지 케이스 / 결정
- **추가만(additive)**: 개별에서 기본분 제외 불가(v1). 더 좁게 닫는 요구가 생기면 후속으로 "제외 grant/플래그" 도입.
- **개인 워크스페이스(kind=personal)**: 기본 공유 설정 노출하지 않음(부서 공유 의미 없음) — 매니저 가드로 자연 차단되나 UI에서도 숨김.
- **워크스페이스 삭제**: CASCADE 로 default-grant 동반 삭제.
- **하위 부서 상속**: 부서 grant 의 하위 상속은 기존 `reachable_*` 로직 그대로 사용(별도 구현 없음).
- **성능**: 종합보고 가시성 계산에 워크스페이스당 default-grant 조회 1회 추가. 인덱스(`workspace_slug`)로 무시 가능 수준. 목록은 default-grant 를 한 번 모아 메모이즈 가능.

## 9. 작업 단위(구현 시)
1. 모델 + Alembic 마이그레이션
2. grants/services 헬퍼 3종 + 종합보고 판정 경로 OR 합산
3. grants/routes 엔드포인트 3종(+스키마)
4. shares.js API + CompositesListPage 진입 UI(ShareEditor 재사용)
5. 개별 종합보고 상속 표시
6. 테스트: 상속(부서/하위부서/user/all_org/edit), 소급 반영, 매니저 권한 가드, 목록 필터 일관성
