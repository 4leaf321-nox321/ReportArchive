# 부서 홈 · 대시보드 재설계 (부서 관점)

개인("내 작업") 화면은 후순위. 먼저 **부서 스코프** 두 화면의 역할을 분리한다.

## 세 층 (참고)
- **부서 홈** `/w/:ws` — 탐색·시작 허브(둘러보고 떠나는 곳). 기간 필터 없음, 개별 항목·바로가기 중심.
- **부서 대시보드** `/w/:ws/dashboard` — 분석·현황(머무는 곳). 기간 필터, 집계·차트 중심.
- **개인 홈**(후순위) — 계정 스코프·부서 횡단의 "내 할 일". 부서 화면과 분리.

## 판별 기준
- 지금 당장 어디로 갈지 돕는다 → **홈**
- 기간을 두고 추세·분포·상태를 분석한다 → **대시보드**
- 나에게 걸린 일 → **개인 홈**(나중)

---

## 결정 사항 (기본값 확정)
1. **진입 동선**: 부서 클릭 시 기본 도착 = **홈**. 대시보드는 좌측 메뉴 + 홈 상단 바로가기로 진입. (현 라우트 유지)
2. **콘텐츠 수치 인사이트의 '지표' 정의**: **Phase 3로 미룸.** 그때 "템플릿에 대시보드 지표 지정" 방식 채택(자동 휴리스틱은 부정확). Phase 1/2와 무관.
3. **고정 보고서(핀)**: 신규 모델 필요 → **Phase 1 제외(후순위).**

---

## A. 부서 홈 (탐색·시작 허브)
| 섹션 | 내용 | 데이터 | 신규/재사용 |
|---|---|---|---|
| 헤더 | 부서명·설명 + 보고서 작성 / 종합보고 | workspace | 재사용 |
| 빠른 이동 | 대시보드·보고서 목록·종합보고·관계도 바로가기 | 라우트 | 신규(링크 카드) |
| 빠른 시작 | 자주 쓴 템플릿 칩 → 새 보고서 | listReports template_id 빈도 + listTemplates | 재사용(빈도 계산) |
| 최근 보고서 | 최근 업데이트 N건(상태·작성자·날짜) | listReports | 기존 |
| 하위 부서 바로가기 | 직속 하위 부서 카드 → 각 부서 홈 | workspace children | 신규(권한 범위 내) |
| 부서 활동 피드 | 생성/수정/게시/코멘트 N건 | GET /api/workspace-activities | Phase 2 ✅ |
| (선택) 고정 보고서 | 핀 | 신규 모델 | 후순위 |

원칙: 집계·차트·기간필터는 홈에 두지 않음.

## B. 부서 대시보드 (분석·현황)
KPI(총 보고서·작성자·템플릿) + 단계별 + 추세 + 건강도 + 분포 + 교차 분석.
| 항목 | 내용 | 데이터 | 상태 |
|---|---|---|---|
| 드릴다운 | KPI·단계 클릭, 분포 막대·교차 셀 클릭 → 보고서 목록 필터 이동 | location.state | ✅ |
| 전기 대비 Δ | KPI에 ▲▼(이번 기간 vs 직전) | 서버 prev 재집계 | ✅ |
| 부서 건강도 | 정체 draft(N일+)·미분류·미해결 코멘트 | GET /api/dashboard(서버 집계) | ✅ |
| 메타데이터 분포 | 차원 선택 1카드 — 모델·부품·개발단계·불량·시험·종류·템플릿별 보고서 수 | distributions | ✅ |
| 교차 분석 | 두 차원 행×열 히트맵(예: 모델×개발단계), 셀 클릭=두 필터 | GET /api/dashboard/crosstab | ✅ |
| 작성자 Top | 작성자별 보고서 수 상위 | author_top | ✅ |
| 기간 비교 | 주별/월별(주 비교)/연별(월 비교)/연도 비교 | period.unit | ✅ |
| 하위부서 포함 토글 | 자손 부서 게시판까지 롤업 | include_descendants | ✅ |
| 선택값 기억 | 기간 종류·분포/교차 차원·하위부서 토글 localStorage 저장 | usePersistedState | ✅ |
| ~~콘텐츠 수치 인사이트~~ | 템플릿 본문 숫자 → **메타데이터 통계로 전환(보류)** | — | 보류(아래) |
| ~~부서×템플릿~~ | **제거**(가치 대비 복잡도) | — | 제거 |

주의: 리뷰/승인 워크플로가 없어 phase 대부분 'drafting'에 몰릴 수 있음 → 건강도는 정체 draft·미분류·미해결 코멘트 중심.

## C. 기술 메모
- ✅ 대시보드 집계는 **서버 API** `GET /api/dashboard?from=&to=&unit=&include_descendants=` 로 이전 완료(클라 집계 제거). 교차표는 `GET /api/dashboard/crosstab?row=&col=`.
- MVP 는 Python 추출(중간 규모). 매우 큰 조직에서 느려지면 SQL GROUP BY/JSONB·캐시로(후속).

## 진행 순서 / 현재 상태 (~v0.48.3)
- **Phase 1 ✅**: 홈 재구성(빠른 이동·빠른 시작·하위부서) + 대시보드 드릴다운 + KPI Δ.
- **Phase 2 ✅**: 부서 활동 피드 + 건강도(정체·미분류·미해결 코멘트) + 작성자 Top.
- **Phase 3 ✅ (방향 전환)**: 서버 집계 API 전환 + 메타데이터 분포 + 교차 분석 + 기간 비교(연/주) + 하위부서 토글 + 드릴다운 개수 일치 + 선택값 저장. 콘텐츠 수치 지표(TemplateMetric)는 만들었으나 **휴면**(메뉴 숨김), 대신 메타데이터 통계 채택. → 상세: **Phase3_대시보드_설계.md**

## 남은 작업 (후순위)
- **개인 "내 작업" 홈** — **불필요로 판단(보류).** 내 공간(/personal/reports)·받은 코멘트(/personal/inbox)·알림(/notifications)이 이미 있어 재탕. 정 필요하면 홈에 요약 줄 하나 정도.
- **고정 보고서(핀)** — 신규 모델 필요.
- **콘텐츠 수치 인사이트(3B-2)** — TemplateMetric 휴면 해제 시: 값 추출·집계·"콘텐츠 지표" 카드. 필요해지면 재개.
- **성능 — 본문 제외 로딩 ✅(v0.49.0, defer content/pages).** 그 위 SQL GROUP BY 전환은 *defer 후 효과 미미·리라이트 위험* 으로 보류(대규모에서 프로파일 후 결정).
- **분포 차원: 게시판별 ✅(v0.49.0).** 폴더별은 org 폴더가 게시판(mount)별이라 부서 횡단 시 의미 모호 → 보류. 작성자별은 목록 작성자 필터 추가 필요(미정).
- **(선택) anchor 저장.**
