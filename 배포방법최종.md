# 배포방법 (최종)

3대 컴퓨터 흐름으로 폐쇄망 서버에 배포한다.

```
[개발 컴퓨터]        [GitHub Actions]            [중간 윈도우]              [폐쇄망 리눅스 서버]
 git push       →   번들 자동 빌드        →     .tar.gz 다운로드       →    install.sh / update.sh
 git tag             Release에 첨부             scp로 서버에 전송
```

중간 윈도우는 **다운로드 + scp**만 한다. 빌드 작업 없음.

---

## 빌드는 언제 자동으로 도나

| 액션 | GitHub Actions가 빌드함? | 결과물 |
|---|---|---|
| `git push` (평소 커밋) | ❌ | 없음 (코드만 GitHub에 올라감) |
| `git push origin v0.2.0` (태그 push) | ✅ | Releases에 `.tar.gz` 자동 첨부 |
| Actions 탭에서 "Run workflow" 클릭 | ✅ | Artifacts에만 업로드 (Release 안 만듦) |

평소 작업은 그냥 `git push` — 빌드 안 됨, GitHub에 코드만 백업되는 셈.
**배포할 시점에만 태그를 새로 찍는다**. 그래야 무료 Actions 사용량을 아끼고
Releases 페이지에 진짜 배포본만 쌓인다.

```bash
# 평소 — 빌드 안 함
git add -A && git commit -m "..."
git push

# 배포 — 이때만 번들이 만들어짐
git tag v0.2.0
git push origin v0.2.0
```

---

## A. 처음 세팅할 때 (한 번만)

### A-1. 개발 컴퓨터 — 첫 번들 트리거

```bash
git tag v0.1.0
git push origin v0.1.0
```

→ GitHub Actions가 약 10분 후 Releases에 `reportarchive-v0.1.0.tar.gz` 첨부.
진행 상황: `https://github.com/4leaf321-nox321/ReportArchive/actions`

### A-2. 중간 윈도우 — 다운로드 + 전송

브라우저로 Releases 페이지에서 `.tar.gz` 다운로드:
```
https://github.com/4leaf321-nox321/ReportArchive/releases/latest
```

PowerShell:
```powershell
scp $env:USERPROFILE\Downloads\reportarchive-v0.1.0.tar.gz user@서버IP:/tmp/
```

### A-3. 폐쇄망 서버 — 최초 설치

```bash
ssh user@서버IP
tar xzf /tmp/reportarchive-v0.1.0.tar.gz -C ~
cd ~/reportarchive-v0.1.0

# 1) OS 패키지 + 서비스 계정 + 디렉터리
sudo ./prepare_server.sh

# 2) DB 사용자·DB 생성
DB_PASSWORD='강한_무작위_비밀번호'
sudo -u postgres psql <<SQL
CREATE USER reportarchive WITH PASSWORD '${DB_PASSWORD}';
CREATE DATABASE report_automation OWNER reportarchive;
SQL

# 3) .env 작성
cp .env.example .env
nano .env
#  SECRET_KEY     = python3 -c "import secrets; print(secrets.token_urlsafe(48))"
#  JWT_SECRET_KEY = 위와 다른 값으로 한 번 더
#  DATABASE_URL   안에 위 DB_PASSWORD 그대로 박기

# 4) 설치 + systemd 자동 시작
sudo ./install.sh

# 5) 확인
curl http://localhost:3000/api/health
sudo systemctl status reportarchive
```

여기까지 끝나면 `http://서버IP:3000` 에서 동작.

---

## B. 업데이트할 때 (매번 동일)

### B-1. 개발 컴퓨터 — 새 태그 push

```bash
git add -A
git commit -m "feat: 새 기능"
git push

git tag v0.2.0
git push origin v0.2.0
```

→ GitHub Actions가 자동으로 새 번들 빌드 + Releases 첨부.

### B-2. 중간 윈도우 — 다운로드 + 전송

```powershell
scp $env:USERPROFILE\Downloads\reportarchive-v0.2.0.tar.gz user@서버IP:/tmp/
```

### B-3. 폐쇄망 서버 — update.sh 한 줄

```bash
ssh user@서버IP
tar xzf /tmp/reportarchive-v0.2.0.tar.gz -C ~
cd ~/reportarchive-v0.2.0
sudo ./update.sh
```

`update.sh`가 자동으로: 서비스 중지 → 이전 SIF를 `app.sif.prev`로 백업 → 새 SIF 배치 → DB 마이그레이션 → 서비스 재시작.

`.env`, DB 비번, systemd 설정은 건드릴 일 없음.

**롤백** (문제 생기면):
```bash
sudo systemctl stop reportarchive
sudo mv /opt/reportarchive/app.sif.prev /opt/reportarchive/app.sif
sudo systemctl start reportarchive
```

---

## 차이 한눈에 보기

| | 처음 (A) | 업데이트 (B) |
|---|---|---|
| OS 패키지 | `prepare_server.sh` | 건너뜀 |
| DB 사용자/DB | `CREATE USER/DATABASE` | 건너뜀 |
| `.env` | 새로 작성 | 건드리지 않음 |
| 설치 명령 | `install.sh` | `update.sh` |
| systemd 등록 | 자동 등록 | 이미 등록됨 |
| 소요 시간 | 30분~1시간 | 약 5분 |

---

## 운영 메모

- 로그: `sudo journalctl -u reportarchive -f`
- 업로드 파일: `/opt/reportarchive/uploads/` (백업 대상)
- DB 백업: `sudo -u postgres pg_dump report_automation > backup.sql`
- `.env` 수정 후: `sudo systemctl restart reportarchive`
- 상세 가이드: 번들 안의 `README.md` 또는 레포의 `deploy/README_OPERATOR.md`
