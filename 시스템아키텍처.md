# 시스템 아키텍처

ReportArchive를 네 가지 관점에서 본 다이어그램. GitHub/대부분의 마크다운 뷰어에서
Mermaid가 자동 렌더링된다. 텍스트로 수정 가능하니 구조가 바뀌면 같이 갱신할 것.

- **1. 배포 토폴로지** — 운영 서버에서 무엇이 어디에 떠 있는가
- **2. 런타임 / 요청 흐름** — 요청 하나가 통과하는 레이어
- **3. 백엔드 모듈 맵** — 22개 모듈의 그룹과 의존 관계
- **4. 프론트엔드 모듈 맵** — 라우트 / modules / shared 의 결합

세부 배포 결정의 *이유* 는 `배포구조.md`, 도메인 설계 합의는 `협업개선_설계.md`
참조.

---

## 1. 배포 토폴로지

운영 환경: Ubuntu 24.04 호스트 + apptainer SIF 단일 번들 + 호스트 PostgreSQL.

```mermaid
flowchart TB
  classDef host fill:#f3f4f6,stroke:#6b7280,color:#111
  classDef sif fill:#fef3c7,stroke:#b45309,color:#111
  classDef mount fill:#ecfccb,stroke:#4d7c0f,color:#111
  classDef ext fill:#dbeafe,stroke:#1d4ed8,color:#111

  Browser["사용자 브라우저<br/>(React SPA 실행)"]:::ext

  subgraph HOST["운영 서버 — Ubuntu 24.04 (호스트 OS)"]
    direction TB
    systemd["systemd<br/>reportarchive.service"]:::host
    nginx["nginx :80/:443<br/>리버스 프록시 · TLS<br/>(선택)"]:::host
    pg[("PostgreSQL :5433<br/>호스트 서비스<br/>(SIF 교체 시 데이터 보존)")]:::host

    subgraph SIF["app.sif — apptainer 컨테이너"]
      direction TB
      uvicorn["uvicorn :3000<br/>FastAPI · UVICORN_WORKERS=4"]:::sif
      pyvenv["/opt/app/venv<br/>Python 3.12 + requirements.txt"]:::sif
      feassets["/opt/app/frontend/dist<br/>Vite 빌드 산출물"]:::sif
    end

    subgraph BIND["bind-mount (호스트 ↔ SIF)"]
      direction TB
      envf[".env (ro)<br/>DB 비밀번호 · 시크릿"]:::mount
      uploads["uploads/ (rw)"]:::mount
      logs["logs/ (rw)"]:::mount
    end
  end

  Browser -- "HTTPS" --> nginx
  Browser -. "SERVE_FRONTEND_DIST 모드<br/>(nginx 생략)" .-> uvicorn
  nginx --> uvicorn
  nginx -. "정적 자산 캐시" .-> feassets
  uvicorn --> pg
  uvicorn --- envf
  uvicorn --- uploads
  uvicorn --- logs
  systemd -. "apptainer run app.sif" .-> SIF
```

핵심 분리선:
- **SIF 안**: OS 베이스(ubuntu:24.04) · Python 3.12 · 백엔드 의존성 · 빌드된 프론트 dist
- **호스트**: apptainer · PostgreSQL · systemd · (선택) nginx
- **bind-mount**: .env · uploads · logs — SIF 교체 시 살아남아야 하는 것들

---

## 2. 런타임 / 요청 흐름

`/api/*` 요청 하나가 어떤 레이어를 통과하는가. 표준 envelope
`{ success, data, message, errors }` 로 응답.

```mermaid
sequenceDiagram
  autonumber
  participant U as 사용자
  participant S as React SPA<br/>(shared/api/client.js)
  participant N as nginx
  participant F as FastAPI app<br/>(main.py)
  participant MW as CORS / 예외 핸들러<br/>(shared/errors.py)
  participant R as APIRouter<br/>modules/&lt;name&gt;/routes.py
  participant A as 인증 의존성<br/>(shared/auth.py)
  participant SVC as services.py
  participant ORM as SQLAlchemy<br/>models.py
  participant DB as PostgreSQL

  U->>S: 페이지/액션 클릭
  S->>N: fetch /api/...
  N->>F: proxy_pass
  F->>MW: 미들웨어 통과
  MW->>R: route 매칭
  R->>A: get_current_user 등
  A-->>R: User
  R->>SVC: 도메인 함수 호출
  SVC->>ORM: 쿼리/커밋
  ORM->>DB: SQL
  DB-->>ORM: rows
  ORM-->>SVC: 객체
  SVC-->>R: 결과 (또는 도메인 에러 raise)
  R-->>F: Pydantic schema
  F-->>N: JSON envelope
  N-->>S: 응답
  S-->>U: 렌더 / 토스트
```

부가 경로 (시퀀스에는 안 그렸지만 같은 진입점):
- **정적 자산**: 결합 배포 모드일 때 `/`, `/assets/*` 와 SPA 폴백을 `main.py` 가 직접 처리
- **헬스체크**: `GET /api/health`
- **OpenAPI**: `/api/docs`, `/api/redoc`, `/api/openapi.json`

---

## 3. 백엔드 모듈 맵

`backend/app/modules/` 아래 22개 모듈. `register_routers()` 가 각각을
`/api/<prefix>` 로 마운트. 모듈을 도메인 그룹으로 묶고, 그룹 간 주요 의존 화살표만
표시 (모든 모듈의 `users` 의존은 노이즈가 커서 생략).

```mermaid
graph LR
  classDef core fill:#fde68a,stroke:#a16207,color:#111
  classDef content fill:#dbeafe,stroke:#1d4ed8,color:#111
  classDef collab fill:#fce7f3,stroke:#9d174d,color:#111
  classDef meta fill:#e9d5ff,stroke:#6b21a8,color:#111
  classDef ops fill:#fee2e2,stroke:#b91c1c,color:#111
  classDef axis fill:#f3f4f6,stroke:#374151,color:#111
  classDef shared fill:#d1fae5,stroke:#065f46,color:#111

  subgraph AXIS["횡단 (모든 모듈이 의존)"]
    auth["auth"]:::axis
    users["users"]:::axis
  end

  subgraph CORE["핵심 도메인"]
    workspaces["workspaces"]:::core
    reports["reports"]:::core
    templates["templates"]:::core
    composites["composites"]:::core
  end

  subgraph CONTENT["콘텐츠 빌딩블록"]
    widgets["widgets"]:::content
    widget_relations["widget_relations"]:::content
    entities["entities"]:::content
    prompts["prompts"]:::content
  end

  subgraph COLLAB["협업 / 활동"]
    comments["comments"]:::collab
    editors["editors"]:::collab
    activities["activities"]:::collab
    notifications["notifications"]:::collab
    members["members"]:::collab
    mounts["mounts"]:::collab
    folders["folders"]:::collab
  end

  subgraph META["분류 / 메타"]
    section_taxonomy["section_taxonomy"]:::meta
    template_categories["template_categories"]:::meta
    report_types["report_types"]:::meta
  end

  subgraph OPS["운영 / 부가"]
    admin["admin"]:::ops
    files["files"]:::ops
    voc["voc"]:::ops
  end

  subgraph SHARED["shared/ 레이어"]
    s_auth["auth.py<br/>JWT · get_current_user"]:::shared
    s_perm["permissions.py"]:::shared
    s_err["errors.py<br/>표준 예외 핸들러"]:::shared
    s_resp["responses.py<br/>envelope helper"]:::shared
    s_store["storage.py<br/>uploads 경로"]:::shared
  end

  auth --> users
  users --> workspaces

  workspaces --> reports
  workspaces --> templates
  templates --> workspaces
  template_categories --> templates
  report_types --> reports

  reports --> entities
  reports --> mounts
  reports --> templates
  reports --> report_types

  composites --> reports
  composites --> workspaces
  composites --> notifications

  folders --> mounts
  folders --> reports
  folders --> workspaces

  mounts --> folders
  mounts --> reports
  mounts --> notifications

  comments --> reports
  comments --> mounts
  comments --> activities
  comments --> notifications

  editors --> reports
  editors --> notifications

  members --> workspaces

  admin --> workspaces
  admin --> files

  CORE -. shared .-> SHARED
  COLLAB -. shared .-> SHARED
  CONTENT -. shared .-> SHARED
```

마운트 prefix는 `backend/app/modules/__init__.py:register_routers()` 에 한 곳으로
모여 있음. 새 모듈은 `app/modules/<name>/{routes,schemas,services,models}.py` 에
넣고 거기에 한 줄 등록.

---

## 4. 프론트엔드 모듈 맵

`createBrowserRouter` 로 정의된 라우트가 페이지 컴포넌트로, 페이지는 `modules/*`,
공통 인프라는 `shared/*`. 모든 인증된 라우트는 `AuthedShell` (= ProtectedRoute →
WorkspaceProvider → CommandPaletteProvider → AppShell) 안쪽.

```mermaid
graph LR
  classDef public fill:#e0e7ff,stroke:#3730a3,color:#111
  classDef ws fill:#fde68a,stroke:#a16207,color:#111
  classDef personal fill:#fce7f3,stroke:#9d174d,color:#111
  classDef cross fill:#dbeafe,stroke:#1d4ed8,color:#111
  classDef admin fill:#fee2e2,stroke:#b91c1c,color:#111
  classDef shared fill:#d1fae5,stroke:#065f46,color:#111

  subgraph ROOT["RootLayout (AuthProvider · Toaster · ThemeProvider)"]
    direction TB

    subgraph PUB["Public"]
      login["/login → auth/LoginPage"]:::public
      signup["/signup → auth/SignupPage"]:::public
    end

    subgraph SHELL["AuthedShell<br/>ProtectedRoute → WorkspaceProvider → CommandPaletteProvider → AppShell"]
      direction TB

      subgraph WS["부서 스코프 /w/:workspace/*"]
        wh["WorkspaceHomePage"]:::ws
        rl["reports/ → ReportsListPage"]:::ws
        rn["reports/new → ReportNewPage"]:::ws
        rd["reports/:id → ReportDetailPage"]:::ws
        dash["dashboard → DashboardPage"]:::ws
        comp["composites → CompositesListPage / DetailPage"]:::ws
        mem["members → MembersPage"]:::ws
      end

      subgraph PERSONAL["개인공간 /personal/*"]
        pr["reports → ReportsListPage"]:::personal
        inbox["inbox → InboxPage"]:::personal
      end

      subgraph CROSS["부서 횡단"]
        prof["/profile"]:::cross
        notif["/notifications"]:::cross
        tpl["/templates · /templates/new · /:id/edit"]:::cross
        voc["/voc · /voc/:postId"]:::cross
        ai["/ai-settings"]:::cross
      end

      subgraph ADMIN["관리자 /admin/*  ·  /server"]
        a1["AdminPage / AccountsAdminPage"]:::admin
        a2["UserSpaceAdminPage / EntitiesAdminPage"]:::admin
        a3["ServerPage"]:::admin
      end
    end
  end

  subgraph SHARED["shared/  공통 인프라"]
    direction TB
    s_api["api/<br/>client.js · envelope 처리"]:::shared
    s_auth["auth/<br/>AuthProvider · ProtectedRoute"]:::shared
    s_ws["workspace/<br/>WorkspaceProvider · 슬러그 해석"]:::shared
    s_theme["theme/  ·  layout/AppShell"]:::shared
    s_cmd["components/CommandPalette"]:::shared
    s_ui["components/ui/  (shadcn)"]:::shared
    s_reports["reports/ · ai/ · annotations/<br/>위젯 · 편집 헬퍼"]:::shared
    s_hooks["hooks/ · utils/ · lib/"]:::shared
  end

  WS -- "fetch /api/*" --> s_api
  PERSONAL --> s_api
  CROSS --> s_api
  ADMIN --> s_api
  s_api -- "Bearer token" --> s_auth
  WS --> s_ws
  PERSONAL --> s_ws
  WS --> s_reports
  CROSS --> s_reports
  SHELL --> s_theme
  SHELL --> s_cmd
  SHELL --> s_ui
```

라우팅 진입점은 `frontend/src/App.jsx:95` 의 `createBrowserRouter`. 새 페이지를
추가할 때는 보통 `src/modules/<name>/<Page>.jsx` 를 만들고 여기에 `<Route>` 한 줄
추가.

---

## 참고 문서

- `README.md` — 개발 환경 부팅 / 두 가지 배포 모드 요약
- `배포구조.md` — SIF 안/호스트 분리와 그 이유
- `배포방법최종.md` · `deploy/README_OPERATOR.md` — 실제 배포 절차
- `협업개선_설계.md` — 협업·개인공간·종합보고 3계층 설계 합의
- `개발가이드.md` · `개발계획.md` — 코드 스타일과 로드맵
