# 조직 간 공개(Cross-Org View) 설계 — 게시판·폴더 단위 외부 열람

ReportArchive를 "조직별로 격리된 보고 도구"에서 **"공개로 표시한 자료는 다른
조직도 볼 수 있고, 관계도로 자유롭게 연결되는 사내 지식 네트워크"** 로 확장하기
위한 데이터 모델·가시성·권한·UI·개발 계획.

전제(사용자 확정):
- 보고서 **연결(linking)은 조직 경계 없이** 전 시스템 가능(이미 그러함).
- 다만 "다른 조직 데이터가 얼마나 열리는가"를 **게시판·폴더 단위로 옵트인** 제어한다.
- 공개로 표시된 자료는 다른 조직에서 **조회(view)** 가능 → 관계도로 연결하면 본문까지 열람.

---

## 1. 배경 — 현재는 완전 격리, 메타만 새는 비대칭

코드 확인 결과(2026-06):

- **읽기 가시성**은 `reports/services.py:184 is_visible_to()` 한 곳에서 갈림:
  - 개인 워크스페이스: 직접 소유분만.
  - 조직 워크스페이스: **mount(게시)된 보고서 + 하위 트리**.
  - virtual(`_global`)·시스템관리자: 전부.
- **"공개" 개념은 현재 전혀 없음** — 전부 멤버십 기반 all-or-nothing.
- **연결(link)·관계도는 이미 전 시스템 개방**이라, 다른 조직 보고서의 **제목·작성자·
  날짜(메타)는 새지만 본문은 403**. → 즉 "연결은 되는데 못 본다"는 어정쩡한 상태.

이 설계는 그 비대칭을 해소한다: **공개로 표시한 것은 제대로 보이게, 아닌 것은 메타도
덜 새게**(2차 강화 후보) 만든다.

---

## 2. 핵심 결정 (사용자 확정)

- [x] **공개 단위 = 게시판 + 폴더 2단계.** 게시판(workspace)에 기본 공개정책,
      그 안 폴더(org folder)마다 override. 미분류(folder_id NULL)는 게시판 기본값을 탄다.
- [x] **공개 깊이 = "공개분은 항상 열람 가능".** 연결 여부와 무관하게 다른 조직
      누구나 검색·목록·관계도·연결·본문 열람 가능(인증된 사내 사용자 한정).
- [x] **설정 권한 = 게시판 매니저.** 그 게시판의 manager(보직장)가 자기 게시판/폴더의
      공개 여부 결정. 조직 폴더 CRUD 권한과 동일선상(`_is_workspace_admin`).
- [x] **노출 범위 = 본문 + 첨부만(읽기전용).** 외부 조직 열람자에겐 보고서 본문과
      첨부/이미지/HTML 임베드까지. **댓글·수정이력·편집·연결 추가/삭제는 차단.**

---

## 3. 가시성 모델 — `is_visible_to` 에 "공개" 한 갈래 추가

### 3.1 판정 규칙 (새 버전)
보고서 R 을, 워크스페이스 W 컨텍스트의 사용자가 **읽을** 수 있는가:

1. (기존) R 의 home workspace == W → 가시(개인 소유).
2. (기존) R 이 W 또는 그 하위 트리에 mount 됨 → 가시(조직 게시).
3. **(신규) R 이 "공개"로 표시된 mount 를 하나라도 가짐 → 모든 인증 사용자에게 가시.**
   - 공개 판정은 **mount 단위**(§4): 그 mount 의 `external_view` 가 effective TRUE.
   - W 가 어디든 상관없음 — 공개는 조직 경계를 넘는다.
4. (기존) virtual·시스템관리자 → 전부.

> 핵심: 공개는 **mount 에 매달린다**(보고서 자체가 아니라). 같은 보고서가 A 게시판엔
> 공개 폴더, B 게시판엔 비공개 폴더로 게시됐다면 — 공개 mount 가 하나라도 있으면
> 열람 가능. (자료 주인이 "이 게시판의 이 폴더에 한해 공개" 를 의도)

### 3.2 effective 공개 판정 (게시판 기본값 + 폴더 override)
```
mount.external_view_effective =
    folder.external_view            if mount.folder_id 있고 folder.external_view 가 NULL 아님
    else workspace.external_view_default
```
- 3-state(NULL=상속 / TRUE / FALSE)로 폴더가 게시판 기본을 덮어쓰거나 따른다.
- 미분류(folder_id NULL) mount → 게시판 기본값 직접 적용.

### 3.3 읽기 전용 강제
공개 경로로 들어온 외부 열람자는 **읽기 전용**. 권한 분리:
- **읽기 게이트**(`is_visible_to`): 공개분 포함하도록 확장.
- **쓰기 게이트**(`permissions.py can_edit`): **변경 없음** — 공개는 읽기만 연다.
  외부 사용자는 owner 도, editor 도, 그 mount 의 멤버도 아니므로 자동으로 편집 불가.
- **곁다리 차단**: 댓글·수정이력·링크 추가/삭제 엔드포인트는 별도로 "공개 열람자"를
  거부해야 함(§6) — 단순 멤버십 검사로는 안 막힐 수 있어 명시적 가드 필요.

---

## 4. 데이터 모델 변경

### 4.1 Workspace — 게시판 기본 공개정책
`workspaces/models.py` Workspace 에 컬럼 추가:
```python
external_view_default: Mapped[bool] = mapped_column(
    Boolean, nullable=False, server_default="false"
)  # 이 게시판에 게시된 보고서를 다른 조직이 조회 가능한가 (폴더가 override 가능)
```
- org 게시판에만 의미. personal/virtual 은 무시(개인공간은 공개 대상 아님 — §8 리스크).

### 4.2 Folder — 폴더별 override (3-state)
`folders/models.py` Folder 에 컬럼 추가:
```python
external_view: Mapped[Optional[bool]] = mapped_column(
    Boolean, nullable=True, server_default=None
)  # NULL=게시판 기본 상속, TRUE=공개, FALSE=비공개. org 폴더에만 의미.
```
- personal 폴더는 항상 무시(개인 폴더는 공개 대상 아님).

### 4.3 마이그레이션
- 신규 `pXX_cross_org_external_view.py`: 위 두 컬럼 add (둘 다 기존 행은 false/NULL =
  **현행 완전 격리 유지** → 안전한 무동작 디폴트).

### 4.4 조회 성능
- `is_visible_to` 의 신규 갈래(3)는 "공개 mount 존재" 한 방 쿼리로.
- 목록/그래프의 대량 판정용으로 **공개 report_id 집합**을 뽑는 헬퍼
  `public_report_ids(db)` 추가 — workspace.external_view_default + folder.external_view
  override 를 조인해 effective TRUE 인 mount 의 report_id 만. (`_scoped_report_ids` 와
  합쳐 "내 스코프 ∪ 공개분").

---

## 5. 영향 받는 읽기 경로 — 전수 점검

| 경로 | 파일 | 변경 |
|---|---|---|
| 단일 보고서 GET | `reports/routes.py:210` | `is_visible_to` 확장이 자동 반영 |
| 보고서 목록 | `reports/services.py list_reports_in_workspace` | "내 스코프 ∪ 공개분" 옵션. 단, **기본 목록엔 공개분 섞지 않음**(내 게시판 뷰가 오염되지 않게) — 별도 "공개 탐색" 뷰/필터로(§7) |
| linkable picker | `reports/services.py:1293` | 이미 전 시스템 — 변경 없음(단 §8 메타 노출은 그대로) |
| 전역 관계도 | `reports/services.py:1072` | scope = "내 스코프 ∪ 공개분"; 엣지는 양끝 모두 이 집합일 때 유지 → **공개 보고서가 노드로 보이고 클릭 시 본문 열림** |
| 보고서 중심 관계도 | `reports/services.py:932` | 이웃 노드 무제한(현행). 단 클릭→본문은 §3 가시성 따름 |

> **목록 vs 탐색 분리**(중요): "공개분 항상 열람 가능"이 곧 "내 게시판 목록에 남의 공개
> 보고서가 쏟아진다"는 아님. 기본 목록은 내 스코프만. 공개분은 (a) 관계도 노드로,
> (b) 검색/탐색 시 명시적으로, (c) 연결 picker 에서 접근. UX 오염 방지.

---

## 6. 곁다리 차단 — "본문+첨부만, 읽기전용"의 구현 지점

공개 열람자(= 그 보고서에 멤버십 없이 공개 경로로 접근한 사용자)에게 막아야 할 것:

| 곁다리 | 엔드포인트(추정) | 처리 |
|---|---|---|
| 댓글 조회/작성 | comments 모듈 | 공개 열람자엔 **숨김**(빈 목록) + 작성 403 |
| 수정 이력 | revisions/history | 403 또는 숨김 |
| 편집/락 | `reports can_edit`·edit-lock | 이미 차단(멤버십 없음) — 확인만 |
| 링크 추가/삭제 | `reports/routes.py:802,865` | `require_writer`+`can_edit` 로 이미 차단 — 확인만 |
| 연결된 다른 보고서 | `reports/routes.py:744 /links` | 본문 열람은 §3 따름. **링크 목록 자체는 노출**(연결 탐색이 목적) |
| 첨부/이미지/HTML임베드 | files·embed | 본문과 함께 허용. embed 는 이미 capability(무인증) |

**구현 핵심**: "이 사용자가 이 보고서에 대해 **공개 경로로만** 접근 중인가"를 판별하는
헬퍼 `is_public_only_viewer(db, actor, report)` 추가 →
= `is_visible_to` 가 갈래 1·2(멤버십/mount)로는 FALSE 인데 갈래 3(공개)으로만 TRUE.
이걸 댓글·이력 라우트에서 가드로 사용. (`_read_with_perms` 의 권한 플래그에도 반영해
프런트가 읽기전용 UI 를 그림.)

---

## 7. 프런트 — 설정 UI & 공개 표시 & 탐색

### 7.1 공개 설정 토글 (게시판 매니저)
- **게시판 기본값**: 게시판 관리 화면(또는 `FolderSidebar` 헤더)에 "다른 조직에 공개"
  토글 — `external_view_default`. 권한: 그 게시판 manager(`workspace.role==='manager'`).
- **폴더별 override**: `FolderSidebar` 의 폴더 행 메뉴에 3-state(상속/공개/비공개) —
  `folder.external_view`. 조직 폴더 편집 권한(매니저)과 동일 게이트.
- 백엔드: `WorkspaceUpdate` 스키마에 `external_view_default` 추가하되 **권한을 매니저로**
  (현재 PATCH /workspaces 는 시스템관리자 전용 → 공개 토글만 매니저 허용하는 별도
  엔드포인트나 게이트 완화 필요 — §8 리스크). 폴더는 `FolderUpdate` 에 `external_view`
  추가, 기존 org-folder 매니저 게이트 재사용.

### 7.2 공개 상태 시각화
- 폴더/게시판에 공개 뱃지(🌐). 공개 폴더 안 보고서 카드에도 "공개" 표시.
- 관계도: 다른 조직 공개 노드는 색/외곽선으로 구분(§11.4 클러스터링 색과 충돌 안 나게).

### 7.3 외부 조직 보고서 열람 화면
- 공개 경로로 연 보고서: 상단에 "다른 조직(○○팀)의 공개 보고서 · 읽기 전용" 배너.
- 댓글/편집/이력 UI 는 숨김(§6 권한 플래그 기반).

---

## 8. 리스크 / 열린 결정

- **메타데이터 누출(기존)**: linkable picker·보고서중심 관계도는 *비공개* 보고서의
  제목·작성자도 노출(현행). 이 설계는 *공개* 본문 열람을 추가할 뿐, 기존 메타 노출은
  안 건드림. → 더 조이려면 "비공개는 메타도 가림"이 별도 과제(2차).
- **공개 토글 권한 vs 현재 PATCH /workspaces**: 워크스페이스 수정은 지금 **시스템관리자
  전용**. 공개 토글을 **게시판 매니저**가 하려면 — (a) 공개 필드 전용 PATCH 엔드포인트를
  매니저 게이트로 새로 두거나, (b) WorkspaceUpdate 를 필드별 권한 분리. → **(a) 권장**
  (기존 시스템관리자 전용 정책 안 흔들고 공개만 위임).
- **개인공간 공개 제외**: 개인 워크스페이스/개인 폴더는 공개 대상에서 제외(혼란·사고
  방지). 개인 보고서를 공개하려면 **조직 게시판에 mount 후 그 폴더를 공개** — 명시적
  경로만 허용.
- **"항상 열람"의 목록 오염**: §5 의 목록 vs 탐색 분리로 방어. 기본 목록엔 공개분 안 섞음.
- **공개 철회 시**: external_view 를 끄면 즉시 비가시(라이브 판정이라 캐시 외 별도 작업
  불필요). 단 이미 연결된 링크는 남고 본문만 다시 403 — 연결 자체는 끊지 않음.
- **embed 번들 capability**: HTML 임베드는 이미 무인증 capability 서빙(별 설계). 공개
  보고서 본문에 박힌 임베드는 그대로 동작 — 일관됨.

---

## 9. 개발 단계 계획

### Phase 1 — 데이터 모델 + 읽기 가시성 (코어) ✅ 구현 완료(2026-06-01)
- [x] 마이그레이션: `workspace.external_view_default`, `folder.external_view`.
      → `migrations/versions/p16_cross_org_external_view.py` (기존 행 false/NULL =
      현행 격리 유지). upgrade/downgrade 라운드트립 확인.
- [x] `public_report_ids(db)` + `report_has_public_mount(db, id)` 헬퍼 +
      `is_visible_to` 갈래 3 추가. effective = `coalesce(folder.external_view,
      workspace.external_view_default)`, org 게시판만. 3-state override 동작 검증.
- [x] `is_public_only_viewer(db, report, ws)` 헬퍼 (멤버십 갈래 1·2 FALSE +
      공개 갈래 3 TRUE). `_is_member_visible` 로 갈래 1·2 공유 추출.
- [~] 단일 보고서 GET: `is_visible_to` 확장이 자동 반영(routes.py:219). 라이브 DB
      에 임시 공개 토글 후 `report_has_public_mount`/`public_report_ids` 동작을
      수동 검증(롤백). 영속 pytest 는 Phase 2 곁다리 테스트와 묶어 작성 예정.

### Phase 2 — 곁다리 차단 + 권한 플래그
- [ ] 댓글·이력 라우트에 `is_public_only_viewer` 가드(숨김/403).
- [ ] `_read_with_perms` 에 `is_public_view`·`can_comment`·`can_edit` 플래그 노출.
- [ ] 링크 추가/삭제가 공개 열람자에게 막히는지 확인(이미 막힘 — 회귀 테스트만).

### Phase 3 — 설정 UI + 권한 위임
- [ ] 공개 토글 전용 PATCH 엔드포인트(게시판 매니저 게이트).
- [ ] 폴더 `external_view` 3-state PATCH(매니저).
- [ ] `FolderSidebar`·게시판 헤더 토글 UI + 공개 뱃지.

### Phase 4 — 탐색 & 관계도 통합
- [ ] 전역 관계도 scope = "내 스코프 ∪ 공개분", 공개 노드 시각 구분.
- [ ] 공개분 탐색(검색/필터) 뷰 — 기본 목록과 분리.
- [ ] 외부 보고서 열람 배너 + 읽기전용 UI.

---

## 10. 파일별 변경 (요약)

- backend models: `workspaces/models.py`(+external_view_default),
  `folders/models.py`(+external_view).
- backend migration: `migrations/versions/pXX_cross_org_external_view.py`.
- backend visibility: `reports/services.py`(`is_visible_to` 확장 +
  `public_report_ids` + 그래프 scope), `shared/permissions.py`/신규
  `is_public_only_viewer`.
- backend routes: `workspaces/routes.py`(공개 토글 PATCH·매니저),
  `folders/routes.py`(external_view), `comments`·`history` 라우트 가드,
  `reports/routes.py`(`_read_with_perms` 플래그).
- backend schemas: `WorkspaceUpdate`/신규 공개 토글 스키마, `FolderUpdate`.
- frontend: `FolderSidebar.jsx`(토글·뱃지), 게시판 설정 UI,
  `ReportGraphPage.jsx`/`LinkGraphCanvas.jsx`(공개 노드 구분),
  보고서 상세(읽기전용 배너), `shared/api/`(workspaces·folders 공개 필드).
