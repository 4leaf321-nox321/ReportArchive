# 업데이트 내용

기능별로 한 줄씩 정리한 업데이트 안내입니다.

---

# 이번 업데이트 (v0.36.0 → v0.37.0)

## 보고서 위젯 복사 — 글·표·차트 (NEW)
- 보고서를 **볼 때** 위젯에 마우스를 올리면 오른쪽 위에 **복사 버튼**이 뜹니다.
  - **긴 글** → 글자를 그대로 복사합니다.
  - **표** → PowerPoint·Word엔 **표 모양 그대로**(색·테두리 포함), 엑셀엔 셀로 붙여넣어집니다.
  - **차트·산점도** → **PNG 이미지로 저장**(다운로드)합니다.
- 운영 환경(평문 HTTP)에서도 복사가 되도록 방식을 맞췄습니다. 기존 **MCP 토큰/명령 복사, 계정 이메일 복사** 등도 HTTP에서 안정적으로 동작합니다.

## 보고서 위젯 크기 조절
- 위젯을 **아래·왼쪽·오른쪽 가장자리 선**과 **아래 두 모서리**로 잡아 크기를 조절할 수 있습니다. 가장자리 가운데의 **＋ 추가 버튼**과 자리를 나눠 쓰며, 위젯이 너무 얇아지지 않도록 **세로 최소 높이**가 생겼습니다.

## 보고서 화면 정리 — 헤더·상태
- 제목 아래가 깔끔해졌습니다. **템플릿 종류·페이지 수·보고서 종류·작성/수정 정보**는 제목 옆 **ⓘ 버튼**을 누르면 보입니다.
- 상단의 **"리뷰 중 / 발행됨" 상태 띠를 없애고**, 제목 아래 **상태 뱃지를 클릭**하면 설명이 뜨도록 바꿨습니다. (편집 시 보고서 종류·관련 정보·연결된 보고서 칩은 같은 자리에 그대로 유지됩니다.)
- 창을 좁히면 버튼이 통째로 아래로 내려가지 않고, "종합보고 제출"이 "제출"로 줄고 버튼이 두 줄로 정리됩니다.

## 같은 폴더 보고서 — 좌측 사이드바
- 보고서 보기 화면 **왼쪽 끝의 폴더 버튼**을 누르면, 같은 폴더의 보고서 목록이 **세로 사이드바**로 펼쳐져 바로 이동할 수 있습니다. (기존 상단의 이전/다음 목록을 대체.)

## 보기 모드(페이지별/전체) — 보고서별 기억
- 보고서마다 기본 보기 모드(**페이지별 / 전체**)를 저장합니다. **편집 모드에서** 토글을 바꾸면, 그 보고서를 다시 열 때 같은 모드로 뜹니다.

## 전체화면 — 설정한 폭 존중
- 전체화면(발표)으로 볼 때, 보고서에 설정한 **폭을 그대로 유지**하고 가운데 정렬합니다(예전엔 화면 전체로 늘어났습니다).

## 공유 버튼 위치 이동
- 보고서 상단의 **"공유"** 버튼을 **"더보기" 메뉴 안**으로 옮겨 툴바를 정리했습니다.

## (관리자) 비밀번호 분실 요청 — 반려
- '비밀번호 찾기'로 들어온 요청 중 **오타·미가입 이메일**처럼 처리할 수 없는 건을 **반려**로 정리할 수 있습니다(계정 관리 화면). 가입된 계정이 없는 이메일이라는 안내도 함께 표시됩니다.

---

# 이전 업데이트 (v0.35.0 → v0.36.0)

## Claude(AI)로 보고서 작성 — 여러 페이지 + 단락 구분
- Claude(MCP)로 보고서를 만들 때 **여러 페이지**로 나눠 작성할 수 있습니다(예: 배경·분석·결론을 페이지별로). 페이지 strip으로 탐색됩니다.
- 각 블록에 **단락 구분**(배경·분석·의사결정·일정 등)을 달 수 있습니다 — 보고서에서 단락 색상 칩으로 표시됩니다. (등록된 단락 코드만 적용되고, 적절한 코드가 없으면 생략.)

---

# 이전 업데이트 (v0.34.1 → v0.35.0)

## Claude(AI)로 보고서 작성 — 더 똑똑하게
- **빈 템플릿으로도** Claude가 필요한 **위젯을 직접 만들며** 보고서를 짭니다(차트·표·비교표 등). 적합한 템플릿이 없어도 처음부터 작성 가능.
- AI 보고서는 **채운 위젯만** 깔끔하게 보입니다(템플릿의 빈 칸은 자동 숨김). 배치도 서버가 자동(2단 매거진).
- 위젯마다 **상세 작성 룰**(형식·자주 틀리는 포인트·혼동되기 쉬운 위젯)을 AI가 받아 따르므로, 레이더·박스플롯·Sankey 같은 까다로운 차트도 한 번에 정확히 그려집니다. (이 룰은 '복사용 프롬프트'와 **동일한 단일 소스**라 항상 일치.)

## (수정) 부서 전용 템플릿으로 만든 AI 보고서 로딩
- 부서 전용 템플릿으로 만든 보고서가 개인공간에서 "템플릿 불러오는 중"에 멈추던 문제를 고쳤습니다 — 이제 그 **계정이 접근 가능한** 템플릿이면 정상 로딩됩니다.

---

# 이전 업데이트 (v0.34.0 → v0.34.1)

## 로그인 — 아이디 저장 / 로그인 유지
- 로그인 화면에 **"아이디 저장"**(다음 로그인 때 아이디 자동 채움) + **"로그인 유지"**(자동 로그인) 체크박스가 생겼습니다. 비밀번호 자체는 앱이 저장하지 않고 브라우저 비밀번호 매니저에 맡깁니다.
- "로그인 유지"를 끄면 브라우저(탭)를 닫을 때 자동 로그아웃됩니다. 선택값은 기억됩니다.

## (운영) MCP 서버 외부 노출 수정
- MCP 서버를 사내망에 열 때(`0.0.0.0`) "Invalid Host header"로 막히던 문제를 고쳤습니다.
- 배포 시 **한 번만** `MCP_HOST=0.0.0.0`을 지정하면 이후 배포가 자동으로 그 설정을 유지합니다(매번 다시 지정 불필요). 더 단단히 하려면 `MCP_ALLOWED_HOSTS`로 허용 호스트를 지정할 수 있습니다.

---

# 이전 업데이트 (v0.33.0 → v0.34.0)

## 사이드바 접기/펼치기
- 왼쪽 부서 메뉴 사이드바를 **접었다 펼 수 있습니다**. 사이드바 오른쪽 경계 가운데의 손잡이(`‹`/`›`)를 누르면 접히고, 접으면 본문이 화면 전체 폭을 씁니다. 접힌 상태는 **새로고침해도 유지**됩니다.

## HTML 임베드 기본 표시 = 인라인
- HTML 임베드 위젯의 기본 표시가 **인라인**(박스 안에서 iframe 을 바로 펼침)으로 바뀌었습니다. '카드'(표지 + 열기 버튼) 모드는 위젯 편집에서 선택할 수 있습니다.
- 참고: 기존에 카드로 보이던 임베드는 이번 변경 이후 인라인으로 표시될 수 있습니다(명시적으로 인라인을 저장했던 것 제외).

---

# 이전 업데이트 (v0.32.1 → v0.33.0)

## AI로 만든 보고서, 보기 좋게 자동 배치
- Claude(MCP)로 만든 보고서 초안이 이제 위젯을 **2단 그리드로 자동 배치**합니다 — 차트·다이어그램은 두 개씩 나란히, 표·비교표·마일스톤은 전폭으로. (기존엔 모든 위젯이 전폭으로 세로로만 쌓여 밋밋했습니다.)
- AI는 레이아웃을 따로 지정할 필요가 없고, **템플릿이 이미 직접 배치를 잡아둔 경우엔 그 배치를 그대로 존중**합니다.

## AI 작성 품질 개선
- 파이/도넛 차트를 AI로 만들 때 **도넛 모양·라벨 표시 옵션이 그대로 유지**됩니다(이전엔 일반 파이로 떨어졌습니다).
- 진행률·마일스톤·순서도 등 **더 많은 위젯을 자연어 요청으로 채울 수 있고**, AI가 못 채운 블록은 숨기지 않고 알려줍니다.

## MCP 토큰 위치 이동
- 개인 MCP 토큰 발급이 **프로필 → "AI 설정"의 MCP 탭**으로 옮겨졌습니다(발급·목록·취소 기능은 동일).

---

# 이전 업데이트 (v0.31.0 → v0.32.1)

## Claude(AI)로 보고서 작성 — MCP 서버
- Claude(Claude Code/Desktop)에서 **자연어로 요청하면 보고서가 시스템에 초안으로 생성**됩니다.
  "프롬프트 복사 → AI에 붙여넣기 → 결과 다시 붙여넣기" 과정을 직접 호출로 대체.
- **항상 초안(draft)** 으로 만들어지고 사람이 검토·게시합니다. 사용자별 토큰으로 동작해 **기존 권한**이 그대로 적용됩니다.
- 구성: AI의 느슨한 입력을 시스템 형식(widget-v1)으로 변환·검증하는 백엔드(`/api/reports/ai-draft`,
  `/api/reports/authoring-guide`) + 별도 MCP 서버(`mcp_server/`, 도구 5종) + 작성 가이드 스킬.
- 배포: `deploy.sh` 가 MCP 서버 venv·의존성·서비스까지 자동 설치(오프라인 휠 동봉). 안내는
  `mcp_server/README.md`, `deploy/README_OPERATOR.md`. (개발: 백엔드 `:3000` + MCP `:3002`)

## 내 MCP 토큰 (프로필)
- **프로필 → "MCP 토큰"** 에서 Claude 연동용 개인 토큰을 직접 발급합니다. 발급값은 **한 번만** 표시되며,
  바로 쓸 수 있는 `claude mcp add` 명령도 함께 제공합니다.
- 토큰은 **이름·만료기간** 지정, **목록·마지막 사용 시각 확인·즉시 취소**가 가능합니다(유출 대비). 서버엔 토큰
  원문이 아닌 해시만 저장됩니다.

---

# 이전 업데이트 (v0.30.0 → v0.31.0)

## 수정 이력 · 되돌리기 (버전 관리)
- 보고서를 저장할 때마다 그 시점의 내용이 **버전으로 기록**됩니다. 보기 모드에서 **더보기(⋯) → "수정 이력 / 되돌리기"** 로 과거 버전을 타임라인으로 보고, 선택해 미리본 뒤 그 버전으로 되돌릴 수 있습니다.
- **되돌리기는 비파괴적** — 과거 버전으로 돌려도 현재 상태가 이력에 그대로 남아 언제든 다시 되돌릴 수 있습니다. (실수로 위젯을 지우고 저장했어도, 삭제 전 버전으로 복원 가능)
- 보고서당 **최근 20개**를 보존하고(오래된 건 자동 정리), 영상·이미지는 참조만 담겨 용량 부담이 적습니다.

## 파일 정리 (관리자) 보강
- 수정 이력 기능과 연동되어, **옛 버전이 참조하는 파일은 오펀(미참조)으로 잡지 않습니다** — 과거 버전 미리보기·되돌리기가 깨지지 않습니다.

---

# 이전 업데이트 (v0.29.0 → v0.30.0)

## 본문 검색 (전문검색)
- 이제 제목뿐 아니라 표·긴 글 등 **보고서 본문 내용까지** 검색됩니다. 상단 검색창에 입력하고 Enter 하면 큰 검색 결과 페이지가 열려 한눈에 고를 수 있습니다.
- 여러 단어를 넣으면 그 단어를 **모두 포함**한 보고서를 찾습니다(단어 AND). 한국어 부분일치도 됩니다("보고서"로 "보고서를"도 검색).
- 검색 결과는 **내가 볼 수 있는 범위**(소유·공유·게시) 안에서만 나오고, 휴지통은 제외됩니다.
- 상단 검색창은 이제 모달을 띄우지 않고 바로 검색 페이지로 이동합니다(명령 팔레트는 ⌘K로 그대로 사용).

## 파일 정리 (관리자)
- 관리자 메뉴에 **"오펀 파일 정리"** 가 생겼습니다 — 어느 보고서·종합보고에서도 쓰지 않는 업로드 파일(보고서에서 뺀 영상·이미지 등)을 검색·선택해 삭제하고 디스크 용량을 회수합니다.
- 갓 올린 파일이 실수로 지워지지 않게 **유예 기간**(즉시/24시간/48시간/7일)으로 보호하고, 삭제 직전 다시 검증해 그 사이 보고서가 쓰게 된 파일은 자동으로 건너뜁니다.

---

# 이전 업데이트 (v0.28.0 → v0.29.0)

## 종합보고
- 2단 보기에서 안건이 하나도 없는 빈 열에도 드래그&드롭으로 안건을 놓을 수 있습니다(그 열·그룹으로 정확히 이동). 기존엔 빈 열에 놓으면 엉뚱한 위치로 가던 문제를 고쳤습니다.

## 이미지 붙여넣기
- 이미지·비교표 위젯에 이미지를 붙여넣을 때, 클립보드에 여러 포맷이 있으면 가장 높은 해상도를 자동으로 골라 올립니다.
- 붙여넣은 이미지가 저해상도면 경고로 알려 주고, 원본 파일을 직접 올리거나(드래그·파일 선택) PPT에서 이미지를 키운 뒤 복사하도록 안내합니다.

---

# 이전 업데이트 (v0.27.0 → v0.28.0)

## 표·비교표 셀 편집
- 표 헤더에서 각 열의 입력 형식(텍스트·숫자·날짜·선택지)을 바로 고를 수 있고, 선택지 항목도 그 자리에서 편집할 수 있습니다(기존엔 템플릿 속성에서만 가능).
- 표·비교표에서 셀을 여러 개 선택해 글자 서식(굵기·기울임·밑줄·취소선·크기·글꼴·색)을 한꺼번에 바꿀 수 있습니다.
- 숫자·날짜처럼 글자 일부만 드래그가 안 되는 셀도 색과 크기를 셀 단위로 바꿀 수 있습니다(비교표의 행 이름·이미지 셀 포함).
- 글자 크기의 "기본" 항목에 실제 크기(예: 기본 18px)를 함께 표시합니다.
- "셀 색"을 "셀 배경"으로 정리하고, 글자색은 "글자 서식"으로 모았습니다.
- 글자 색·크기 팔레트가 가끔 안 닫히던 문제와, 여러 셀을 한꺼번에 바꿀 때 팔레트가 여러 개 뜨던 문제를 고쳤습니다.

---

# 이전 업데이트 (v0.26.0 → v0.27.0)

## 종합보고 양식(시작 양식)
- 종합보고의 "요약·그룹 골격·보기 설정"을 이름 붙인 양식으로 저장해, 새 종합보고를 그 골격으로 바로 시작할 수 있습니다(안건은 직접 추가).
- 종합보고 상세의 "양식으로 저장", 새 종합보고 만들기의 "시작 양식 선택"으로 사용합니다.
- 시작 양식 선택을 크게 띄운 모달에서 전사·조직별·개인(내 양식)으로 분류해 고를 수 있습니다.

## 템플릿 관리
- "템플릿 관리"가 「보고서 템플릿」·「종합보고 양식」 두 탭으로 나뉘었고, 종합보고 양식의 이름·설명·공개 범위·그룹 골격을 여기서 수정·삭제할 수 있습니다.
- 보고서 템플릿과 종합보고 양식 모두 전사·조직별·개인으로 분류해 볼 수 있습니다.

## 위젯
- 새 위젯의 "제목 생략" 선택을 위젯 종류별로 기억해, 다음에 같은 종류를 추가하면 자동으로 그 상태로 시작합니다(계정에 저장되어 기기를 바꿔도 유지).

## 보고서 화면 정리
- 보고서 상단 버튼이 많아 헷갈리던 점을 줄였습니다 — 맨 오른쪽 "더보기(⋯)" 메뉴를 만들고 폴더 이동·활동 이력·수정 잠금·관계도·재사용 저장을 그 안으로 모았습니다.
- "제출" 버튼을 "종합보고 제출"로 바꿔 게시·공유와 헷갈리지 않게 했습니다.

---

# 이전 업데이트 (v0.25.1 → v0.26.0)

## 글자 색·서식
- 표·비교표 셀과 제목(heading)에서도 글자 일부만 선택해 색을 칠할 수 있습니다('긴 글'처럼). Word로 내보낼 때도 색이 유지됩니다.

## 주석(이미지·차트·산점도)
- 이미지 주석의 박스·라벨 위치가 편집 화면과 실제 보기 화면에서 일치하도록 맞췄습니다(라벨 글자가 상자 밖으로 넘치던 것도 수정).
- 라벨을 달지 않았던 주석에도 선택 후 "+ 라벨"로 라벨을 추가할 수 있습니다.
- 주석을 선택한 뒤 Esc를 누르면 위젯 편집 창이 닫히지 않고 선택만 해제됩니다(라벨 입력 중 Esc는 입력 종료).
- 점 주석을 라벨 없이 여러 개 빠르게 찍을 때 화면이 멈추던 문제를 해결했습니다.

## 부서 이동
- 좌측 상단 부서 선택기 아래에 좌·상·하·우 화살표가 생겨, 형제/상위/하위 부서로 바로 이동할 수 있습니다(키보드 화살표도 지원).
- 부서를 바꿔도 보던 화면(보고서·관계도·대시보드·종합보고)과 설정이 유지되고, 그 부서의 데이터로 바뀝니다(보고서는 폴더 구조가 달라 메인 목록으로).

## 관계도
- 관계도 범위를 "이 게시판만 / 하위 부서까지" 중에서 고를 수 있고, 범위 밖이지만 연결된 보고서를 표시하거나 숨길 수 있습니다.

## 긴 글
- 여러 줄 '긴 글'에서 Delete 키로도 다음 줄을 끌어올려 합칠 수 있습니다(기존엔 Backspace만 가능).

## 템플릿·권한
- 일반 멤버도 자기 부서 템플릿을 직접 만들고 수정·삭제할 수 있습니다(전사 공개·다른 부서 공유는 매니저 권한).

---

# 이전 업데이트 (v0.25.0 → v0.25.1)

## 본문 멘션·링크
- @멘션으로 다른 보고서·부서로 이동한 뒤, 화면에 뜨는 "돌아가기" 버튼으로 읽던 곳(스크롤 위치까지)으로 한 번에 복귀합니다.
- "부서" 멘션을 클릭하면 곧바로 이동하지 않고, 부서 정보와 최근 보고서를 미리보기 카드로 먼저 보여줍니다(원할 때만 "부서 보고서 열기").
- 멘션 작성 시 작성자·부서 검색 창이 입력 창 뒤에 가려지던 문제를 고쳤습니다.

## 위젯 참조(그림·표 번호)
- 본문의 # 그림·표 참조를 클릭하면, 가까이 있으면 해당 위젯이 잠깐 강조되고, 화면 밖·다른 페이지에 있으면 미리보기 카드로 그 자리에서 바로 확인합니다.
- 미리보기에서 "위젯으로 이동"으로 해당 위젯까지 가고, "돌아가기"로 읽던 곳으로 복귀합니다.

## 보고서 보기
- 뒤로가기·"돌아가기"로 돌아올 때 읽던 스크롤 위치가 그대로 복원됩니다.

## 계정·기타
- 계정 관리에서 이메일 복사가 일부 환경(HTTP 접속)에서 안 되던 문제를 고쳤습니다.

---

# 이전 업데이트 (v0.24.0 → v0.25.0)

## 본문 멘션·링크
- 본문에서 @를 입력해 다른 보고서나 협업 부서를 링크하고, "연결된 보고서"로도 함께 등록할 수 있습니다.
- @멘션에 "관련 정보(엔티티)" 유형이 추가되어 모델명 등도 본문에서 연결할 수 있습니다.

## 위젯 참조(그림·표 번호)
- 그림·표 등 위젯에 번호가 자동으로 매겨지고, 캡션 앞에 "그림 3."처럼 표기됩니다(순서를 바꾸면 자동 재번호).
- 본문에서 #를 입력해 같은 보고서의 그림·표를 골라 참조를 넣고, 클릭하면 해당 위젯으로 이동합니다.

## 글자 색·서식
- 본문·캡션 등에서 글자색을 18색 팔레트로 고를 수 있고, 다크모드에서도 자동으로 잘 보이게 조정됩니다.
- 본문은 문장 일부만 선택해 색과 글꼴(맑은 고딕·바탕·굴림 등)을 바꿀 수 있습니다.
- 표·이미지의 캡션과 ※참고 내용도 글자 일부만 색·서식을 지정할 수 있습니다.
- 외부(엑셀·워드)에서 붙여넣은 검정 글씨가 다크모드에서 안 보이던 문제를 해결했습니다.

## 표·비교표 셀
- 표·비교표에서 셀을 선택해 배경색과 글자색을 칠할 수 있습니다.

## 보고서 보기
- 전체화면을 문서 전체가 채워지는 진짜 발표 모드(Fullscreen)로 바꿨습니다.

## 게시·공유·권한
- 게시 다이얼로그에서 게시 현황 확인·게시 취소가 가능하고, 좌우 2단·모두 펼치기를 지원합니다.
- 콘텐츠·게시판·폴더를 아우르는 통합 권한(공유) 체계로 정비했습니다.
- 게시 편집 정책에 "작성자 + 게시판 매니저만 편집"을 추가했습니다.

## 종합보고
- 종합보고를 새로 만들면 바로 편집 모드로 들어갑니다.

## 계정·로그인
- 비밀번호를 잊었을 때 "비밀번호 찾기"로 요청하면 관리자가 임시 비밀번호를 발급합니다(최초 로그인 시 새 비밀번호로 변경).
- 회원가입 이메일 호스트 기본값을 samsung.com으로 채워 잘못된 도메인 가입을 줄였습니다.

## 기타
- 화면 좌측 상단에 현재 버전이 표시됩니다.

---

# 이전 업데이트 (v0.19.0 → v0.24.0)

## 보고서 작성·편집
- 페이지 순서를 페이지 칩을 끌어서 자유롭게 바꿀 수 있습니다(맨 뒤로도 이동).
- 위젯 크기를 조절할 때 가로 폭(%)이 실시간으로 표시됩니다.
- 위젯 편집 창이 실제 화면 폭으로 열려, 편집한 모습이 결과와 그대로 일치합니다.
- 위젯에 마우스를 올리면 "편집" 버튼이 뜨고, 더블클릭으로도 편집할 수 있어 드래그 중 실수로 편집창이 열리지 않습니다.
- 위젯 크기 조절이 마우스를 부드럽게 따라오도록 개선했습니다.
- 위젯을 추가하는 화살표 버튼이 더 크고 잘 보이게 바뀌었습니다.
- 같은 폴더의 이전/다음 보고서로 바로 이동하고, 접이식 형제 목록으로 둘러볼 수 있습니다(백스페이스로 목록 복귀).
- 목록으로 돌아갈 때 보던 폴더/주차를 그대로 유지합니다.
- 보고서 저장 시 위젯 위치·높이가 어긋나던 문제를 줄였습니다.

## 관련 정보·태그
- 보고서 상단에 "관련 정보"(모델명 등)를 고정 배너로 한눈에 볼 수 있습니다.
- "협업 부서"를 부서 목록에서 골라 관련 정보에 등록할 수 있습니다.

## 표·비교표·차트 위젯
- 표 위젯의 열 폭을 헤더에서 끌어 조절하고, 선택한 열만 폭을 균일하게 맞출 수 있습니다.
- 표·비교표의 절대 폭을 지정하고, 마지막 열 핸들로 표 전체 폭을 조절할 수 있습니다.
- 비교표에 엑셀(표) 내용을 복사-붙여넣기 할 수 있습니다.
- 표·이미지·비교표 아래에 "※ 참고" 내용을 적을 수 있습니다.
- 여러 위젯에서 콘텐츠 글자 크기 설정이 실제로 반영됩니다.
- 등고선 차트의 크래시를 고치고 숫자 비례 축을 지원합니다.

## 게시·공유·템플릿
- 조직 게시판에는 해당 조직의 게시글만 보이도록 정리했습니다.
- 보고서를 복사할 때 "전체 복사 / 본문만 복사"를 고를 수 있습니다.
- 게시 다이얼로그를 개선했습니다(폴더 라벨, 창 크기, 게시 보드 자동 펼침).
- 템플릿을 다른 부서에 공유할 수 있습니다(공유 부서 트리에서 선택).
- 내용이 채워진 채로 시작하는 "시작 프리셋"을 추가하고 템플릿 관리 화면을 개편했습니다.

## 종합보고
- 단일 / 2단 / 리스트(좌측 목록 + 우측 상세) 보기 모드를 선택할 수 있습니다.
- 설명과 안건 사이에 위젯으로 자유롭게 작성하는 "요약" 영역이 생겼습니다.
- 안건을 그룹으로 묶고, 그룹 이름 수정·순서 변경·드래그 합류·해제를 할 수 있습니다.
- 안건에 소속 부서가 표기되고, 안건 추가 화면에 부서 트리 필터와 일괄 선택이 생겼습니다.
- 안건을 추가할 때 하위 부서 게시글까지 고를 수 있습니다(종합보고끼리의 중첩은 제외).
- 보고서에서 "제출"로 종합보고에 안건을 신청하면 작성자가 승인해 추가합니다(여러 명이 동시에 모아도 충돌 없이 안전).
- 목록으로 돌아갈 때 보던 주차를 그대로 유지합니다.

## 협업·기타
- 미해결 코멘트가 없으면 보고서를 열 때 코멘트창이 닫힌 상태로 시작합니다.
- "내 활동" 화면에서도 부서 매니저에게 부서 메뉴가 보입니다.
- 일부 파일(임베드) 업로드 시 발생하던 오류를 수정했습니다.
