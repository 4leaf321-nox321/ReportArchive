# 지식그래프 — 보고서 관계도 시각화 계획

## 1. 목적

옵시디언 그래프 뷰처럼 보고서 간 관계를 한 화면에 직관적으로 보여준다.
다만 옵시디언이 "노트 + 태그" 두 노드 종류만 가지는 데 비해 우리는 보고서가
가진 풍성한 메타데이터(보고서 종류, Entity 태그, 종합보고 소속, 일자,
작성자, 게시 조직)를 활용해 더 본격적인 *관계도* 로 확장한다.

핵심 가치
- 한 사안의 시계열 흐름과 분기 구조가 한 눈에 보임
- 같은 모델/시험/부품을 다룬 보고서들이 자연스럽게 모임 (entity 공유 클러스터)
- 종합보고가 어떤 안건들을 묶고 있는지 hub-spoke 로 가시화
- 시간축을 도입해 "이 사안이 1월부터 6월까지 어떻게 진행됐나" 같은
  옵시디언이 못 보여주는 관점 제공

## 2. 그래프의 entity 종류

### 2.1 노드 타입

| 타입 | 시각 | 의미 |
|---|---|---|
| **report** | 큰 원 | 본체. 색·크기는 사용자 선택 |
| **entity** | 작은 다이아몬드 | 관련 정보 태그 (모델/부품/시험 등 axis 별 색) |
| **composite** | 사각형 | 종합보고. 안건 보고서들의 hub |
| **author** (옵션) | 작은 사람 아이콘 | 사람 중심 협업 패턴 |

기본은 report 만. 사용자가 토글로 다른 레이어를 더하거나 빼는 방식 (옵시디언의 "Show tags" 패턴).

### 2.2 엣지 타입

| 타입 | 시각 | 의미 |
|---|---|---|
| **explicit_link** | 진한 색·화살표 | report ↔ report, `report_links.kind` 별 색 |
| **has_tag** | 회색·실선 | report → entity |
| **composite_member** | 점선 | composite → report |
| **authored_by** (옵션) | 옅은 색 | report → author |

### 2.3 시각 코딩 (사용자 선택)

- **노드 색** : 보고서 종류 / 작성자 / 게시조직 / 가장 강한 entity axis — 드롭다운으로 갈아끼움
- **노드 크기** : degree(연결수) / 최근 활동 / 일정 + degree 조합
- 기본값: 색 = 보고서 종류, 크기 = degree

## 3. 시간축 활용

보고서가 일자를 가진다는 점은 우리만의 강점.

- **기간 필터 슬라이더** : 두 핸들 — 그 기간 안의 보고서만 표시 + force layout 다시 흐름. presets (지난 1주/1개월/3개월/1년) 제공.
- **타임라인 정렬 모드** : 노드 X 좌표를 `report_date` 로 고정, Y 는 force. 좌→우 시간 흐름이 한 눈에. 옵시디언 그래프에 없는 차원.
- **재생(Play) 모드** : 시간 슬라이더를 자동으로 흘려 노드·엣지가 시점 순으로 등장. 회고/분기 발표용으로 강력.

## 4. 필터 패널 (좌측 슬라이드 또는 상단 바)

- 기간 슬라이더 (`report_date` 기반)
- 보고서 종류 multi-select
- Entity axis 별 multi-select (모델 X 와 시험 Y 의 교집합)
- 작성자 (cmdk 검색 가능 콤보 — picker 의 FilterCombo 재사용)
- 게시 조직 (마찬가지)
- Link kind 토글 (참조 / 후속)
- 「고립 노드 숨김」, 「내가 만진 보고서만」 토글

선택된 필터는 URL 쿼리스트링에 보존 → 공유 가능
(예: "최근 3개월 모델X 관련 보고서만").

## 5. 진입점 두 가지

같은 React 컴포넌트가 두 진입점에서 동작 — 차이는 데이터 소스와 초기 필터.

### 5.1 로컬 그래프
- 보고서 상세 → 「📊 관계도로 보기」 버튼 (메타 칩 옆 또는 본문 하단 「연결된 보고서」 섹션 옆)
- 모달로 펼침. 그 보고서 ±N hop 만 표시
- 깊이 슬라이더 (1~3)

### 5.2 글로벌 그래프
- 워크스페이스 메뉴 → 「🕸 관계도」 전용 페이지
- 모든 필터·시간축·매핑 옵션 노출
- 기본 필터는 워크스페이스 한정 (글로벌 무필터는 limit 안내)

## 6. API 설계

### 6.1 글로벌
```
GET /api/reports/link-graph
    ?date_from=YYYY-MM-DD
    &date_to=YYYY-MM-DD
    &types=<id>&types=<id>               # 보고서 종류 (OR within)
    &entities=<id>&entities=<id>         # entity ids (axis 별 AND)
    &authors=<name>&authors=<name>
    &mounts=<slug>&mounts=<slug>
    &kinds=reference&kinds=successor     # link kind 필터
    &include_tags=false                  # entity 노드 추가
    &include_composites=false
    &include_authors=false
    &limit=500
```

### 6.2 로컬
```
GET /api/reports/{id}/link-graph
    ?depth=2
    &include_tags=true
    &<공통 필터>
```

### 6.3 응답 shape (둘 다 동일)
```jsonc
{
  "nodes": [
    {
      "type": "report",
      "id": 123,
      "title": "...",
      "owner_name": "...",
      "workspace_slug": "...",
      "report_type_id": 4,
      "report_date": "2026-05-12",
      "mount_slugs": ["team1"],
      "tag_ids": [9, 12, 33]
    },
    { "type": "entity", "id": 9, "label": "모델X", "axis": "model" },
    { "type": "composite", "id": 7, "title": "주간업무보고 5월 4주" }
  ],
  "edges": [
    { "from": "report:123", "to": "report:456", "kind": "successor" },
    { "from": "report:123", "to": "entity:9", "kind": "has_tag" },
    { "from": "composite:7", "to": "report:123", "kind": "composite_member" }
  ],
  "truncated": false,
  "center_id": "report:123"  // 로컬에서만
}
```

ID 가 `type:numericId` 형태인 것은 React-force-graph 에서 노드 id 가 unique
string 이어야 하기 때문. 그래야 report 와 entity 의 동일 id 가 충돌하지 않음.

## 7. 라이브러리

**`react-force-graph-2d`** — Canvas, ~5000 노드까지 부드러움.
- 노드 타입별 모양/색 커스텀 가능 (`nodeCanvasObject`)
- 줌·팬·드래그 기본
- WebGL 버전이 같은 API라 나중 확장 쉬움
- 1MB 안팎으로 가벼움

대안: 직접 D3-force + SVG — 100 노드 이하면 충분하지만 줌·드래그·force tick 다 직접 구현 필요. 시작점으로는 비추.

## 8. 단계 계획

각 phase 가 독립적으로 가치가 있어 단계마다 멈춰서 평가 가능.

### Phase 1a — 로컬 그래프 MVP (2~3일) ✅ 구현 완료
- [x] 백엔드 `GET /api/reports/{id}/link-graph` (BFS depth=2, ±depth hop 양방향)
      — `services.build_link_graph()` + `LinkGraph*` 스키마, 노드 상한 300 +
      `truncated` 플래그
- [x] 프론트 `react-force-graph-2d` 도입 (lazy chunk — 모달 열 때만 로드)
- [x] `ReportGraphModal` — 보고서 상세 툴바에 「관계도」 버튼 (뷰/편집 공통)
- [x] 노드: report만. 엣지: explicit_link 만 (link kind 별 색 + 화살표)
- [x] 색=회색 단색 (종류색 매핑은 Phase 2 로 보류 — 결정 §10), 크기=degree
- [x] 클릭 → 보고서 이동, hover → 미니카드(tooltip), 깊이 슬라이더 (1~3)
- [x] 중심 노드 강조 (짙은 톤 + indigo 링), 빈 그래프 안내 문구

여기서 가치 검증. 의미 있다 판단되면 1b 로 진행, 아니면 다른 방향 모색.

> 구현 메모 (백엔드/데이터):
> - 엣지는 plan §6.3 의 `from`/`to` 대신 force-graph 기본 accessor 와 같은
>   `source`/`target` 으로 내려보낸다 (프론트 무변환). 노드 `id` 는
>   `"report:<id>"` unique string, raw id 는 `report_id`.
> - BFS 는 hop 단위 `IN` 쿼리로 양방향 확장, 노드 상한 300 + `truncated`.
>   degree 는 서버가 채워 내려준다.
>
> 구현 메모 (프론트 — 테스트하며 잡은 것들):
> - **신규 의존성 주의**: `react-force-graph-2d` 를 새로 깐 직후엔 Vite dev
>   서버를 재시작해야 lazy `import()` 가 동작 (dep pre-bundle).
> - **캔버스 크기**: Radix Dialog(portal+open 애니메이션) 안에서 ResizeObserver
>   초기 측정이 0 으로 갇히는 문제 → DialogContent 를 `flex flex-col h-[80vh]`
>   고정, 본문 `flex-1 min-h-0` 으로 두고 getBoundingClientRect+rAF+RO 3중 측정.
> - **과확대**: 노드가 적으면 `zoomToFit` 이 화면을 꽉 채우려 과확대 →
>   fit 후 줌 배율을 `MAX_FIT_ZOOM`(현재 1.6) 으로 클램프.
> - **드래그 깜빡임**: 노드 드래그 → 시뮬레이션 재가열 → `onEngineStop` 재발화
>   → zoomToFit 재실행으로 줌이 튐. `didFitRef` 로 자동 맞춤을 "데이터 로드당
>   한 번"만 하도록 가드 (graph 바뀔 때 리셋).
>
> 추가/변경 파일:
> - backend: `reports/services.build_link_graph`, `reports/schemas.LinkGraph*`,
>   `reports/routes` GET `/{id}/link-graph`
> - frontend: `reports/ReportGraphModal.jsx` (신규), `shared/api/reportLinks.js`
>   `getReportLinkGraph`, `shared/reports/linkKinds.js` `colorHex`,
>   `reports/ReportLinks.jsx` `useLinkKinds` export, `reports/ReportDetailPage.jsx`
>   툴바 버튼+모달, `package.json` `react-force-graph-2d`

### Phase 1b — 글로벌 진입점 추가 (1~2일) ✅ 구현 완료
- [x] 백엔드 `GET /api/reports/link-graph` (기간/종류/kind 필터, limit=500)
      — `services.build_global_link_graph` + `_scoped_report_ids`. `/{report_id}`
      동적 path 보다 위에 등록.
- [x] `/w/{slug}/report-graph` 전용 페이지(`ReportGraphPage`) + 사이드바
      메뉴 entry (보고서 ↔ 종합보고 사이, Network 아이콘)
- [x] 1a 캔버스 재사용 — `LinkGraphCanvas` 로 추출, 모달/페이지 공유
      (fetch·필터·메시지는 호스트가, 렌더·줌·사이징은 캔버스가)
- [x] limit=500 + "필터를 좁혀 주세요" 안내 (degree 높은 허브부터 남김)
- [x] 워크스페이스 스코핑 — X-Workspace-Slug 헤더 기준. virtual 뷰면 무스코핑

> 설계 결정 (1b): 글로벌 그래프는 **연결된 보고서만** 그린다(고립 노드 제외).
> 워크스페이스 전체 보고서를 점 구름으로 띄우면 무의미하기 때문. "고립 노드
> 표시" 토글은 Phase 4 후보. 스코프 안에서 양 끝이 모두 보이는 link 만 엣지로.

### Phase 2 — Entity 통합 (3일) — 🟢 대부분 완료
- [x] **노드 색 매핑 옵션** (글로벌 페이지) — 보고서 종류 / 작성자 / 단색
      드롭다운 + 색 범례. `shared/reports/graphColors.js` (빈도순 12색 배정,
      초과분은 「기타」 회색), `LinkGraphCanvas` 에 `nodeColor` prop 추가.
      기본값 = 보고서 종류 (plan §2.3). 조직(mount) 기준은 추후.
- [x] **entity 노드 + has_tag 엣지** (레이어 컨트롤, OFF 기본) — 모달·글로벌
      페이지 양쪽. entity 는 axis 색 다이아몬드(`axisColor`, slug 해시 고정색),
      report 는 원. has_tag 엣지는 회색 실선·무화살표. entity 는 *레이어*
      성격 — 이미 그래프에 있는 report 에만 매달고 새 보고서를 끌어오지 않는다.
      entity degree = 공유 보고서 수(허브일수록 큼). report 노드 degree 엔
      has_tag 를 안 더해 토글에 따라 크기가 안 흔들림.
- [x] **고카디널리티(부품 등) 폭증 대응** (`GraphTagLayer` 컨트롤) — 부품처럼
      값이 많은 축은 1회성 태그가 잎사귀로 매달려 헤어볼이 된다. 두 장치로 거름:
      ① **축 선택** — 그릴 축(slug)을 고름. 기본 ON 은 저카디널리티 축
      (`model`·`phase`)만, 부품·BOM 등은 수동 ON. ② **공유된 것만**(기본 ON)
      — *그래프 안에서* 2개 이상 보고서가 공유한 태그만(min_degree=2). 백엔드
      `_entity_layer(axes, min_degree)` + 두 빌더/라우트에 `tag_axes`·
      `tag_min_degree`. 모달·페이지가 `GraphTagLayer` 팝오버를 공유.
- [x] **entity 필터 multi-axis** (글로벌 페이지) — `GraphEntityFilter`
      (검색 팝오버, axis 별 그룹·선택 칩). 백엔드 `entity_ids` 는 axis 별
      AND, 같은 axis 안에서는 OR (모델X AND 시험Y; plan §6.1). 필터는 레이어
      표시와 독립 — 값으로 좁히되 태그 노드는 안 띄울 수도 있음.
- [x] entity 노드 검색 — Phase 4 「노드 검색」이 제목·entity 라벨 모두 부분일치로
      하이라이트(매칭만 또렷, 나머지 흐리게). 필터 팝오버 값 검색과 별개.
- [x] 색 매핑을 로컬 모달에도 적용 — 색 로직을 `graphColorMapping.jsx`
      (`useGraphColors` 훅 + `GraphColorLegend`)로 추출해 페이지·모달 공유.
      모달은 열릴 때 `listReportTypes` 1회 fetch(종류 범례 이름용) + 색 드롭다운.

> 구현 메모 (Phase 2 entity):
> - 백엔드: `LinkGraphNode` 에 type=`"report"|"entity"` + entity 필드
>   (entity_id/label/axis/axis_label), report 필드는 Optional 로 완화. 서비스
>   `_entity_node`/`_entity_layer`, 두 빌더에 `include_tags`, 글로벌에
>   `entity_ids`. 라우트 양쪽에 `include_tags`(+ 글로벌 `entities`) 쿼리.
> - 프론트: `LinkGraphCanvas` paintNode 가 type 으로 모양/색/라벨 분기,
>   linkColor·화살표가 has_tag 분기. entity 노드 클릭은 무시(보고서 이동 X).
>   `GraphEntityFilter`(신규), `graphColors.axisColor`(신규), api
>   `getGlobalLinkGraph`/`getReportLinkGraph` 에 includeTags·entityIds.
> - 줌/깜빡임: 진입 시 force 시뮬레이션 펼침이 보이던 깜빡임을 `ready`
>   opacity 게이트로 가림(식은 뒤 fit+살짝 확대해 페이드인). 노드 간격은
>   `CHARGE_STRENGTH`/`LINK_DISTANCE`, 라벨은 전체 제목 노출로 조정.

> 추가/변경 파일 (Phase 2 entity):
> - backend: `reports/schemas.LinkGraph*`, `reports/services`(_entity_node,
>   _entity_layer, build_link_graph/build_global_link_graph), `reports/routes`
> - frontend: `reports/GraphEntityFilter.jsx`(신규), `reports/LinkGraphCanvas`,
>   `reports/ReportGraphPage`, `reports/ReportGraphModal`,
>   `shared/reports/graphColors.axisColor`, `shared/api/reportLinks`

> 버그 수정 (연결 라벨 캐시) — 관계도가 link kind 카탈로그를 재사용하다
> 발견. `ReportLinks.jsx` 의 모듈 캐시 `_fetchKinds` 에 `.catch` 가 없어서,
> 초기 로드 타이밍/HMR 중 fetch 가 한 번 실패하면 `_inflight` 가 거부된
> 프로미스로 영구히 남아 이후 모든 호출이 그걸 돌려받았다 → 보고서 연결
> 폼의 "상대 보고서 타입" 드롭다운(및 관계도 엣지 색/kind 토글)이 빈 채로
> 고착. 수정: 실패는 캐시하지 않고 inflight 를 풀어 재시도 가능하게 +
> `useLinkKinds` 에 자동 재시도(최대 3회, 1.5s 백오프). 백엔드/데이터는
> 정상이었음 (공개 `/api/reports/link-kinds` 가 admin 과 같은 테이블·서비스).

### Phase 3 — 시간 시각화 (3일) — ✅ 구현 완료
- [x] 기간 슬라이더 (presets) — 글로벌 페이지 기간 드롭다운(전체/1주/1개월/
      3개월/1년)으로 Phase 1b 에서 이미 제공.
- [x] **타임라인 정렬 모드 (X 고정)** — 「타임라인 정렬」 토글. report 노드의
      x 를 `report_date` 로 고정(force-graph `fx`), y 는 force 가 정해 좌→우
      시간 흐름. 날짜 없는 노드·entity 는 자유(링크에 끌려 배치). 캔버스 하단에
      축 캡션(← 과거 · 최근 →, min~max 일자). 가로로 넓어 진입 확대(BOOST)는
      이 모드에서만 건너뜀. 백엔드 무변경(노드에 report_date 이미 포함).
- [x] **스윔레인 + 시간 눈금 (Phase 3 (2), 상용 KronoGraph·간트 방식)** —
      타임라인일 때 「레인」 드롭다운(없음/보고서 종류/작성자). 레인을 고르면
      y 도 카테고리 띠로 고정(`fy`) — 레인 안에서 날짜 순 greedy 서브행 패킹으로
      겹침 회피. `onRenderFramePre` 로 시간 눈금선(기간 길이에 따라 연/분기/월/
      주 자동)+날짜 라벨+레인 띠·이름을 노드 아래 배경에 그림. 레인 모드는
      fit 여백을 넓혀(90) 가장자리 라벨이 안 잘리게. 상용 비교: 노드를 시간에
      배치+레인은 KronoGraph/GitKraken/간트 계열, force 유지+타임바 스크럽은
      KeyLines/Linkurious/Gephi 계열 — 후자(타임바·재생)는 다음 검토 후보.
- [x] **타임바 + 재생 (Phase 3 (3), 상용 KeyLines·Linkurious·Gephi 방식)** —
      「시간 재생」 독립 토글(타임라인 정렬과 무관). 캔버스 하단에 활동
      히스토그램 + 양쪽 핸들 기간 브러시 + ▶재생. 노드를 옮기지 않고, 선택
      기간 [from,to] 밖 report 노드·엣지를 캔버스가 흐리게(dim) 처리 → 위치
      안정. 재생은 from=min 고정·to 를 6초에 걸쳐 끝까지 흘려 시간순으로 쌓는
      애니메이션(force-graph 가 매 프레임 repaint 하므로 timeFilter prop 갱신만으로
      동작). `GraphTimeBar`(신규), `LinkGraphCanvas` 에 `timeFilter` prop
      (inWindow/edgeInWindow + paintNode globalAlpha dim, 엣지 색 fade).
- [x] **URL 쿼리스트링 상태 보존** — 글로벌 페이지 필터를 쿼리스트링에 동기화
      (`useSearchParams`, 기본값은 생략해 URL 짧게). 키: `d`(기간)·`types`·
      `kinds`·`color`·`tl`(타임라인)·`lane`(레인)·`play`(타임바)·`tag`/`tagax`/
      `tagsh`(관련정보 레이어). 값 필터(selectedEntities)·재생 기간은 보류.

> 구현 메모 (Phase 3): 타임라인은 순수 프론트 레이아웃(재조회 없음). `timeline`
> 토글 시 graphData 가 fx 를 다시 계산하고, 리셋 effect 가 `timeline` 도 보고
> ready/fit 을 다시 돌려 깜빡임 없이 재배치. 변경: `LinkGraphCanvas`(timeline
> prop·timeAxis·fx·축 캡션), `ReportGraphPage`(타임라인 토글·URL 동기화).

### Phase 4 — Composite + 폴리시 (3일) — ✅ 구현 완료
- [x] **노드 검색 → 하이라이트** — 필터 바 「노드 검색」. 제목·entity 라벨
      부분일치 노드만 또렷, 나머지 흐림(dim). 캔버스 `searchQuery` prop.
- [x] **1차 이웃 강조** — 노드 hover 시 그 노드+직접 이웃만 또렷, 나머지·무관
      엣지 흐림. 인접맵(`adjacency`)+`onNodeHover`. (클릭 대신 hover 가 더 가볍다)
- [x] **PNG export** — 캔버스 우상단 「PNG」 버튼. force-graph canvas 를
      `toDataURL` 로 저장(모달·페이지 공통). 검색·기간·hover 강조가 dim 으로
      한 `focusSet`/`timeFilter` 경로에 합류(노드 globalAlpha, 엣지 색 fade).
- [x] **composite 노드 + composite_member 엣지** (「종합보고」 토글) — 모달·
      글로벌 양쪽. 종합보고를 앰버 사각형 hub 로, 소속 보고서로 점선·무화살표
      엣지(kind="composite_member"). 백엔드 `_composite_node`/`_composite_layer`
      (`composite_report_items.ref_report_id` 로 그래프 안 보고서를 멤버로 가진
      종합보고만 hub 로, entity 레이어와 동일 성격), 두 빌더·라우트에
      `include_composites`. composite 노드 클릭 → 종합보고 상세
      (`/w/{slug}/composites/{id}`) 이동(페이지·모달 공통).
- [x] **고립 노드 표시 토글** (글로벌 전용) — 「고립 노드」 토글. 켜면 필터에
      맞는 스코프 내 *모든* 보고서를 점으로 추가(연결 없는 것 포함). 백엔드
      `build_global_link_graph(include_isolated)` — 후보를 endpoint ∪ 스코프
      전체로 확장, node_ids = 적격 보고서 전체. 고립은 degree 0 이라 limit
      초과 시 가장 먼저 잘려 허브 우선 보존. 헤더 설명도 토글 따라 갱신.
- [x] 큰 그래프 가드 — limit 500 + truncated 안내 배지(Phase 1b)로 이미 충족.

> 구현 메모 (Phase 4 폴리시): 모든 흐림(dim)을 한 경로로 통일 — `dimNode`/
> `dimEdge` 가 (타임바 기간 밖) ∨ (강조 집합 밖) 을 판정. `focusSet` =
> hover 이웃 ∨ 검색 매칭(hover 우선). `idOf` 로 엣지 끝점이 문자열/객체 모두
> 처리. 변경: `LinkGraphCanvas`(searchQuery·hover·adjacency·PNG),
> `ReportGraphPage`(검색 입력).

**현재 상태 (2026-05): Phase 1a~4 전부 구현 완료.** 글로벌 관계도에서 기간/
종류/관계/관련정보 필터, 종류·작성자 색 매핑, 관련정보·종합보고·고립노드 레이어
토글, 타임라인 정렬·스윔레인·타임바 재생, 노드 검색·hover 이웃 강조·PNG 내보내기,
URL 상태 보존까지 동작. 로컬 모달도 색 매핑·검색·관련정보·종합보고 토글 공유.
남은 것은 §11 향후 확장(각자 독립된 큰 기능)뿐.

## 9. 성능 / 위험

### 9.1 서버 부담 — 작음
- 그래프 페이지 호출 빈도가 낮음 (list page 의 1/10 ~ 1/50)
- 응답 크기 ~200KB 안팎 (limit=500 기준)
- 쿼리는 join 한 번에 끝 — 100ms 안에
- 5분 client cache (ETag) 효과적

### 9.2 클라이언트 부담 — 적당
- 노드 ~1000 까지 Canvas 부드러움
- 그 이상 시 WebGL 전환 또는 클러스터링

### 9.3 시각 정보 과다 — 디폴트 신중
- 기본은 link 만, 레이어 토글로 entity/composite 추가
- 옵시디언/Notion 의 "show tags" 가드 패턴

### 9.4 시간축 모드 — 점진
- 처음엔 force 안에 기간 필터만, 사용자가 실제로 쓰면 타임라인 정렬 추가

## 10. 결정 사항 (체크리스트)

문서를 읽고 진행 전 확인이 필요한 항목들:

- [x] Phase 1a (로컬만) 으로 출발 — **동의** (구현 완료)
- [x] 노드 색 기본값 — **Phase 1a 는 회색 단색**, 종류색 매핑은 Phase 2 로
- [x] 글로벌 그래프 진입점 — **워크스페이스 사이드바 메뉴**에 「관계도」 추가
      (보고서 ↔ 종합보고 사이)
- [x] 큰 그래프 정책 — **워크스페이스 자동 한정 + 연결된 보고서만 + limit 500**
      (degree 높은 허브 우선, 초과 시 truncated 안내)
- [x] entity 노드 도입 시점 — **Phase 2 에서 도입** (토글 레이어, OFF 기본)

## 11. 향후 확장 가능성 (= 남은 다음 단계 후보)

Phase 1~4 가 끝났으므로, 다음으로 갈 수 있는 건 아래의 독립된 큰 기능들이다.
각각 별도 설계가 필요하다 (우선순위·난이도 메모 포함).

- **협업자(편집자) 그래프** — 사람↔보고서 관계. 백엔드에 작성자/편집자 노드
  레이어 추가(authored_by 엣지). 데이터(편집 이력)는 이미 있어 난이도 중.
- **노드 자동 클러스터링** (community detection) — 큰 그래프에서 묶음 시각화.
  프론트 d3/그래프 알고리즘, 난이도 중상.
- **검색·필터를 list page 와 공유** — 같은 쿼리스트링으로 그래프↔목록 왕복.
  중복 필터 로직 통합 필요, 난이도 중.
- **그래프 위 인라인 코멘트** — 코멘트를 link 처럼 표시. 코멘트 모델 연동, 중상.
- **종합보고 안 트리 뷰 vs 그래프 뷰 선택** — 종합보고 상세에서 멤버 구조를
  그래프로. 기존 캔버스 재사용 가능, 난이도 하~중.
- (운영) 색 매핑에 **게시조직(mount)** 기준 추가, composite/타임라인 등을
  로컬 모달까지 확대 등 소규모 폴리시.
