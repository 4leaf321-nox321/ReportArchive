# 본문 전문검색 설계 · 진행 체크리스트

보고서 **본문(content)** 까지 검색하는 전문검색. 키워드(MVP)로 시작하되, 나중에
AI(임베딩·RAG)를 **덧붙이기만** 하면 되도록 토대(청크 추출 + 권한 스코프 +
retrieval seam)를 함께 깐다.

> 진행하면서 이 파일의 체크박스를 갱신한다. `[ ]` 미완 · `[x]` 완료 · `[~]` 진행중.

---

## 핵심 설계 결정

1. **한국어 = `pg_trgm` 트라이그램 부분일치** (← `to_tsvector('korean')` 안 씀; 스톡 PG에
   한국어 설정·사전 없음, 조사 때문에 `simple`도 부정확). 언어 비의존 3글자 n-gram이라
   "보고서"가 "보고서를"에 잡힘. GIN 인덱스 + `ILIKE`/`similarity()`.
2. **본문은 `content::text` 통째 색인 금지** — 위젯 콘텐츠를 **평문으로 평탄화**해
   `reports.search_text`(TEXT)에 저장. JSON 키·HTML 태그·file_id·색토큰 등 노이즈 배제.
3. **권한 스코프는 기존 `visible_report_ids(actor)` 재사용** — 검색도
   `WHERE id = ANY(visible) AND deleted_at IS NULL`. 공개 열람자는 기존 public 경로.
4. **AI-ready seam (지금 비용 0, 나중에 드롭인):**
   - 추출기를 **청크 생성기**로 (블록/페이지 단위 + 메타 `{report_id, page_idx, block_id, widget_type}`)
     → 지금은 join 해서 `search_text` 로 쓰고, 나중엔 **임베딩 단위 + 인용 단위**.
   - `retrieve(actor, query, k)` **추상화**: 지금 trgm 키워드 1개, 나중에 하이브리드(키워드+벡터)로 교체.
   - 벡터 저장(`pgvector` + `report_chunk_embeddings`)은 **지금 안 만듦**, 자리만 비워둠.

---

## Phase 1 — 백엔드: 평문/청크 추출기 (순수 함수, DB·레지스트리 무결합) ✅

- [x] `backend/app/widgets/text_extraction.py` 생성
  - [x] `TextChunk` 데이터클래스(`text`, `report_id`, `page_idx`, `block_id`, `widget_type`, `key`)
  - [x] content JSON 재귀 워크 — 문자열만 수집, 비텍스트 키 denylist 제외, 실제 HTML 태그만 strip
  - [x] `extract_chunks(content, pages, title, ...) -> list[TextChunk]` (블록/페이지/제목 단위)
  - [x] `extract_searchable_text(...) -> str` (청크 join, 길이 상한) + `extract_searchable_text_for_report(report)` 래퍼
  - [x] 블록 내 동일 조각 중복 제거(평문 미러 ↔ *_html)
- [x] 샌티티 테스트(표/비교표/긴글/제목/이미지/key_value/페이지명 샘플) — file_id·색토큰·숫자 제외, 한국어 평문·HTML strip 검증 통과

## Phase 2 — 백엔드: 마이그레이션 + 모델 ✅

- [x] `migrations/versions/p37_report_search_text.py` (head 단일 확인, p36→p37)
  - [x] `CREATE EXTENSION IF NOT EXISTS pg_trgm`
  - [x] `reports.search_text TEXT NULL` 추가
  - [x] `CREATE INDEX ix_reports_search_text_trgm USING gin (search_text gin_trgm_ops)`
- [x] `reports/models.py` 에 `search_text` 컬럼 + `__table_args__` GIN 인덱스 선언(autogen drop 방지)
- [x] **백필을 마이그레이션 upgrade() 안에 포함** — 운영 자동 업데이트(`setup_and_upgrade_db.py`
      = `alembic upgrade head` 만 돌림)가 색인까지 같이 하도록. **운영서버에서 별도 백필 불필요.**
- [x] **개발 DB 적용 완료** — `alembic upgrade head` → pg_trgm·GIN·컬럼 생성 + 기존 107건 전부 백필 확인.

## Phase 3 — 백엔드: 쓰기 시 인덱스 유지 + 백필 ✅

- [x] **SQLAlchemy 이벤트 훅**(`before_insert`/`before_update`)으로 중앙화 — title/content/pages
      변경 시에만 `search_text` 재계산(폴더이동·잠금터치 등은 skip). 흩뿌리기 대신 누락 0.
- [x] `backend/scripts/backfill_search_text.py` — 이제 **선택적**(마이그레이션이 1회 백필 담당).
      추출기 로직을 바꿔 **전체 재색인**(`--all`)이 필요할 때만 수동 실행.
- [x] 임포트 검증: 모델·서비스·`app.main`·백필 스크립트 모두 OK

## Phase 4 — 백엔드: 검색 서비스 + 라우트 ✅

- [x] `services.search_reports(db, actor, q, limit, offset) -> (rows, total)` — `search_text` ILIKE
      부분일치 + 가시 스코프(`visible_report_ids`) + 휴지통 제외, 제목매치 우선→최신순
- [x] `services.search_snippet(text, needle)` — 매치 주변 발췌(원문 케이스, … 말줄임)
- [x] `GET /api/reports/search?q=&limit=&offset=` 라우트(`/{report_id}` 위 정적 등록 확인)
- [x] 공개 열람자(public_viewer)=이 게시판 공개분, virtual=무스코프 분기
- [x] 앱 임포트·라우트 등록·스니펫 검증 통과
- [~] `retrieve()` seam 추상화 — 현재는 `search_reports` 가 그 역할(AI 단계서 하이브리드로 교체 예정)

## Phase 5 — 프론트엔드 ✅

- [x] `reports/api.js` `searchReports(q, {limit, offset})` → `{results:[{report,snippet}], total}`
- [x] `CommandPalette.jsx` — 인라인 결과 리스트(별로라는 피드백)를 빼고, 검색어 입력 시
      최상단 "검색 결과 전체 보기" 항목으로 전용 페이지에 진입(Enter=첫 항목 자동 선택).
- [x] **전용 검색 페이지 `SearchPage.jsx` + 라우트 `/w/:workspace/search`** — 큰 2열 카드 그리드
      (제목 + 3줄 스니펫 + 매치 하이라이트 + 메타), `?q=` URL 동기화, 250ms 디바운스, 더보기
      페이지네이션(offset). 워크스페이스 스코프는 apiClient X-Workspace-Slug 자동 적용.
- [x] lint(새 오류 없음) · `vite build` 통과

## Phase 6 — 테스트 · 롤아웃 ✅(코드) / ⏳(DB 적용)

- [x] 추출기 단위테스트 `tests/test_search_text_extraction.py` — **11개 통과**(실행 검증).
      denylist·HTML strip·평문보존·청크메타·페이지·중복제거·한국어 조사형 포함.
- [x] 스코핑·휴지통·한국어 부분일치 통합 테스트 `tests/test_report_search.py` — **개발 DB에서 3개 통과**.
- [x] 개발 DB 마이그레이션 적용 + 107건 백필 + 통합테스트 통과 (운영은 자동 업데이트가 동일 수행).
- [ ] (사용자) 운영 배포 후 명령 팔레트(⌘K)에서 실제 본문 검색 동작 확인

## Phase 7 — (나중) AI 확장 — 별도 승인

- [ ] `pgvector` + `report_chunk_embeddings` 테이블
- [ ] 청크 임베딩 + 하이브리드(키워드+벡터) retrieve
- [ ] RAG 질의응답(Claude) + 인용 링크(block_id → 위젯 점프)
- [ ] 종합보고(composite) 검색 — 같은 추출기 재사용

---

## 진행 로그

- Phase 1 착수.
- **Phase 1 완료** — `text_extraction.py`(청크 추출기) 작성·샌티티 테스트 통과. 위젯 타입 무결합 제네릭 워크 + denylist + HTML strip + 블록 내 중복 제거. 청크 메타(`report_id/page_idx/block_id/key`)로 AI-ready.
- **Phase 2 완료** — 마이그레이션 `p37_report_search_text`(pg_trgm + `reports.search_text` + GIN trgm), 모델 컬럼·인덱스 반영. alembic head 단일 확인.
- **Phase 3 완료** — SQLAlchemy 이벤트 훅으로 본문/제목/pages 변경 시 자동 재색인 + 백필 스크립트.
- **Phase 4 완료** — 검색 서비스·스니펫·`GET /api/reports/search` 라우트. 가시 스코프·휴지통 제외. 앱 임포트 검증.
- **백엔드 전체 완료.** 실제 동작에는 ① `alembic upgrade head` ② `python scripts/backfill_search_text.py` (둘 다 라이브 DB 필요) 가 선행돼야 함.
- **Phase 5 완료** — 프론트 `searchReports` API + 명령 팔레트(⌘K/Ctrl+K)에 본문검색 결합. lint·build 통과.
- **Phase 6 완료(코드)** — 추출기 단위테스트 11개 실행·통과, 검색 엔드포인트 통합테스트 3개 작성(DB 필요).
- **MVP 구현 끝.** 남은 건 사용자 측 DB 적용:
    1. `cd backend && ./venv/bin/alembic upgrade head`  (p37: pg_trgm + 컬럼 + GIN)
    2. `./venv/bin/python scripts/backfill_search_text.py`  (기존 보고서 색인)
    3. `./venv/bin/python -m pytest tests/test_report_search.py -v`  (통합테스트)
  이후 ⌘K 명령 팔레트에서 본문 검색이 동작. 새 저장분은 이벤트 훅이 자동 색인.
- **개발 DB 적용 완료** — 마이그레이션이 백필까지 수행(107건). 통합테스트 3개 통과. 운영은
  자동 업데이트(`alembic upgrade head`)가 색인까지 같이 하므로 **별도 작업 불필요**.
- Phase 7(AI: pgvector·하이브리드·RAG)은 별도 승인 시점에.
