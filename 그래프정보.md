# 그래프·시각화 위젯 라이브러리 매핑

ReportArchive 보고서 위젯이 어떤 차트/렌더링 라이브러리에 의존하는지 정리.
새 위젯을 추가하거나 기존 위젯에 기능을 얹을 때 어느 패턴(recharts vs plotly vs d3-hierarchy vs 자체 SVG)을
따라야 하는지 빠르게 판단할 수 있도록 만든 문서.

## 위젯별 라이브러리

| 위젯 type | 파일 | 라이브러리 | 비고 |
|---|---|---|---|
| `chart` (bar / line) | `Chart.jsx` | recharts | SVG 기반, React 컴포넌트 트리 |
| `scatter` (2D) | `Scatter.jsx` | recharts | 동일 |
| `radar` | `Radar.jsx` | recharts | 동일 |
| `scatter3d` | `Scatter3D.jsx` | plotly.js-dist | WebGL 3D, `type: 'scatter3d'` / `'surface'` |
| `heatmap` | `Heatmap.jsx` | plotly.js-dist | 2D 매트릭스 (`type: 'heatmap'`) |
| `contour` | `Contour.jsx` | plotly.js-dist | 등고선 (`type: 'contour'`) — heatmap 의 데이터 모델 공유 |
| `treemap` | `Treemap.jsx` | plotly.js-dist | 사각형 패킹 (`type: 'treemap'`) — long-form `rows: [{label,parent,value,color}]` |
| `pie` | `Pie.jsx` | plotly.js-dist | 파이/도넛 (`type: 'pie'`, `hole > 0` 으로 도넛) — colorscale 은 클라이언트 sampling |
| `box` | `Box.jsx` | plotly.js-dist | 박스플롯 (`type: 'box'`) — long-form `{group, value}`, 그룹별 1 trace |
| `packing` | `Packing.jsx` | d3-hierarchy + 자체 SVG | Circle packing (`d3.hierarchy().sum() → d3.pack()`) |
| `tree` | `Tree.jsx` | d3-hierarchy + 자체 SVG | 노드·엣지 트리 다이어그램 (`d3.tree().nodeSize()`) |
| `network` | `Network.jsx` | d3-force + 자체 SVG | 노드·엣지 일반 그래프 (force / circular / grid / radial) — `nodes[] + edges[]` 모델 |
| `waffle` | `Waffle.jsx` | 자체 SVG | 격자 비중 (largest-remainder method 로 셀 배정) |
| `cad_3d` | `Cad3d.jsx` | three.js | dynamic import (`three`, `OrbitControls`, `GLTFLoader`, `STLLoader`) |
| `equation` | `Equation.jsx` | katex | LaTeX 수식 렌더 |
| `html_embed` | `HtmlEmbed.jsx` | (없음, `<iframe sandbox>`) | 사용자 업로드 HTML 을 sandbox iframe 으로 |
| `video` | `Video.jsx` | (없음, `<video>` 네이티브) | 업로드 파일 → blob URL → `<video controls>` |
| `heading`, `rich_text`, `key_value`, `bulleted_list`, `table`, `image`, `attachment`, `milestone`, `flowchart`, `progress_bar`, `raci_matrix` | 각 동명 파일 | (외부 차트 라이브러리 없음) | 자체 React + Tailwind / SVG |

## 라이브러리별 특성

### recharts (chart / scatter / radar)

- React 컴포넌트 트리 안에 들어가서 plot 영역이 React 의 reconciliation 대상.
- `useXAxisScale`, `useYAxisScale`, `useXAxisDomain` 같은 hook 으로 픽셀 ↔ 데이터 좌표 변환을
  외부로 노출 → `@/shared/annotations` 시스템(freeform draw / drag / select / edit)이
  plot 위에 정확히 좌표가 일치하는 SVG overlay 를 얹을 수 있음.
- **그래서 chart / scatter 는 풍부한 annotation 시스템을 갖추고 있고, 다른 plotly 위젯들은
  의도적으로 annotation 을 제외함** (좌표 변환이 외부로 노출 안 됨).
- 리사이즈 비용은 적당 — `ResponsiveContainer` 가 부모 사이즈에 자동으로 맞춤.

### plotly.js-dist (scatter3d / heatmap / contour / treemap / pie / box)

- 내부적으로 모든 DOM 을 Plotly 가 직접 관리 (`Plotly.react(el, data, layout, config)`).
- React 는 부모 div 의 ref 만 넘기고, 그 안쪽은 Plotly 의 영역.
- 좌표 변환 함수 (`xaxis.l2p`, `xaxis.p2l`) 가 internal 이라 `@/shared/annotations` 가
  그대로 안 붙음 — 우리 annotation 시스템은 recharts 전용.
- 리사이즈 비용이 높음 — 매 cell drag tick 마다 `Plotly.react` 가 다시 그리면 stutter.
  → `resizing` flag + 200ms idle timer 로 burst suspend 패턴 (Chart / Heatmap / Contour /
  Treemap / Pie / Box 모두 동일 패턴; scatter3d 는 WebGL 컨텍스트 보존 위해 dim overlay 만).
- **알려진 trace 별 함정**:
  - **contour**: 평탄 surface (모든 셀 동일값 / null) 에서 `makeCrossings` crash →
    `flatOrEmpty` useMemo 로 감지 후 `Plotly.react(el, [], layout)` 빈 trace 로 회피.
  - **treemap**: `branchvalues: 'remainder'` 로 부모 자동 roll-up 을 시도하면 일부 환경에서
    숫자 어긋남 → 클라이언트에서 bottom-up aggregate 후 `branchvalues: 'total'` 로 강제.
  - **pie colorscale**: Plotly pie 의 `marker.colorscale` 이 일관되게 동작하지 않음
    (일부 스케일이 default palette 로 떨어짐) → `COLORSCALE_PRESETS` + `lerpHex` 로 클라이언트
    sampling 후 명시 hex 배열을 `marker.colors` 로 전달. **다른 위젯의 colorscale 옵션과
    혼동 주의** — pie 에서는 단색 (Blues/Reds/Greens) 도 시각 차이가 미세해서 드롭다운에서
    의도적으로 제외.
  - **pie sort**: Plotly 의 `sort: true` 는 슬라이스 시각 순서만 바꾸고 colors 배열은 안
    바꿔서 색-라벨 매핑이 어긋남 → 클라이언트에서 미리 정렬 후 `sort: false` 로 전달.
  - **box jsonschema enum tuple 버그**: backend 의 `enum` 을 list 가 아닌 tuple 로 두면
    jsonschema 검증 단계에서 500 (clean ValueError 아님). `enum: list(...)` 로 명시.

### d3-hierarchy + 자체 SVG (packing / tree)

- Plotly 에 native trace 가 없어서 layout 만 d3 가 계산하고 그리기는 우리 React/SVG 가 직접.
- `d3.hierarchy(data).sum(d => d.value)` → `d3.pack().size([w,h]).padding(p)(root)` 로
  Circle Packing layout; `d3.tree().nodeSize([w,h])` 로 트리 layout.
- **cycle 가드 필수**: parent 가 cycle 을 형성하면 (A→B, B→A 또는 self-parent) recursion 이
  무한 → stack overflow. `aggregateRowValues` / `buildNode` 양쪽 모두 `visiting` Set 로
  진입 감지해서 cycle 노드는 value 0 / 빈 children 으로 처리. Packing / Tree 모두 동일
  패턴.
- **Multiple roots**: parent 가 빈 row 가 둘 이상이면 synthetic `__root__` 로 wrap, 렌더할
  때는 synthetic 노드 hide. Packing 의 합성 root 는 회색 neutral, Tree 는 안 보이게.
- **SVG 두 패스 렌더** (Packing 의 라벨 occlusion fix): 한 번에 노드+라벨을 그리면 d3 의
  BFS 순서 때문에 부모 라벨이 자식 원에 덮임 → 모든 원/노드를 먼저 그리고, 모든 라벨을
  두 번째 g 에서 위 레이어로 그림.

### d3-force + 자체 SVG (network)

- packing / tree 와 마찬가지로 layout 만 d3 가 계산하고 그리기는 React/SVG 직접.
- `forceSimulation(nodes).force('link', forceLink(edges).id(d=>d.id).distance(...))`
  `.force('charge', forceManyBody().strength(...)).force('center', forceCenter(w/2, h/2))`
  `.force('collide', forceCollide().radius(r+2))` 조합. 매 tick 에 `posRef.current` 를
  업데이트 + `setPosVersion` 로 React 재렌더.
- **시뮬레이션 lifecycle**: cleanup 에서 `sim.stop()` 필수. 안 그러면 unmount 후에도
  RAF 가 계속 돌면서 dead ref 에 쓰려다 경고. tick / end 핸들러도 `sim.on('tick', null)`
  로 detach.
- **구조 핑거프린트로 sim 재생성 가드**: 노드 좌표 persist → content 업데이트 →
  `sanitizedNodes` ref 변경의 사이클이 매 persist 마다 새 시뮬레이션 (alpha=1) 을 만들면
  캔버스가 영구히 떨림. `structuralKey = "id1|id2..//s1>t1|s2>t2..."` 로 구조가 진짜
  바뀐 경우에만 sim 재생성, 좌표만 바뀐 경우는 그대로 유지.
- **위치 persist closure 갱신**: `sim.on('end')` 핸들러는 simulation 객체가 보존되는
  동안 한번만 등록되므로 closure 가 옛 `onPersistPositions` 를 들고 있음 → 옛 content
  snapshot 으로 저장돼서 사용자 편집이 silently revert 됨. `persistRef` 로 우회.
- **layout 전환**: `force` 외의 layout (circular / grid / radial) 은 결정적 1회 계산 후
  posRef 에 일괄 기록. 시뮬레이션이 안 돌므로 드래그는 posRef 직접 수정.
- **노드 핀 (`fixed`)**: shift+드래그 종료 시 d3-force 의 `fx` / `fy` 를 유지해서 시뮬이
  그 노드를 못 움직이게 → content 의 `fixed:true` 로 영속화.
- **화살표는 marker 가 아니라 명시 path**: SVG `<marker>` + `marker-end` 가 SPA 라우트의
  fragment ref 해석 + `userSpaceOnUse` + 부모 transform 조합에서 일부 렌더러가 사이즈 0
  으로 collapse. line 의 끝점을 노드 반경만큼 뒤로 당기고 isoceles 삼각형 path 로 직접
  그리는 게 더 안정적. 같은 fill/opacity 가 자동 상속됨.
- **셀프 루프 미지원**: source === target 엣지는 별도 arc 렌더러가 필요해서 v1 은
  무조건 drop. 데이터에 있어도 silently skip.
- **pan / zoom 은 SVG `<g transform>` 만 사용**: viewBox 를 안 건드려서 `html2canvas`
  export 시 부모 컨테이너의 정사각형 크기가 그대로 유지. 줌은 휠 + 좌표는
  pointer-toward 패턴 (커서 아래 월드 좌표가 zoom 후에도 같은 화면 위치).

### 자체 SVG (waffle)

- 외부 layout 라이브러리 없이 격자 배치만 직접 계산.
- **Largest-remainder method** (`allocateCells`) — 각 그룹에 정수 cell 수를 배정해서
  합이 항상 정확히 `cols × grid_rows` 가 되도록. 단순 `round(v/sum*total)` 은 분수 부분
  처리에서 1~N 셀이 떠다님 → Hamilton 방식으로 floor + 잔여를 큰 fraction 순서로 분배.
- Plotly / d3 layout 의존성 없으니 SSR / 정적 빌드에 가장 친화적.

### three.js (cad_3d)

- 매우 큰 라이브러리 (~ 700KB minified). **dynamic import 로 초기 번들 사이즈 절약** —
  cad_3d 위젯이 실제로 화면에 등장할 때만 로딩.
- `OrbitControls` + `GLTFLoader` / `STLLoader` 도 examples/jsm 에서 dynamic import.
- 자체 browser-native fullscreen 토바 + scene-aware refit + 측정/주석 기능 보유 — generic
  fullscreen viewer (Dialog) 와 겹치지 않도록 `WIDGETS_FULLSCREEN_VIEWER` 셋에서 의도적 제외.

### katex (equation)

- LaTeX → HTML 렌더 라이브러리. 가벼움 (~ 200KB minified, font 별도).
- 백슬래시 escape 가 JSON 표준과 충돌해서 LLM 응답에서 자주 깨짐 → import 단계의
  `parseJsonWithLatexFix` 가 `\sigma` → `\\sigma` 자동 보정.

### native HTML elements (html_embed / video)

- 외부 라이브러리 0. **인증 헤더 호환을 위해 blob URL 패턴** — `fetchFileBlob(file_id)`
  로 받아 `URL.createObjectURL(blob)` 후 src 에 박음. `<video>` / `<iframe>` 의 `src` 가
  Authorization 헤더를 못 붙이는 제약을 우회.
- 단점: 전체 파일을 메모리에 올린 뒤 렌더. video 같은 큰 파일은 부담. 1GB 이상이 자주
  들어가면 signed URL 또는 token-in-querystring 으로 native streaming 이 필요.
- **파일 업로드는 백엔드에서 streaming write** (`reserve_storage_path` +
  `register_upload`) — 1GB 비디오도 서버 메모리 ~8MB 만 유지. video 확장자는 별도 limit
  `upload_max_bytes_video` (1GB default), CAD 는 200MB, 그 외 25MB.

### 자체 React / SVG (그 외 위젯들)

- 외부 라이브러리 없음. Tailwind utility class + 직접 SVG/HTML 마크업으로 충분.
- 데이터 형태가 단순 (1D list / key-value / table rows) 이라 라이브러리 도입 비용이 무의미.

## 신규 위젯 추가 시 가이드

1. **2D 라인/막대/산점/레이더 같은 React 친화 시각화** → recharts. annotation 시스템 자동
   상속 가능.
2. **2D 매트릭스 / 등고선 / 3D / 복잡한 Plotly 전용 trace** → plotly.js-dist. 단, annotation
   은 기본적으로 빠짐 (필요하면 Plotly native `layout.shapes` / modebar drawing tools
   활성화).
3. **계층 데이터 시각화 — 사각형 패킹 / 부채꼴** → plotly.js-dist 의 `treemap` / `sunburst`
   먼저 검토. Plotly 가 못 그리는 모양 (Circle packing, node-edge tree) 이면 d3-hierarchy
   layout + 자체 SVG.
4. **비계층 그래프 — 의존성 / 인용 / 소셜 네트워크** → d3-force + 자체 SVG (network 패턴
   참고). 사이클이 있거나 multi-parent 관계라면 계층 위젯 대신 이쪽.
5. **격자형 비중 / 픽토그램** → 자체 SVG + 직접 layout 계산. waffle 패턴 참고.
6. **3D 메쉬/CAD** → three.js dynamic import. cad_3d 패턴 참고.
7. **단순 다이어그램 / 카드 / 표** → 자체 React + Tailwind 가 빠름.

### 공통 데이터 모델 — `rows: [{label, parent, value, color}]`

treemap, packing, tree 셋은 **데이터 형태가 완전히 동일**. 사용자가 같은 row 를 그대로
복사해서 다른 시각화로 swap 할 수 있는 게 의도된 동선. 새 계층 위젯을 추가할 때도 이
형태를 유지하면 사용자 학습 비용이 0:

```json
{
  "rows": [
    {"label": "전체", "parent": "", "value": null},
    {"label": "재료", "parent": "전체", "value": 30},
    {"label": "가공", "parent": "전체", "value": 20}
  ]
}
```

자식이 있는 row 의 `value` 는 항상 자식 합으로 auto-aggregate; 모든 위젯이 같은
`aggregateRowValues` 헬퍼 패턴 사용.

### 예외 — network 는 `nodes[] + edges[]` 별도 모델

일반 그래프는 parent-child 단일 관계가 아니라 임의의 엣지 (사이클·다중 경로 가능) 이므로
hierarchy 모양에 못 끼움. 의식적으로 별도 모델 사용:

```json
{
  "nodes": [
    {"id": "A", "label": "...", "group": "...", "value": 10},
    {"id": "B", "label": "...", "group": "...", "value": 14}
  ],
  "edges": [
    {"source": "A", "target": "B", "weight": 1, "directed": true}
  ]
}
```

- `id` 는 페이지 내 고유 — 엣지가 참조. 누락된 id 를 가리키는 엣지는 렌더 단계에서
  silently drop (백엔드 JSON Schema 로는 cross-field reference 표현 불가).
- 노드 위치 `(x, y)` 는 사용자가 드래그/시뮬레이션 안정화 시 자동 persist (`fixed:true`
  까지 같이 저장).

## auto-fit / fullscreen / 정사각형 셋 (ReportDetailPage.jsx)

세 위젯 셋이 그래프류의 layout 동작을 결정 — 신규 그래프 위젯 추가 시 적절히 합류:

- `WIDGETS_DEFAULT_NO_AUTOFIT` — auto_fit 기본값 OFF. 그래프류 + `html_embed`.
- `WIDGETS_SQUARE_AUTOFIT` — auto-fit 측정이 `clientWidth` (정사각형). 순수 그래프류만
  (`html_embed` / `treemap` 은 제외 — 자연 종횡비 없음).
- `WIDGETS_FULLSCREEN_VIEWER` — 우상단에 fullscreen 아이콘 노출 + 95vw/95vh 다이얼로그.
  그래프류 + `html_embed` + `video`. `cad_3d` 는 자체 browser-native fullscreen 으로 별도
  처리.

## 알려진 한계

- **plotly.js 위젯의 annotation** — 위에서 설명한 좌표 변환 노출 부재. 진짜 필요해지면
  Plotly modebar 의 `drawline / drawopenpath / eraseshape` 옵션으로 native 도구를
  활성화하는 게 첫 번째 선택지.
- **plotly.js 의 docx / PDF export** — iframe / WebGL 콘텐츠를 잘 못 잡음. 정적 스냅샷
  fallback 이 필요할 때 별도 디자인.
- **WIDGET_PROMPT_EXAMPLES (`shared/ai/widgetExamples.js`)** — 새 위젯 추가 시 반드시
  entry 를 추가해야 AI 프롬프트 카탈로그에 노출됨. AI 가 file_id 가 필요한 위젯
  (`image`, `attachment`, `cad_3d`, `html_embed`, `video`) 은 "만들지 마세요" 카테고리에
  합류.
- **계층 위젯의 cycle 처리** — packing / tree / treemap 모두 parent 가 cycle 을 만들면
  자식 합 계산이 무한 재귀. 모든 위젯이 `visiting` Set 가드 필수.
