# Windows → Linux 이식 로드맵

---

## 🟢 현재 위치 / 다음 액션 (가장 먼저 읽기)

**현재 상태 (2026-05-15)**: **Phase 1 완료**. WSL2 Ubuntu 24.04 에서 backend + PostgreSQL + Alembic 마이그레이션 (head 까지) 모두 동작 확인됨. `python run.py` → `GET /api/health` → `200 {"status":"ok"}`.

**중요한 작업 위치 전환**: 이제부터 코드의 **진실의 원본은 WSL `/home/yongjin/projects/ReportArchive/`**. Windows `F:\data\0_Program\62_reportAutomation` 사본은 더 이상 편집/빌드 대상이 아님 (앞으로 손대지 말 것). VS Code 는 **Remote-WSL 확장**으로 WSL 폴더를 원격 편집하는 방식으로 전환.

**다음 액션**: **Phase 2 — Apptainer 정의 파일 (.def) 작성**. 사용자는 VS Code Remote-WSL 셋업만 끝나면 됨 (1회).

**진단 결과**:
| 항목 | 값 |
|---|---|
| CPU | AMD Ryzen 9 5950X |
| 메인보드 | ASUS |
| BIOS 가상화 (SVM) | ✅ Enabled (2026-05-15) |
| WSL 버전 | 2.7.3 |
| 커널 | 6.6.114.1-microsoft-standard-WSL2 |
| Ubuntu 24.04 배포판 | ✅ Ubuntu 24.04.4 LTS |
| UNIX 사용자 계정 | ✅ `yongjin` |
| Python / Node | ✅ 3.12.3 / 20.20.2 |
| Apptainer | ✅ 1.5.0 |
| PostgreSQL (WSL 임시) | ✅ 16.13, DB `report_automation` + `_test` |
| 프로젝트 clone (SSH) | ✅ `~/projects/ReportArchive` |
| 미커밋 변경 rsync 재현 | ✅ 14건 동일 상태 |
| backend native 실행 | ✅ `/api/health` 200 OK |

---

## 🔧 BIOS SVM 활성화 절차 (재부팅 가능해지면 실행)

### 옵션 A. 일반 재부팅 → BIOS 키 연타
1. PC 재부팅
2. ASUS 로고 뜰 때 `Del` 키 연타 (안 되면 `F2`)
3. BIOS 진입 → `Advanced Mode` (F7 또는 우측 상단 토글)
4. 경로:
   ```
   Advanced  →  CPU Configuration  →  SVM Mode  →  [Enabled]
   ```
   - 메뉴 위치 모델에 따라 다름. **"SVM"** 키워드로 찾으면 됨
   - 일부 ASUS 보드는 `OC` 탭 또는 `AMD CBS` → `CPU Common Options` 안에 위치
5. `F10` → Save & Exit → Yes → 재부팅

### 옵션 B. Windows 통해 BIOS 자동 진입 (키 연타 불필요)
1. **설정 → 시스템 → 복구 → 고급 시작 옵션 → 지금 다시 시작**
2. 재부팅 후 자동으로 옵션 화면 → **문제 해결 → 고급 옵션 → UEFI 펌웨어 설정** → 다시 시작
3. 위 옵션 A 의 3~5 단계와 동일

### 활성화 후 검증
재부팅 완료되면 PowerShell:
```powershell
Get-CimInstance Win32_Processor | Select-Object VirtualizationFirmwareEnabled
```
→ `True` 면 성공. `False` 면 BIOS 설정 다시 확인.

또는 작업관리자(Ctrl+Shift+Esc) → 성능 → CPU → 우측 하단 "가상화: 사용" 확인.

### 검증 통과 후 WSL 설치 재시도
```powershell
$env:WSL_UTF8 = "1"
wsl --install -d Ubuntu-24.04 --no-launch
wsl --list --verbose       # Ubuntu-24.04 항목 보여야 정상
wsl -d Ubuntu-24.04        # 첫 실행 → UNIX username/password 입력 (Windows 사용자명과 달라도 됨, 영문 소문자 추천. 비번은 입력해도 안 보임 — 정상)
```

---

## ⏳ 재부팅 전까지 미리 할 수 있는 작업 (Optional, 강추)

WSL 없이도 작성 가능한 텍스트 파일들. 재부팅 후 WSL 들어가서 바로 `apptainer build` 만 하면 됨.

체크리스트:
- [ ] `deploy/` 디렉토리 생성
- [ ] `deploy/backend.def` 초안 (FastAPI + uvicorn)
- [ ] `deploy/frontend.def` 초안 (Node 빌드 + Nginx)
- [ ] `deploy/postgres.def` 초안 (PostgreSQL 18)
- [ ] `deploy/build.sh` (세 SIF 일괄 빌드)
- [ ] `deploy/run-dev.sh` (WSL 로컬 인스턴스 테스트)
- [ ] `deploy/.gitignore` (bind-dirs/, *.sif 제외)
- [ ] `deploy/README.md` (deploy 디렉토리 사용법)
- [ ] `DEPLOY.md` (운영 서버 처음 깔 때 절차)
- [ ] 운영용 `.env` 템플릿 보강 (`backend/.env.production.example`)
- [ ] 루트 `.gitignore` 보강 (SIF, 빌드 산출물 등)

→ 진행하려면 클로드에게 "**deploy 초안 만들어줘**" 라고 말하면 됨.
→ 단점: 실제 WSL 에서 빌드 안 해봤으니 첫 빌드 때 거의 확실히 수정 필요.

---

## 0. 확정된 결정사항

| 항목 | 결정 |
|---|---|
| 운영 서버 OS | Ubuntu 24.04 |
| 운영 권한 | root 가능 |
| 운영 네트워크 | **인터넷 차단 (폐쇄망)**, 내부망만 접근 |
| 외부 노출 | 없음 (HTTPS 미적용, HTTP 만) |
| 배포 형식 | **Apptainer SIF** (Docker 아님) |
| 컨테이너 분리 | backend / frontend(Nginx) / postgres 각각 별 SIF |
| PostgreSQL | 18 우선, 16 도 호환 OK |
| 개발 환경 | **WSL2 + Ubuntu 24.04** (운영과 동일 미러) |
| 빌드 위치 | **WSL 에서만 빌드** (운영 서버는 폐쇄망이라 빌드 불가) |
| 운영 전송 | SIF 파일을 scp / USB 등으로 운영 서버에 옮겨 실행만 |

## 코드 베이스 진단 요약

- ✅ 경로는 전부 `pathlib.Path` → Windows 종속성 없음
- ✅ Windows 전용 라이브러리 / `os.system` / `shell=True` 없음
- ✅ DB는 PostgreSQL 고정 (SQLite 파일경로 이슈 없음)
- ✅ 인코딩 utf-8 명시 (`config.py`)
- ✅ 로깅은 stdout (컨테이너 친화적)
- ✅ `backend/setup_venv.sh` 이미 존재 (LF 줄바꿈 OK)
- ⚠️ Dockerfile / 컨테이너 정의 파일 부재 → 새로 작성 필요
- ⚠️ systemd unit 부재 → 새로 작성 필요
- ⚠️ 운영용 nginx 설정 부재 (예시는 있음) → 작성 필요

**이식 자체의 난이도: LOW. 작업의 대부분은 "코드 수정"이 아니라 "배포 인프라(.def, systemd, bind 마운트 구조) 작성"임.**

---

## Phase 0 — 환경 결정 ✅ 완료

운영서버/DB/네트워크/빌드 전략 확정됨 (위 표).

---

## Phase 1 — WSL2 Ubuntu 24.04 개발 환경 ✅ 완료 (2026-05-15)

### 1-1. WSL2 인프라 설치 ✅
- [x] `wsl --install -d Ubuntu-24.04` 실행 → WSL2 + VirtualMachinePlatform 활성화
- [x] 1차 재부팅 완료

### 1-2. BIOS SVM 활성화 ✅
- [x] BIOS 진입 → SVM Mode Enabled
- [x] 재부팅 후 `VirtualizationFirmwareEnabled : True` 확인 (2026-05-15)

### 1-3. Ubuntu 24.04 배포판 설치 ✅
- [x] `wsl --install -d Ubuntu-24.04 --no-launch` (2026-05-15 완료)
- [x] `wsl -d Ubuntu-24.04` 첫 실행 → UNIX username `yongjin` 생성 (2026-05-15)
- [x] 검증: `uname -a` (Linux 6.6.114.1-microsoft-standard-WSL2), `lsb_release` (Ubuntu 24.04.4 LTS)

### 1-4. WSL 내부 빌드 환경 셋업 ✅ (2026-05-15)
- [x] apt update + upgrade
- [x] build-essential, git, curl, wget, pkg-config, libseccomp-dev, squashfs-tools, cryptsetup, uidmap, libpq-dev
- [x] Python 3.12.3 + venv + dev + pip (Ubuntu 24.04 기본; 로드맵의 3.11 → 3.12 로 갱신)
- [x] Node.js 20.20.2 + npm 10.8.2 (NodeSource setup_20.x)
- [x] postgresql-client-16
- [x] Apptainer 1.5.0 (ppa:apptainer/ppa)
- [x] **추가**: postgresql-16 서버 (네이티브 검증용 임시) — DB `report_automation`, `report_automation_test` 생성, postgres 비번 `32167`

### 1-5. 프로젝트 접근 ✅ (2026-05-15)
- SSH 키(ed25519) WSL 생성 + GitHub 등록 완료
- `git clone git@github.com:4leaf321-nox321/ReportArchive.git` → `~/projects/ReportArchive`
- Windows F:\ 의 미커밋 변경 14건 (widget_relations 기능 작업 + 로드맵 갱신) 은 `git ls-files -mo --exclude-standard -z | rsync --files-from=- --from0 ...` 으로 동일 dirty 상태 재현. WSL git status 와 Windows git status 가 일치.
- **이후 F:\ 사본은 사용하지 않음**. 일상 편집은 VS Code Remote-WSL 으로 WSL 폴더 직접.

### 1-6. 베이스라인 동작 검증 ✅ (2026-05-15)
- `backend/setup_venv.sh` → venv + 60여 개 의존성 설치 성공
- `.env` 자동 생성 (`.env.example` 복사) — DB URL `postgresql+psycopg://postgres:32167@localhost:5432/report_automation` 이 WSL PG 와 일치
- `python scripts/setup_and_upgrade_db.py` → Alembic head 까지 마이그레이션 적용 (widget_relations 시드 포함)
- `python run.py` → uvicorn 5174 기동, `GET /api/health` → `200 {"status":"ok","env":"development"}`
- Linux 네이티브에서 코드 변경 없이 그대로 동작 — Phase 2 진입 조건 충족

---

## Phase 2 — Apptainer 정의 파일 작성

### 디렉토리 구조 (신규)

```
deploy/
  backend.def           # FastAPI + uvicorn
  frontend.def          # Node 빌드 + Nginx 서빙
  postgres.def          # PostgreSQL 18 또는 16
  build.sh              # 세 SIF 일괄 빌드
  run-dev.sh            # WSL 에서 로컬 인스턴스 띄우기
  bind-dirs/            # 호스트 bind 대상 (gitignore)
    pgdata/             # postgres 데이터
    uploads/            # 파일 업로드 영구 저장
    logs/
  README.md             # deploy 작업 안내
```

### backend.def (개념)

```apptainer
Bootstrap: docker
From: python:3.11-slim

%files
    backend /opt/app/backend

%post
    apt-get update && apt-get install -y --no-install-recommends \
        build-essential libpq-dev curl && rm -rf /var/lib/apt/lists/*
    cd /opt/app/backend
    pip install --no-cache-dir -r requirements.txt

%environment
    export APP_ENV=production
    export PYTHONUNBUFFERED=1

%runscript
    cd /opt/app/backend && exec python run.py

%startscript
    cd /opt/app/backend && exec python run.py
```

### frontend.def — 두 가지 패턴
Apptainer 는 multi-stage 가 없어서:
- **A.** 빌드 SIF 별도 → `dist/` 추출 → Nginx SIF 에 `%files` 로 복사
- **B.** 단일 def 에서 빌드 + Nginx 모두 (이미지 큼, 단순)

처음엔 B 추천.

### postgres.def
```apptainer
Bootstrap: docker
From: postgres:18-bookworm

%environment
    export PGDATA=/var/lib/postgresql/data

%startscript
    exec /usr/local/bin/docker-entrypoint.sh postgres
```
`PGDATA` 는 호스트 bind 로 영구화.

### 빌드는 WSL 에서
```bash
cd deploy
apptainer build backend.sif backend.def
apptainer build frontend.sif frontend.def
apptainer build postgres.sif postgres.def
```
산출물: `backend.sif`, `frontend.sif`, `postgres.sif` — 운영 서버로 전송.

---

## Phase 3 — 인스턴스 운영 (WSL 에서 테스트, 운영서버는 systemd)

### 인스턴스 실행 패턴
```bash
# Postgres (DB 데이터는 호스트 bind)
apptainer instance start \
    --bind ./bind-dirs/pgdata:/var/lib/postgresql/data \
    postgres.sif pg

# Backend
apptainer instance start \
    --bind ./bind-dirs/uploads:/opt/app/backend/uploads \
    --bind ./backend/.env:/opt/app/backend/.env:ro \
    backend.sif api

# Frontend (Nginx)
apptainer instance start frontend.sif web
```

### systemd unit (운영 서버용)
`/etc/systemd/system/report-postgres.service`, `report-backend.service`, `report-frontend.service` 세 개.
부팅 시 자동 시작, 실패 시 재시작.

---

## Phase 4 — 운영 서버 첫 배포

### 폐쇄망 배포 흐름
1. **WSL 에서 빌드**: `deploy/build.sh` 실행 → 3개 SIF 생성
2. **전송**: scp / USB / 사내 파일 서버 통해 운영 서버로 옮김
3. **운영 서버 준비** (한 번만):
   - `apptainer` 설치 (오프라인 .deb 패키지 미리 준비)
   - `report` 사용자 생성, `/opt/report-automation/` 디렉토리 구조 생성
   - `.env` 운영값 채우기 (`SECRET_KEY`, `JWT_SECRET_KEY`, `DATABASE_URL`, `CORS_ORIGINS`)
4. **DB 초기화**: 임시로 backend 컨테이너 안에서 `python scripts/setup_and_upgrade_db.py`
5. **systemd enable + start** 세 서비스
6. **검증**: `curl http://localhost:5174/api/health`, 프론트 페이지 확인

### 폐쇄망용 추가 고려
- `apptainer` 자체 .deb 파일 (의존성 포함) 미리 준비
- 이미지 빌드 시 ALL 의존성을 SIF 안에 포함 (runtime 추가 다운로드 0)
- pip 패키지 캐시 / npm 캐시 — 빌드 단계에서 다 받아둠

---

## Phase 5 — WSL ↔ 운영 동기화 워크플로우

### 일상 개발 사이클
```
[Windows 코드 편집]
        ↓
[git commit + push (이미 됨)]
        ↓
[WSL: git pull → 변경 확인]
        ↓
[WSL: 로컬 인스턴스로 동작 확인]
        ↓
[WSL: SIF 재빌드]
        ↓
[scp/USB 로 운영서버 전송]
        ↓
[운영서버: systemctl restart]
```

### 산출물
- `DEPLOY.md` — 새 운영 서버에서 처음 깔 때 절차
- `deploy/README.md` — SIF 빌드/실행 사용법
- `deploy/build.sh`, `deploy/transfer.sh` — 빌드/전송 자동화

---

## 미결정 / 추후 결정

- [ ] uploads 디렉토리 운영 위치 (`/var/lib/report-automation/uploads/` 정도?)
- [ ] PostgreSQL 18 vs 16 확정 (지금은 18 가정)
- [ ] 로그 어디로 모을지 (journald? 별도 파일?)
- [ ] DB 백업 주기 / 보관 정책
- [ ] WSL 메모리/CPU 제한 필요 여부 (`%UserProfile%\.wslconfig`)

---

## 돌아왔을 때 빠른 시작

1. 위 **"🔧 BIOS SVM 활성화 절차"** 보고 BIOS 진입 → SVM Enabled → 재부팅
2. PowerShell:
   ```powershell
   Get-CimInstance Win32_Processor | Select-Object VirtualizationFirmwareEnabled
   ```
   → `True` 확인
3. 클로드에게: "**SVM 켰어, 다음 단계 진행해줘**"
   - 그러면 Ubuntu 다운로드 → WSL 첫 실행 안내 → Apptainer 셋업 순으로 자동 진행
