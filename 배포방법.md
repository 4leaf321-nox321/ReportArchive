# 배포방법

운영 서버에 코드를 가져가서 배포하는 절차.

## 최초 배포

### 사전 준비
- Python 3.11+, Node 18+, PostgreSQL 14+
- `DATABASE_URL` 계정에 `CREATE DATABASE` 권한 (또는 DBA가 DB 미리 생성)

### 백엔드
```bash
cd backend
./setup_venv.sh                          # Windows: setup_venv.bat
source venv/bin/activate                 # Windows: venv\Scripts\activate
cp .env.example .env
```

`backend/.env`를 열어 운영값으로 수정 (필수):
```ini
APP_ENV=production                       # ★ 반드시 development → production 변경
SECRET_KEY=...                           # 강한 무작위 값
JWT_SECRET_KEY=...                       # 강한 무작위 값
DATABASE_URL=postgresql+psycopg://USER:PASS@HOST:5433/report_automation
CORS_ORIGINS=https://app.example.com     # 실제 프론트엔드 origin
```

`APP_ENV=production`이어야 `run.py`가 멀티-워커 uvicorn으로 부팅되고
`DEBUG=False`가 적용됩니다. 안 바꾸면 운영에서 개발 모드로 동작합니다.

```bash
python scripts/setup_and_upgrade_db.py   # DB·스키마 자동 처리
python run.py                            # systemd 등으로 데몬화 권장
```

### 프론트엔드 — 둘 중 하나 선택

**A. 독립 배포** (다른 서버 / nginx / CDN)
```bash
cd frontend
npm ci
echo "VITE_API_BASE_URL=https://api.example.com" > .env.production
npm run build
# dist/ 를 nginx 등 정적 서버에 업로드
```

**B. 백엔드 결합 배포** (한 서버, nginx 없이도 가능)
```bash
cd frontend && npm ci && npm run build
# backend/.env 에 한 줄 추가:  SERVE_FRONTEND_DIST=../frontend/dist
# 백엔드 재시작 → /api 와 SPA 모두 한 프로세스에서 서빙
```

> nginx를 앞단에 둘 때는 `nginx/nginx.conf.example` 참고.

---

## 업데이트 배포 (매 배포 동일)

```bash
git pull

# 백엔드
cd backend && source venv/bin/activate
pip install -r requirements.txt
python scripts/setup_and_upgrade_db.py   # 스키마 변경 자동 반영, 데이터 보존
sudo systemctl restart report-backend    # (또는 사용 중인 프로세스 매니저)

# 프론트엔드 변경이 있으면
cd ../frontend && npm ci && npm run build
# 결합 배포면 위 백엔드 재시작으로 끝. 독립 배포면 dist/ 를 정적 서버에 동기화.
```

---

## 운영 메모
- **`backend/.env`의 `APP_ENV=production`** — 안 바꾸면 개발 모드로 동작. 반드시 확인.
- `setup_and_upgrade_db.py`는 idempotent — 매 배포 무조건 실행해도 안전.
- 첫 부팅·일반 부팅·레거시 DB 전환을 모두 알아서 처리 (분기 상세는 `backend/README.md`).
- 롤백: `alembic downgrade -1` (한 리비전씩 안전하게 되돌림).
- 환경변수: `backend/.env`(서버별 비밀값), `frontend/.env.production`(빌드 시점 주입).
