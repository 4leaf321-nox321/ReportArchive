# 개발계획

이 플랫폼의 큰 그림과 작업 순서. 운영 배포는 `배포방법.md`, 개발 절차는 `개발가이드.md`.

## 비전

정해진 양식의 보고서를 **JSON Schema로 규격화**하고, **AI가 그 스키마에 맞춰 작성**하며,
보고서 내용을 **메타데이터로 색인**하고 점진적으로 **엔티티 그래프 / 온톨로지**로
확장하는 보고서 자동화 플랫폼.

---

## 1. 핵심 설계 아이디어

### 1-1. 보고서 템플릿 = JSON Schema (Draft 2020-12)

한 스키마 파일이 세 가지 역할을 동시에 수행:
- AI 출력의 강제 구조 (structured output)
- 백엔드 유효성 검증 (Pydantic이 JSON Schema 흡수)
- 프론트엔드 폼 자동 생성 (RJSF / JSON Forms)

설계 원칙:
- **버전 필수** — `template_id` + `version`. 양식이 바뀌어도 과거 보고서는 옛 스키마로 검증/렌더 가능해야 함.
- **섹션 단위 분해** — `sections: [{id, title, schema}]` 형태. AI가 섹션 단위로 작성/재작성하기 좋음.
- **메타 어노테이션** — 각 필드에 `x-ai-prompt`, `x-source-required`, `x-aggregatable`, `x-tone` 같은 비표준 키를 넣어 AI 프롬프트 조립과 cross-report 집계에 활용.

### 1-2. 데이터 모델 (PostgreSQL 중심, 3계층)

```
reports                 -- 보고서 인스턴스
  id, template_id, template_version, status, owner, created_at, ...
  content JSONB         -- 실제 보고서 내용 (스키마에 맞춘 JSON)

report_metadata         -- 빠른 필터·집계용 평탄화 컬럼
  report_id, type, period, project_id, tags[], ...

report_entities         -- 보고서가 참조하는 개체 (온톨로지 진입점)
  report_id, entity_id, role
```

`content`는 JSONB + GIN 인덱스. 자주 쓰는 키만 `report_metadata`로 평탄화.
content → metadata 동기화는 트리거 또는 인덱싱 잡으로 자동화.

### 1-3. AI 작성 흐름 (단계적)

"보고서 한 방 생성"은 거의 실패함. 권장 단계:

1. **Intake** — 사용자가 raw 입력 (회의록, 데이터, 자료 링크) 제공
2. **Plan** — AI가 템플릿을 보고 섹션별로 자료 매핑 계획을 JSON으로 출력
3. **Draft per section** — 섹션 단위 structured output (Schema 강제)
4. **Validate** — JSON Schema + 비즈니스 룰 → 실패 시 차이를 알려주고 repair
5. **Cite** — 섹션별 출처 강제 (환각 방지)
6. **Human review** — 사용자가 섹션 단위 승인 / AI 재작성 요청

추가 패턴: few-shot (과거 승인본 주입), 회사 용어집(glossary) 시스템 프롬프트 첨부, 재작성 시 diff 뷰.

### 1-4. 온톨로지 — 단계적 도입

처음부터 RDF/OWL은 과함. 3단계:

| Phase | 도구 | 적용 시점 |
|---|---|---|
| **1. 평면 엔티티 테이블** | PostgreSQL: `entities`, `entity_links` | MVP. 80%의 가치를 여기서 회수 |
| **2. 그래프 쿼리** | Apache AGE 확장 (같은 DB) 또는 Neo4j 별도 서비스 | 그래프 트래버설 쿼리가 자주 필요해질 때 |
| **3. 의미론적 추론** | RDF + OWL (GraphDB / Apache Jena Fuseki) | 자동 분류·추론이 필요한 지식관리 수준에서 |

대부분 Phase 1에서 충분. Phase 2 이상은 실제 워크로드를 보고 결정.

### 1-5. 도메인 어휘 컨트롤

자유 텍스트와 구조화의 절충:
- **태그/카테고리**: controlled vocabulary (선택지 고정 + 사용자 추가 → 관리자 승인)
- **개체 참조**: 자동완성 (Project, Person, Tech를 DB lookup)
- **자유 텍스트 섹션**: 그대로 두되, AI가 작성 후 자동으로 entity 추출 → 백엔드 링크 저장 (온톨로지 씨앗)

---

## 2. 기술 스택 권장

| 영역 | 도구 |
|---|---|
| 스키마 정의 | JSON Schema 2020-12 |
| 백엔드 검증 | Pydantic (JSON Schema 자동 호환) |
| 프론트엔드 폼 | `@rjsf/core` 또는 `react-jsonschema-form` |
| AI 호출 | Anthropic Messages API (structured output / tool use) |
| 콘텐츠 저장 | PostgreSQL JSONB + GIN 인덱스 |
| 검색 | PostgreSQL full-text (`tsvector`) → 필요해지면 Meilisearch/OpenSearch |
| 임베딩 검색 (RAG) | `pgvector` 확장 (같은 DB 안) |
| 그래프 (Phase 2) | Apache AGE 확장 |
| 온톨로지 (Phase 3) | GraphDB / Apache Jena |

---

## 3. 단계적 로드맵

| 단계 | 산출물 | 상태 |
|---|---|---|
| **Phase 0 — GUI 골격** | 레이아웃, 공통 컴포넌트, 빈 모듈 라우팅 (아래 §4 참고) | ✅ 레벨 B 완료 |
| **Phase 1 — 백엔드 골격 + end-to-end** | DB 모델, RLS, CRUD API, 더미 인증, 권한 체계, 시드 템플릿, 스키마 빌더 MVP, 프론트가 실제 API 호출 | ✅ 완료 |
| **MVP** | 첫 JSON Schema 동적 폼(RJSF), 보고서 CRUD 풀 동작 | |
| **v0.2** | 섹션 단위 작성·재작성, 검증·repair 루프, 사용자 승인 워크플로 | |
| **v0.3** | 메타데이터 평탄화, 태그/카테고리, 자동완성 entity 참조 | |
| **v0.4** | 엔티티 그래프(평면), 보고서 간 cross-reference, 집계 대시보드 | |
| **v0.5+** | RAG (과거 보고서 임베딩), 그래프 쿼리 (AGE), 의미론적 추론 (RDF) | |

원칙: **JSON Schema와 평면 엔티티 테이블 두 개로 시작**. 데이터가 쌓이면서 자연스럽게 확장.

---

## 4. Phase 0 — GUI 골격을 먼저 만든다

데이터 모델이 흔들리는 동안에도 GUI 틀은 안정적으로 굳혀둘 수 있음.
**보고서 / AI 기능은 나중에 GUI에 꽂기만** 하면 됨.

### 4-1. 지금 안전하게 만들 것 (락인 없음)

레이아웃·네비게이션·디자인 시스템은 데이터 모델과 무관하게 굳어진다.

- **앱 셸**: 헤더, 사이드바, 브레드크럼, 푸터
- **인증/계정 페이지**: 로그인, 회원가입, 프로필 (목업 backend OK)
- **대시보드/홈**: 빈 카드, KPI placeholder
- **리스트 ↔ 상세 패턴**: 빈 목록·검색·필터·페이지네이션 UI
- **공통 컴포넌트**: Modal, Toast, Form, Table, Tabs, Drawer, Confirm dialog
- **권한·역할 가드**: ProtectedRoute, 역할별 메뉴 노출
- **공통 훅·API 클라이언트**: 에러 처리, 로딩 상태, 토스트 연동
- **빈 모듈 라우팅**: 향후 보고서 / 관리 / 대시보드 자리만 라우트로 예약
- **테마/디자인 시스템**: 색상, 타이포, spacing, 컴포넌트 라이브러리 (shadcn / MUI / AntD 중 택1)
- **i18n 골격** (한국어/영어 확장 가능성 대비)

### 4-2. 미리 만들지 말 것 (락인 위험)

데이터 모델이 결정되기 전에 굳히면 다 뜯어내야 함.

- **보고서 작성 폼의 구체 필드** — JSON Schema 기반 동적 폼으로 갈 것이므로 손코딩은 헛수고
- **보고서 템플릿별 전용 페이지** — 템플릿이 바뀌면 폐기
- **온톨로지 시각화** — 엔티티 모델 잡히기 전엔 시각화 UI 자체가 의미 없음
- **검색 UI의 구체 필터** — 메타데이터 스키마 결정 후에

이 영역들은 "자리만 잡아둔 빈 페이지"로만 두고, 실제 위젯은 데이터 모델 확정 후 채울 것.

### 4-3. 실용 팁

- **목업 데이터 + 가짜 API**로 GUI 화면 흐름을 먼저 완성. `frontend/src/shared/api/` 옆에 `mock/` 폴더.
- **Storybook** 도입 시 컴포넌트와 페이지 개발 분리 — 백엔드 없이도 진척됨.
- **디자인 토큰**(색상·여백)을 CSS variables로 추출 → 나중에 테마 변경이 쉬움.

### 4-4. Phase 0 진행 현황 (2026-05-05)

레벨 B 완료. Phase 1로 흡수됨 — mock 데이터는 실제 API로 모두 전환됨.

**구축됨**
- shadcn/ui + Tailwind CSS 셋업, 디자인 토큰(CSS variables)
- AppShell (Header / Sidebar / Outlet)
- 부서(Workspace) 모델 — URL `/w/:workspace`, `WorkspaceProvider`로 슬러그 매칭
  - 현재 부서: `dev`(개발) + `_global`(공통/횡단)
  - DB는 단일 + `workspace_id` 컬럼 + Postgres RLS 방향으로 결정
- 사이드바 메뉴(한국어): 부서 메뉴(홈/보고서/대시보드) + 공통(템플릿 관리/엔티티/AI 작업/관리자)
- 공통 컴포넌트: DataTable(검색·정렬·페이지네이션), EmptyState, EntityChip, ConfirmDialog, WorkspaceSelector, AIDock(5단계 step UI)
- 7개 페이지 mock: 부서 홈, 보고서 목록, 보고서 상세(섹션 목차 + 본문 + AI 도크), 대시보드, 템플릿, 엔티티/태그, AI 작업, 관리자
- Sonner 기반 Toaster

**의도적으로 미구현 (락인 위험으로 보류)**
- JSON Schema 기반 동적 폼 — 보고서 상세 본문은 placeholder만
- 검색 UI의 구체 필터
- 템플릿별 전용 페이지

---

## 5. Phase 1 산출물 (2026-05-05 완료)

### 5-1. 백엔드 모델

```
workspaces            slug(PK), name, parent_slug(FK self), color, virtual, sort_order
users                 id(PK), email(unique), name, is_active
workspace_members     id(PK), user_id(FK), workspace_slug(FK), role(admin|editor|viewer)
templates             (template_id, version) composite PK,
                      name, description, category, schema(JSONB),
                      owner_workspace_slug(FK NULL=글로벌),
                      is_published, is_latest, created_by_user_id
reports               id(PK), workspace_slug(FK), template_id+version(composite FK),
                      title, status(draft|in_review|approved|archived),
                      period, owner_user_id, tags[], content(JSONB), timestamps
```

핵심 결정:
- **템플릿 버전은 immutable** — 편집 = 새 버전 발행, 과거 보고서는 옛 버전으로 그대로 렌더
- **하이브리드 가시성** — `templates.owner_workspace_slug = NULL`은 글로벌, 값이 있으면 그 워크스페이스 트리(본부+자손)에서만 보임
- **워크스페이스 트리 권한 상속** — 본부에 admin이면 산하 모든 팀에서 admin
- **JSON Schema 검증** — 템플릿 저장 시 스키마 자체 검증, 보고서 저장 시 content를 스키마로 검증

### 5-2. 인증 / 권한

- 더미 미들웨어: `X-User-Id`, `X-Workspace-Slug` 헤더 → `CurrentUser` 의존성
- 권한 의존성: `require_admin`, `require_editor` (admin 포함)
- 시드 사용자: `guest@example.com` (id=1), `dev`에 admin / `biz`에 viewer
- 실제 인증(JWT/OAuth)은 §6 결정 보류 — `get_current_user`만 교체하면 됨

### 5-3. API endpoints

```
GET    /api/me                                — 현재 사용자 + role + 모든 멤버십
GET    /api/workspaces                        — 부서 트리 (가시성 필터 포함)
GET    /api/templates                         — 가시성 적용된 latest 템플릿
GET    /api/templates/:id                     — 특정 템플릿 latest
GET    /api/templates/:id/versions            — 버전 히스토리
GET    /api/templates/:id/versions/:version   — 특정 버전 (보고서 렌더용)
POST   /api/templates                         — 신규 (admin만, v1 발행)
POST   /api/templates/:id/versions            — 새 버전 (admin만)
GET    /api/reports                           — 워크스페이스 트리 스코프
GET    /api/reports/:id                       — 가시성 체크
POST   /api/reports                           — editor+ (가상 워크스페이스에선 차단)
PATCH  /api/reports/:id                       — content는 스키마 재검증
DELETE /api/reports/:id
```

### 5-4. 프론트엔드

- `apiClient`에 `X-User-Id`, `X-Workspace-Slug` 자동 첨부
- `AuthProvider` (`/api/me`) + `WorkspaceProvider`(부서 목록 fetch)
- 모든 페이지 mock 제거, 실제 API 호출
- 헤더에 사용자 이름 / 이메일 / role 표시
- 관리자만 "신규 템플릿" / "편집" 버튼 노출
- **스키마 빌더 MVP**: 섹션·필드 추가/순서변경/삭제, 7가지 필드 타입(짧은텍스트/긴텍스트/숫자/정수/날짜/예아니오/선택지), 가시성 (글로벌/부서종속), 미리보기 자리

### 5-5. 운영 명령

```bash
cd backend
python scripts/setup_and_upgrade_db.py    # DB 생성 + 스키마
python scripts/seed_initial_data.py        # 부서·사용자·템플릿 시드 (idempotent)
python run.py                              # 서버 시작
```

---

## 6. 다음 단계 후보 (Phase 1 이후)

### 6-1. JSON Schema 동적 폼 (RJSF) — 가장 자연스러운 다음

- `@rjsf/core` 도입 + shadcn 테마 어댑터
- 보고서 상세의 section placeholder를 진짜 폼으로 교체
- `x-ai-prompt`, `x-source-required` 같은 메타 어노테이션 약속

### 6-2. AI 리포팅 실연결

- Anthropic Messages API + structured output
- AI 도크 5단계 (Intake → Plan → Generate → Validate → Cite) 실제 구현
- 프롬프트 캐싱(시스템 프롬프트 + 회사 용어집)
- `ai_jobs` 테이블 + 큐

### 6-3. 인증

- 자체 JWT부터 (단순) → 필요시 OAuth/SSO
- 더미 미들웨어 교체

### 6-4. 보고서 데이터 강화

- `report_metadata` 평탄화 (어떤 키 자주 조회하는지 보고 결정)
- `report_entities` (엔티티 추출 도입 시)
- Postgres RLS 정책 (현재는 앱 레이어 필터링)
- 인덱스 / GIN

### 6-5. 템플릿 빌더 보강

- 미리보기 패널 (RJSF로 라이브 렌더)
- 카테고리 트리화
- 템플릿 복제
- raw JSON 편집기 fallback

### 6-6. 협업

- 검토/승인 워크플로 (status 전이)
- 코멘트 / 멘션
- 변경 이력

### 6-7. 위젯 카탈로그 확장 후보

현재 9종 운영 중: `heading`, `rich_text`, `key_value`, `bulleted_list`, `table`, `kpi_card`, `image`, `attachment`, `chart`. 한국 비즈니스 보고서 패턴 기준으로 추가 검토할 후보 — 우선순위만 적어두고 실제 구현은 사용자 요청 기반으로.

**1순위 (가치 ↑ / 구현 ↓)**

- **`checklist`** — 상태 가진 체크 항목 (완료/진행/대기 + 담당/마감). 인시던트 조치 항목, 회의록 후속 조치, 주간 보고 할 일에 자연스러움. content shape: `{items: [{text, status, owner?, due?}]}`. 작업량 1-2h.
- **`callout`** — 강조 박스 (info/warning/success/danger). 본문 흐름 중 "주의·팁·중요" 표시. props: `tone`, content: `{markdown}`. 작업량 30m-1h.
- **`approval_line`** — 결재선/서명 슬롯 (검토자·승인자 이름·직책·날짜·서명 이미지). 한국 비즈니스 환경 필수. props에 슬롯 정의, content에 채움. 작업량 2-3h.

**2순위 (가치 ↑ / 구현 중간)**

- **chart에 `pie`/`donut` 추가** — 별도 위젯 아니라 chart의 `chart_type` 확장. 매출 구성·점유율 시각화. 작업량 1-2h.
- **`progress`** — 진행률 바. props: `label`, `target?`, `unit?`. content: `{value: 0..100, note?}`. kpi_card와 비슷하지만 시각적 비율 강조. 작업량 1-2h.
- **`report_link`** — 다른 보고서 참조 카드. AI 컨텍스트로도 활용 가능. content: `{report_id, snippet?}`. 작업량 2-3h.
- **`timeline`** — 시간선 형태 사건 나열. 인시던트 보고에서 표 대신 사용. content: `{events: [{time, title, description, severity?}]}`. 작업량 2-3h.

**3순위 (케이스별)**

- **`code_block`** — 문법 강조. RFC/ADR. content: `{code, language?}`. 1h (lib 도입 시 +1-2h).
- **`mermaid`** — 다이어그램 (아키텍처/플로우). 번들 크기 +150KB 비용. 2h.
- **`team_roster`** — 사람/팀원 카드 묶음. 회의록 참석자, 프로젝트 멤버. 1-2h.
- **`formula`** — KaTeX 수식. 엔지니어링/금융. 라이브러리 +50KB. 1h.
- **`video`** — YouTube/내부 영상 embed. demo 보고서. 1h.
- **`comparison`** — before/after, 옵션 A vs B. 마이그레이션·A/B 테스트. 1-2h.

**원칙**:
- 새 위젯 추가는 항상 백엔드 `app/widgets/registry.py` (props_schema + content_schema_for + default_props) + 프런트 `templates/widgets/<Name>.jsx` (PropsPanel/Preview/Editor) + `widgets/index.js` 등록 3단계.
- 위젯 contract 변경(필드 추가/삭제) 시 `seed_initial_data.py`의 `_spec_matches_db`가 stale 시드를 자동 갈아엎음.
- 새 위젯이 실제 사용자 흐름에서 필요해질 때 추가, 추측으로 미리 안 만듦.

---

## 7. 결정 보류 항목 (정해질 때 이 문서를 갱신)

- [x] 컴포넌트 라이브러리 — **shadcn/ui + Tailwind** (2026-05-05)
- [x] 부서(Workspace) 모델 — **단일 DB + `workspace_slug` + 트리 상속 권한** (2026-05-05)
- [x] 템플릿 버전 관리 — **immutable per (id, version), 편집 = 새 버전** (2026-05-05)
- [x] 템플릿 공유 — **글로벌 + 워크스페이스 트리 종속 혼합** (2026-05-05)
- [x] 권한 체계 — **admin/editor/viewer + 트리 상속** (2026-05-05)
- [ ] AI 모델 / 프로바이더 (Claude vs OpenAI vs 둘 다)
- [ ] 인증 방식 (자체 JWT / OAuth / SSO)
- [ ] Postgres RLS vs 앱 레이어 필터링 (지금은 앱 레이어, 멀티 테넌트 강화 시 RLS)
- [ ] 다국어 필요 여부
