# 업데이트 내용

기능별로 한 줄씩 정리한 업데이트 안내입니다. 더 이전 버전(v0.34.1 이하) 내역은 [업데이트내용_보관.md](업데이트내용_보관.md)에 있습니다.

---

# 이번 업데이트 (v0.52.19 → v0.52.20)

## AI 설정 — "스킬 생성" 탭 추가 (Claude/Codex 스킬로 내보내기)
- AI 설정에 **「스킬 생성」 탭**이 생겼습니다. 버튼 한 번으로 ReportArchive 보고서 작성 규칙·위젯 예시를 **Claude 스킬(.zip)** 로 내보냅니다(현재 위젯 33종 규칙 포함, 매번 최신 규칙으로 생성).
- 받은 zip 은 **Claude Desktop/claude.ai 는 업로드**, **Codex CLI·Claude Code 는 압축을 풀어** `~/.codex/skills/` 또는 `~/.claude/skills/` 폴더에 넣으면 됩니다(같은 SKILL.md 표준이라 변환 불필요). 한 번 등록하면 긴 프롬프트를 매번 붙여넣지 않고도 보고서 JSON 을 받아 「로컬 → JSON에서 불러오기」로 가져올 수 있습니다.

---

# 이전 업데이트 (v0.52.18 → v0.52.19)

## AI 설정 — MCP 토큰 연동을 모든 클라이언트에서 동일하게 안정화
- Claude Code·Claude Desktop·Codex CLI **세 연동 방식 모두 `npx mcp-remote` 브리지로 통일**했습니다. 발급 화면의 명령/설정을 그대로 붙여넣으면, HTTP 주소(`--allow-http`)와 사내 인증서(`NODE_OPTIONS=--use-system-ca`) 문제로 연결이 실패하던 환경에서도 동일하게 동작합니다(브리지 사용으로 Node.js 필요).

## 로그인 — 아이디 입력 예시를 도메인 포함으로
- 로그인 아이디 칸의 예시를 `admin`에서 **`example@samsung.com`**으로 바꿔, 도메인을 포함한 전체 주소를 입력해야 한다는 점을 안내합니다.

---

# 이전 업데이트 (v0.52.17 → v0.52.18)

## 관계도 — "고립 노드" 정의 보정 + 기간 필터를 보고서에만
- **메타정보(공유 관련정보·종합보고)로 다른 보고서와 이어진 보고서**는 더 이상 고립 노드로 표시되지 않습니다(전엔 직접 링크가 없으면 작은 점=고립으로 처리). 관련정보·종합보고 레이어를 켜면, 같은 태그나 같은 종합보고로 묶인 보고서가 서로 연결된 것으로 보입니다.
- **기간 필터는 "보고서 노드"에만 적용**되도록 바로잡았습니다. 전엔 기간을 좁히면 관련정보(메타) 허브까지 같이 사라졌지만, 이제 기간을 좁혀도 메타 연결 구조는 유지됩니다(짝이 기간 밖이라 가려져도 연결이 끊기지 않음).

## MCP — 보고서 작성 시 메타데이터 지원
- MCP로 보고서 초안을 만들거나 수정할 때 **보고일·태그(관련정보)·보고서 종류**를 함께 지정할 수 있습니다. 작성 가능한 종류·관련정보 축을 조회하는 도구도 추가했습니다.

## AI 작성 — 권한·템플릿 가시성을 "개인공간 기준"으로
- AI(MCP)로 글쓰기가 **현재 보고 있는 게시판의 권한과 무관하게** 본인 개인공간에 정상 작성됩니다(전엔 읽기전용 게시판을 보고 있으면 권한 오류가 났습니다).
- AI 초안에서 쓸 수 있는 **템플릿 목록이 계정 기준**(내가 속한 모든 부서)으로 보이도록 했습니다.

## 주석(annotation) — 라벨 위치·설명선 + 보고서 게시 위치 보기
- 주석 **라벨을 끌어서 위치를 옮길 수** 있고, **설명선(리더 라인)** 스타일을 추가했습니다("점" 주석은 점 위치에서 선이 시작됩니다).
- 보고서 「더보기」에 **"게시 위치"**(게시된 게시판·폴더 보기)가 생겼고, 항목을 누르면 그 위치로 이동합니다.
- 마일스톤에서 **같은 날짜 항목 글자가 겹치던 문제**를 고쳤습니다.

---

# 이전 업데이트 (v0.52.16 → v0.52.17)

## 공유 — 게시판/폴더를 공유받으면 그 안 문서를 "목록에서" 볼 수 있게
- 게시판·폴더를 다른 부서에 공유하면, **공유받은 부서(하위부서 포함)가 그 게시판을 열어 목록을 브라우즈**할 수 있습니다(전엔 전체공개분만 보이던 한계 개선). 멤버가 아니어도 공유받은 분은 보입니다.
- 폴더 목록 숫자가 **"총 N개 중 M개 열람"** 으로 표시되어, 내가 못 여는 문서가 더 있다는 걸 알 수 있습니다(폴더 숫자와 실제 목록이 어긋나던 것도 해소).
- 보안: 게시(올리기)로 자동 생기는 부서 권한만으로는 상위 게시판 글이 하위 계정에 새지 않도록 정리했습니다.

## 종합보고 — "부서 기본 공유" 추가
- 종합보고 목록 화면에 **「부서 기본 공유」**(매니저·관리자)가 생겼습니다. 한 번 설정하면 그 부서의 **종합보고 전체(기존·신규)가 대상 부서에 자동으로 열립니다**(즉시 반영, 해제하면 즉시 닫힘). 보고서엔 영향 없습니다.
- 종합보고의 개별/게시판 공유도 보고서와 동일하게 동작하도록 일관화했습니다.

## 템플릿 — "보고서 설정"의 긴 글 머리기호 저장
- 템플릿 「보고서 설정」에서 **긴 글 머리기호**를 넣고 저장하면 유지되고(전엔 재진입 시 사라짐), 그 템플릿으로 만드는 **새 보고서에 머리기호가 상속**됩니다.

## 긴 글 — 제목 생략 유지 + 붙여넣기 개선
- 제목 생략을 켠 긴 글에 **본문을 입력해도 생략이 유지**됩니다(전엔 자동 채움이 다시 켜짐).
- **여러 줄 텍스트를 붙여넣으면 줄마다 행으로** 들어갑니다(머리표·들여쓰기는 단계로 인식). **전체 선택 후 붙여넣기**(선택분 교체)도 됩니다.

---

# 이전 업데이트 (v0.52.15 → v0.52.16)

## 종합보고·보고서 — Word 저장이 실패하던 문제
- **종합보고와 보고서의 Word(.docx) 저장이 "Cannot read properties of null (reading 'replace')" 오류로 실패하던 문제**를 고쳤습니다. 텍스트 색상 팔레트의 "기본(색 없음)" 항목 처리가 빠져 Word 내보내기 모듈이 아예 로드되지 못하던 것 — 이제 정상적으로 저장됩니다. (PPT 저장은 영향 없었습니다.)

---

# 이전 업데이트 (v0.52.14 → v0.52.15)

## 종합보고 — AI 작성 보고서에 "내용" 빈 줄이 끼던 문제
- AI로 작성한 보고서를 종합보고에 넣으면, **원본엔 없던 「내용」이라는 빈 제목 줄이 긴 글 사이에 끼어 보이던 문제**를 고쳤습니다. AI가 채우지 않은 빈 위젯이 종합보고 화면에서만 되살아나 자동 제목("내용")만 표시되던 것 — 이제 보고서 상세 화면과 똑같이 **채운 위젯만** 보입니다(화면·HTML 저장 모두 반영). 기존 보고서도 재저장 없이 즉시 정상 표시됩니다.

---

# 이전 업데이트 (v0.52.13 → v0.52.14)

## 표·비교표 — 격자 테두리(굵기·색) 옵션
- 표·비교표 편집화면에 **「테두리」 옵션**을 추가했습니다. 켜면 행·열 전체에 격자선이 들어가고(끄면 기존 형태), **굵기(얇게/보통/굵게)** 와 **색(팔레트, 기본=테마색)** 도 고를 수 있습니다. 색은 다크모드에 맞춰 자동 조정됩니다.
- 화면·HTML 저장·Word·PPT 내보내기에 모두 반영됩니다.

---

# 이전 업데이트 (v0.52.12 → v0.52.13)

## 대시보드 — 부서 건강도 "미분류"가 하위부서까지 집계
- 대시보드 "부서 건강도"에서 **「하위부서 포함」을 켜도 미분류(폴더 미지정) 개수가 현재 부서만 세던 문제**를 고쳤습니다. 이제 정체 초안·열린 코멘트처럼 **하위부서 게시판의 미분류까지 합산**됩니다(KPI 숫자와 클릭 후 목록의 불일치도 해소).

---

# 이전 업데이트 (v0.52.11 → v0.52.12)

## 종합보고 — "모두 펼치기/접기" 상태 저장
- 종합보고에서 안건 **「모두 펼치기/접기」 상태가 저장**되도록 했습니다. 예전엔 새로고침하면 다시 접혔는데, 이제 펼친/접은 기본 상태가 유지됩니다(단일·2단 보기 모두, 복사본도 유지).

---

# 이전 업데이트 (v0.52.10 → v0.52.11)

## AI 작성 — 마크다운 표식이 글자에 그대로 보이던 문제
- AI가 `**굵게**`, `# 제목`, 백틱 코드, `- 목록` 같은 **마크다운 문법을 쓰면 위젯엔 별표·우물정자가 그대로 보이던 문제**를 고쳤습니다. 위젯 본문은 평문이라, 이런 표식을 서버가 **자동으로 정리**해 깔끔하게 들어갑니다.
- AI 안내(작성 룰)에도 "텍스트는 평문, 마크다운 쓰지 말 것"을 명시했습니다. (`file_id`·`C#`·`-5%` 같은 정상 텍스트는 그대로 보존)

---

# 이전 업데이트 (v0.52.9 → v0.52.10)

## HTML로 저장 — 본문이 가운데 정렬 + 창에 맞춰 표시
- 보고서를 **HTML로 저장**하면 본문이 **왼쪽으로 붙던 문제**를 고쳤습니다. 위젯 그리드가 폭을 잃고 전체로 늘어나며 내용이 왼쪽에 몰리던 것 — 작성 당시 폭을 그대로 유지해 **가운데 정렬**되도록 바로잡았습니다.
- **창 크기에 맞춰 자동 축소(scale-to-fit)** — 보는 창이 본문보다 좁아도 비율을 줄여 창에 꽉 맞춰 가운데로 보여줍니다(보고서·종합보고 공통).

---

# 이전 업데이트 (v0.52.8 → v0.52.9)

## AI 작성 — 웹 URL 이미지/파일을 위젯에 바로 넣기
- AI(Claude)로 작성할 때, **웹 URL에 있는 이미지·파일을 이미지/첨부/영상/3D 위젯에 바로 넣을 수 있게** 했습니다. 서버가 그 URL에서 파일을 직접 받아 저장(SSRF 방어 포함)하고 위젯에 연결합니다 — 예전엔 파일 위젯은 사람이 직접 업로드해야 했습니다.
- 새 도구 `upload_from_url`(MCP) + 엔드포인트 `POST /api/files/from-url`. 사용법: URL로 file_id를 받아 위젯 content에 넣음.
- 단, **사용자 PC에만 있는 로컬 파일/채팅 첨부**는 URL이 없어 해당하지 않습니다(웹 UI에서 직접 추가).

---

# 이전 업데이트 (v0.52.7 → v0.52.8)

## AI 작성 — "제목 생략"도 AI가 설정 가능
- AI(Claude)로 보고서를 쓸 때, 캡션(제목)을 쓰는 **모든 위젯에서 「제목 생략」을 지정**할 수 있게 했습니다. 예전엔 제목을 비워도 템플릿 라벨이 자동으로 제목이 돼 **항상 제목이 나왔는데**, 이제 AI가 `caption_skip_autofill: true`를 주면 **제목 행이 통째로 사라집니다**(예: 제목 없는 긴 글).
- AI 안내(작성 룰·복사용 프롬프트)에도 이 옵션을 설명에 추가했습니다.

---

# 이전 업데이트 (v0.52.6 → v0.52.7)

## 종합보고 — 빈 그룹도 저장 시 유지
- 종합보고를 저장된 **양식으로 시작**할 때, 처음 저장에서 어떤 안건도 안 넣은 **빈 그룹이 사라지던 문제**를 고쳤습니다. 이제 안건이 하나도 없는 그룹도 **저장·새로고침 후에도 그대로 유지**됩니다.
- 양식에서 새 종합보고를 만들면 빈 그룹 골격이 **바로 영속**됩니다(첫 저장 전에 새로고침해도 안 사라짐).

---

# 이전 업데이트 (v0.52.5 → v0.52.6)

## 표·비교표 — 「복사」 버튼이 운영서버(HTTP)에서도 동작
- 표·비교표 편집화면의 데이터 **「복사」 버튼이 평문 HTTP 운영서버에서 안 되던 문제**를 고쳤습니다. (개발서버=HTTPS·localhost에선 되고, HTTP에선 브라우저 클립보드 API가 조용히 실패하던 것 — 뷰 모드 복사처럼 우회 방식으로 폴백하도록 보강.)
- **비교표 편집화면에도 「복사」 버튼을 추가**했습니다(표와 동일). 헤더=CASE 라벨, 본문=행 라벨+각 CASE 값으로 TSV 복사 — 엑셀·시트에 바로 붙여넣기 가능.

---

# 이전 업데이트 (v0.52.4 → v0.52.5)

## MCP 토큰 — Codex CLI 설정도 제공
- 토큰 발급 화면에 **Codex CLI(`~/.codex/config.toml`)** 등록용 설정도 추가했습니다. 이제 등록 방법이 **Claude Code · Claude Desktop · Codex CLI** 세 가지로 나옵니다.
- Codex는 HTTP를 바로 지원해 별도 브리지 없이 「설정 복사」 후 `config.toml` 에 붙여넣고 재시작하면 됩니다.

---

# 이전 업데이트 (v0.52.3 → v0.52.4)

## MCP 토큰 — Claude Desktop 복사는 「항목만」
- Claude Desktop 설정 복사 시 바깥 `{ "mcpServers": { … } }` 래퍼를 빼고 **`"reportarchive": { … }` 항목만** 복사되도록 했습니다 — 기존 설정 파일의 `mcpServers` 안에 바로 끼워넣기 편하게.

---

# 이전 업데이트 (v0.52.2 → v0.52.3)

## MCP 토큰 — Claude Desktop 설정 형식 수정
- Claude Desktop 설정 파일은 HTTP 서버를 **직접 받지 못해**(이전 형식은 연결되지 않았습니다), `npx mcp-remote` 브리지로 연결하는 **올바른 형식**으로 바로잡았습니다. (Node.js 필요)
- 토큰 발급 화면에서 「설정 복사」 후 `claude_desktop_config.json` 의 `mcpServers` 에 붙여넣고 Claude Desktop을 재시작하면 됩니다.

---

# 이전 업데이트 (v0.52.1 → v0.52.2)

## MCP 토큰 — Claude Desktop 설정도 함께 제공
- 토큰을 발급하면 이제 **Claude Code(터미널 명령)** 와 **Claude Desktop(설정 파일)** 두 가지 등록 방법이 모두 나옵니다.

---

# 이전 업데이트 (v0.52.0 → v0.52.1)

## MCP 토큰 — 삭제하면 목록에서 사라짐
- 「AI 설정 → MCP 토큰」에서 토큰을 삭제하면 이제 **목록에서 바로 사라집니다**(예전엔 「취소됨」으로 남아 있었음). 이미 「취소됨」으로 남아 있던 항목들도 함께 정리됩니다.

---

# 이전 업데이트 (v0.51.1 → v0.52.0)

## AI(Claude) 작성 — 고급 위젯 지원 확대
- AI가 **산점도·히트맵·레이더·네트워크·생키·박스/밀도·트리/마인드맵·트리맵·comparison·RACI** 등 **고급 위젯도 안정적으로 채울 수 있게** 했습니다. 예전엔 형식이 조금만 어긋나도 실패해 다시 시도해야 했는데, 이제 **흔히 틀리는 형식을 서버가 자동으로 바로잡아** 한 번에 들어갑니다.
  - 예: 값을 배열로만 줘도 알아서 맞춤, 숫자를 글자로 줘도 숫자로 변환, 키 이름을 비슷하게 줘도(예: 항목 라벨) 자동 매핑.
- 파일을 올려야 하는 위젯(이미지·첨부·3D CAD·동영상)은 여전히 사람이 화면에서 채웁니다(AI는 비워 둠).

---

# 이전 업데이트 (v0.50.1 → v0.51.1)

## AI(Claude)로 기존 초안 이어서 수정 + 페이지 추가
- 이제 Claude에서 **새 보고서 작성만이 아니라, 만들어 둔 초안을 이어서 수정**할 수 있습니다 — "방금 만든 초안에 표 한 줄 추가해줘", "요약 문단 고쳐줘"처럼 말하면 됩니다.
- **준 부분만 바꾸고 나머지는 그대로** 둡니다. 안 건드린 블록과 **화면에서 직접 맞춰 둔 레이아웃도 유지**됩니다(블록 구성이 바뀐 경우에만 자동 재배치).
- **페이지 추가**도 됩니다 — "두 번째 페이지 만들어서 산점도 넣어줘"처럼 요청하면 **기존 페이지는 그대로 둔 채** 새 페이지를 뒤에 붙입니다.
- **내 초안 목록 조회**가 추가돼, AI가 이어서 수정할 초안을 쉽게 찾습니다.
- 안전장치: AI 수정은 **본인이 만든 「작성 중」 초안만** 대상입니다(리뷰/발행 단계나 남의 보고서는 수정하지 않음). 게시·발행은 사람이 화면에서 합니다.
- 편집 충돌 방지: **누군가 그 보고서를 편집 화면에서 열어 두면(본인 다른 탭 포함) AI 수정이 거부**됩니다 — 화면에서 작업 중이던 내용을 AI가 덮어쓰지 않도록 막습니다. 편집 화면을 닫고 다시 요청하면 됩니다.
  - ※ 이 기능은 Claude 연동(MCP)을 설정한 경우에만 쓸 수 있습니다 — 프로필 → 공통·AI 설정 → 「MCP 토큰」 참고.

---

# 이전 업데이트 (v0.50.0 → v0.50.1)

## 홈·대시보드 레이아웃 정리
- 부서 홈의 **「고정 보고서」·「최근 보고서」를 그리드 카드**로 바꿨습니다 — 한 행에 여러 개(최대 3열)가 들어가 가로로 길지 않고 읽기 편합니다. 제목은 두 줄까지 표시.
- 대시보드에서 **「작성자 Top」과 「추세」를 한 행 2열**로 배치하고, 그 아래에 **교차 분석**이 오도록 정리했습니다.
- **주별** 보기에서 추세가 비어 있던 문제를 고쳐 **그 주의 일별 추세**(월~일)를 보여줍니다.

---

# 이전 업데이트 (v0.49.2 → v0.50.0)

## 부서 홈 — 고정 보고서(핀)
- 부서 매니저가 **중요한 보고서를 부서 홈에 고정**할 수 있습니다 — 보고서 상세의 **«더보기 → 부서 홈에 고정»**. 고정된 보고서는 **부서 홈 상단 「고정 보고서」 섹션**에 멤버 모두에게 보입니다.
- 매니저는 홈에서 각 항목의 **✕로 고정 해제**할 수 있습니다. (부서 단위 공용 핀이라 그 부서 게시판의 보고서만 고정 가능)

---

# 이전 업데이트 (v0.49.1 → v0.49.2)

## 대시보드 분포·교차 — 「전체 보기」
- 분포·교차 분석이 상위 일부만 보일 때 **「전체 보기」** 버튼으로 펼칠 수 있습니다.
  - 분포: 상위 12개 → 전체(최대 100개)를 스크롤로. 「접기」로 복귀.
  - 교차 분석: 행·열을 더 넓혀(최대 40×40) 다시 불러오고, 표는 가로·세로 스크롤(머리행·라벨열 고정).

---

# 이전 업데이트 (v0.49.0 → v0.49.1)

## 대시보드 교차/분포 — 게시판 정렬 + 잘림 표기
- 교차 분석에서 **게시판** 차원은 이제 **상위 부서가 왼쪽/위**로 오도록 부서 트리 순서로 정렬됩니다.
- 값이 많아 일부만 보일 때(교차표는 행·열 상위 8개, 분포는 상위 12개) **"건수 상위만 표시 (행 8/30 …)" 처럼 전체 개수를 함께 표기**합니다. 더는 조용히 잘리지 않습니다. (교차표 열이 많으면 표가 가로 스크롤됩니다 — 행 라벨은 고정.)

---

# 이전 업데이트 (v0.48.3 → v0.49.0)

## 대시보드 — 게시판별 분포 + 집계 성능 개선
- 분포/교차 분석에 **「게시판」 차원**을 추가했습니다 — **하위부서 포함** 시 어느 부서 게시판에 보고서가 몰리는지 보여주고, 막대/셀 클릭 시 **그 게시판으로 필터된 목록**으로 이동합니다.
- **집계 성능 개선**: 대시보드 집계가 보고서 **본문(content/pages)을 더 이상 불러오지 않습니다**(메타데이터만 사용). 보고서가 많은 큰 조직에서 대시보드 로딩이 가벼워집니다.

---

# 이전 업데이트 (v0.48.2 → v0.48.3)

## 대시보드 — 선택값 기억 (재진입 시 유지)
- 대시보드에서 고른 **기간 종류 · 분포 차원 · 교차 분석 행/열 차원 · 하위부서 포함** 설정을 **브라우저에 저장**해, 다른 화면 갔다 돌아와도 **초기화되지 않고 유지**됩니다. (특정 주/월 이동은 매번 현재 기간으로 시작합니다.)

---

# 이전 업데이트 (v0.48.1 → v0.48.2)

## 대시보드 드릴다운 — 개수 불일치 수정
- 교차 분석 셀(및 분포 막대)을 클릭해 목록으로 들어갔을 때 **개수가 대시보드와 안 맞던 문제**를 고쳤습니다. 원인은 드릴다운이 대시보드의 **기간·하위부서 포함 스코프를 목록에 안 넘긴** 것 — 이제 같은 **기간(보고 기준일)·하위부서 포함**을 그대로 적용해 **개수가 일치**합니다. 적용된 기간·하위부서 포함도 목록 상단 「필터 적용 중」 배너에 표시됩니다.

---

# 이전 업데이트 (v0.48.0 → v0.48.1)

## 대시보드 — 「하위부서 포함」 토글
- 대시보드 상단에 **「하위부서 포함 / 제외」 토글**이 생겼습니다(보고서 목록과 동일 방식). 켜면 **하위(자손) 부서 게시판까지 합쳐** KPI·단계·추세·분포·교차 분석을 집계하고, 끄면 그 부서 게시판만 봅니다.
- 기존에 헤더에 "및 하위 부서"라고 적혀 있었지만 실제로는 자기 부서만 집계하던 표기 오류도 함께 바로잡았습니다(토글 상태에 따라 정확히 표시).

---

# 이전 업데이트 (v0.47.1 → v0.48.0)

## 대시보드 — 종류·템플릿 드릴다운 + 교차 분석
- 분포 카드의 **종류·템플릿 막대도 클릭**하면 그 값으로 필터된 보고서 목록으로 이동합니다(엔티티에 이어). 이를 위해 보고서 목록에 **「종류」·「템플릿」 필터**를 추가했습니다(툴바 선택 + 드릴다운).
- **교차 분석** 카드가 생겼습니다 — 두 차원(예: **모델 × 개발단계**, 종류 × 템플릿…)을 골라 **교차 보고서 수를 표(히트맵)**로 봅니다. **셀을 클릭하면 두 필터가 동시에 적용된** 보고서 목록으로 이동합니다.
- 드릴다운으로 들어가면 목록 상단 **「필터 적용 중」 배너에 적용된 필터가 값까지 표시**됩니다(엔티티 값·종류·템플릿 칩, 각 칩 ✕ 로 개별 해제).

---

# 이전 업데이트 (v0.47.0 → v0.47.1)

## 대시보드 분포 — 막대 클릭 드릴다운
- 「분포」 카드의 **엔티티 축(모델·부품·개발단계·불량·신뢰성시험…) 막대를 클릭**하면, 그 값으로 **필터된 보고서 목록**으로 바로 이동합니다(예: "모델A" 막대 → 모델A 보고서). 목록 상단 활성 필터 배너에도 표시됩니다.
- (종류·템플릿 차원은 목록에 대응 필터가 없어 클릭 비활성입니다.)

---

# 이전 업데이트 (v0.46.0 → v0.47.0)

## 대시보드 — 메타데이터 분포 통계
- 대시보드의 "엔티티 커버리지" 카드를 **「분포」 카드**로 바꿨습니다. 위쪽 드롭다운으로 **차원을 골라** 봅니다 — **모델별 · 부품별 · 개발단계별 · 불량별 · 신뢰성시험별 · 종류별 · 템플릿별**. 각 값의 보고서 수를 막대로 보여주고, 값이 없는 보고서는 "미지정 N건"으로 표시합니다.
- 콘텐츠 수치 지표(템플릿 본문 숫자)는 차원이 너무 많아 대시보드 구성이 어려워, **보고서 메타데이터 기반 통계로 방향을 전환**했습니다. (관리자 「대시보드 지표」 화면은 당분간 숨김)

---

# 이전 업데이트 (v0.45.2 → v0.46.0)

## 대시보드 콘텐츠 지표 — 관리 화면 (Phase 3B-1)
- 시스템 관리에 **「대시보드 지표」**가 생겼습니다. 템플릿을 고르면 그 템플릿의 **숫자 필드(key_value)**를 골라 **대시보드 지표로 등록**할 수 있습니다(표시명·집계 방식·단위 지정).
- 등록한 지표는 그 템플릿을 쓰는 **모든 부서 대시보드의 "콘텐츠 지표"에 동일하게** 표시될 예정입니다(실제 값 집계·표시는 다음 단계 3B-2). 시스템 관리자만 정의/편집합니다.

---

# 이전 업데이트 (v0.45.1 → v0.45.2)

## 대시보드 — 기간 비교 뷰 보강
- **연도 비교** 기간이 생겼습니다 — 전체 기간을 **연 단위 막대(2024·2025·2026…)**로 나란히 놓고 해마다 비교합니다.
- **월별**이 이제 그 달을 **주 단위로 비교**해서 보여줍니다(이 달의 주별 추세).
- (정리) point 기간은 "한 단계 더 잘게 묶어 비교"합니다 — **월별→주별**, **연별→월별**.

---

# 이전 업데이트 (v0.45.0 → v0.45.1)

## 대시보드 — 연별 보기 추가
- 기간 선택에 **「연별」**이 생겼습니다(주별·월별 아래). 특정 연도를 골라 ←/→로 해마다 이동하며, 그 해를 **1~12월 월별 추세**로 보여주고 KPI는 **전년 대비 증감**까지 표시합니다.

---

# 이전 업데이트 (v0.44.0 → v0.45.0)

## 대시보드 — 서버 집계 전환 (Phase 3A)
- 대시보드의 모든 집계(KPI·단계·추세·건강도·엔티티·작성자)를 **서버에서 한 번에 계산**하도록 바꿨습니다. 기존엔 브라우저가 부서의 모든 보고서를 받아 직접 계산했는데, 이제 **압축된 집계 결과만** 받아 큰 부서에서도 가볍습니다.
- **부서 × 템플릿** 패널은 제거했습니다(가치 대비 복잡도가 커서).

---

# 이전 업데이트 (v0.43.0 → v0.44.0)

## 부서 홈 · 대시보드 정리 (Phase 1)
- **부서 홈**을 "탐색·시작 허브"로 재구성했습니다 — **빠른 이동**(대시보드·보고서 목록·종합보고·관계도 바로가기), **빠른 시작**(자주 쓰는 템플릿으로 바로 새 보고서), 최근 보고서, **하위 부서 바로가기**.
- **부서 대시보드**에 **드릴다운**이 생겼습니다 — KPI(총 보고서)·단계별 항목·부서별 막대를 **클릭하면 해당 보고서 목록**으로 바로 이동합니다.
- KPI에 **직전 기간 대비 증감(▲▼)**을 함께 표시합니다.
- 대시보드에 **부서 건강도**(정체 초안 14일+·미분류 — 클릭 시 해당 목록으로) 와 **엔티티(모델) 커버리지**(기간 내 보고서가 다룬 모델 상위) 카드를 추가했습니다.
- 부서 홈에 **부서 최근 활동** 피드를 추가했습니다 — 이 부서 보고서들의 **생성·편집·게시·코멘트** 흐름을 한곳에 최신순으로 모아 보여주고, 클릭하면 해당 보고서로 바로 이동합니다.
- 대시보드에 **작성자 Top** 카드를 추가했습니다 — 선택 기간 내 **작성자별 보고서 수 상위**를 막대로 보여줍니다.
- 보고서 목록에 **활성 필터 배너**가 생겼습니다 — 대시보드 드릴다운(상태·미분류 등)으로 들어오면 **지금 어떤 필터가 걸려 있는지** 목록 상단에 칩으로 표시하고, 칩별 ✕ 또는 「필터 해제」로 즉시 풀 수 있습니다. *좌측 폴더 숫자는 전체 기준, 아래 목록은 필터 결과*라는 안내도 함께 표시해 숫자 차이로 인한 혼란을 없앴습니다.
- 대시보드 **「정체 초안」** 타일을 클릭하면 목록도 정확히 **작성 중 + N일+ 미수정** 분만 보이도록 정체(미수정) 필터를 함께 적용합니다 — **타일 숫자와 목록 숫자가 일치**합니다.
- 대시보드 **부서 건강도**에 **미해결 코멘트** 수를 추가했습니다 — 이 부서 보고서들의 아직 해결되지 않은 코멘트 스레드 합계입니다.
- 역할을 나눴습니다: **홈 = 어디로 갈지(탐색)**, **대시보드 = 어떻게 돌아가나(분석)**. (개인 "내 작업" 화면은 다음 단계)

## 보고서 폴더 — "하위부서 포함" 보기 (NEW)
- 부서(조직) 게시판의 **폴더 사이드바 상단에 「하위부서 포함」 토글**이 생겼습니다. 켜면 이 부서 폴더 아래에 **하위 부서들이 폴더처럼** 펼쳐지고, 그 안에 각 부서의 폴더들이, 또 그 아래 더 하위 부서들이… **한 트리로 한 번에** 보입니다.
- 하위 부서 **이름을 클릭**하면 그 부서의 보고서 전체가, **그 부서의 폴더를 클릭**하면 해당 폴더 보고서가 — **현재 화면에서 바로** 표시됩니다(페이지 이동 없음).
- **권한 범위 안만** 보입니다: 볼 수 없는 부서는 이름조차 나오지 않고, 멤버가 아닌 공개 게시판은 공개 폴더만 표시됩니다. (상위 부서 멤버는 하위 부서까지 자동 포함.)

## PPT 저장 — 표·비교표가 편집 가능한 표로
- PPT로 저장할 때 **표·비교표·RACI 매트릭스**가 이제 그림이 아니라 **편집 가능한 PowerPoint 표**로 들어갑니다 — 슬라이드에서 셀 내용·서식을 직접 고칠 수 있습니다.
- **셀 병합, 셀 배경색·글자색, 정렬, 다중행 헤더**가 그대로 옮겨집니다.
- 단, **비교표의 이미지 셀**처럼 표 안에 그림이 들어간 경우에는 (PowerPoint 표 셀에 이미지를 넣을 수 없어) 그 위젯만 기존처럼 이미지로 들어갑니다.

## PPT 저장 — 위젯 제목(캡션) 포함
- 텍스트·표로 들어가는 위젯(긴 글·표·비교표 등)도 **위젯 제목(캡션)이 위에 함께** 들어갑니다 — "**그림 N / 표 N**" 번호와 라벨까지 화면에 보이는 그대로. (예전엔 표·긴 글의 제목이 빠졌습니다.)

## PPT 슬라이드 가이드 — 실제 분할과 일치
- 편집 화면의 **슬라이드 가이드 점선**이 이제 "화면 높이 균등 분할"이 아니라 **실제 PPT로 나뉘는 경계**를 따라갑니다 — 편집 모드에서 위젯이 더 길게 보여도, 각 위젯의 **뷰(저장) 높이** 기준으로 슬라이드 경계를 잡고 **위젯 사이(틈)에 맞춰** 표시합니다. 그래서 "가이드 안에 맞춰 배치했는데 PPT/뷰에서 다른 슬라이드로 넘어가는" 어긋남이 줄어듭니다.
- 위젯이 슬라이드 경계에 걸쳐 잘리지 않도록, 경계는 항상 위젯 틈에서만 끊깁니다.
- **편집 가이드와 실제 PPT 분할이 같은 로직**을 쓰도록 통일했고, **거의 한 슬라이드 분량이면 사소하게 넘쳐도 한 장에 담아**(약간 축소) 두 번째 슬라이드가 거의 빈 채로 쪼개지는 일을 막습니다.

---

# 이전 업데이트 (v0.40.0 → v0.41.0)

## PPT 저장 — 글 위젯이 편집 가능한 텍스트로
- PPT로 저장할 때 **긴 글·제목·항목 리스트·키-값**은 이제 이미지가 아니라 **편집 가능한 PowerPoint 텍스트**로 들어갑니다 — 슬라이드에서 **글자 수정·검색·복사**가 되고, 확대해도 또렷합니다.
- 글자 **색·굵기·기울임·밑줄**과 **글머리 기호(■ · -)·들여쓰기**가 유지되며, 글꼴은 '맑은 고딕'으로 들어갑니다.
- 표·차트·다이어그램·수식·이미지 등 나머지 위젯은 기존처럼 화면 모양 그대로 이미지로 들어갑니다.

---

# 이전 업데이트 (v0.39.0 → v0.40.0)

## 보고서 PPT로 저장 (NEW)
- 보고서 보기 화면의 **「로컬」 메뉴**에 **"PPT로 저장"**이 생겼습니다(Word·PDF·HTML 옆). 저장 시 **슬라이드 비율(16:9 / 4:3 / 16:10)**을 고르면 됩니다(보고서에 슬라이드 가이드 비율이 있으면 자동으로 미리 선택).
- 보고서가 세로로 길면 **행 단위로 여러 슬라이드에 나눠** 담깁니다. 슬라이드보다 큰 위젯은 그 슬라이드에 맞춰 줄여 가운데 배치합니다.
- 위젯은 **화면에 보이는 모양 그대로**(색·테두리·캡션 "그림 N/표 N" 포함) 이미지로 들어갑니다. 저장 중에는 흰 슬라이드에 맞게 자동으로 라이트 테마로 캡처합니다.
- 참고: 1차 버전은 모든 위젯이 **이미지**로 들어가 PPT에서 위치 이동은 되지만 텍스트 편집은 안 됩니다(추후 글·제목 등은 편집 가능한 텍스트로 개선 예정). 동영상·HTML 임베드처럼 캡처할 수 없는 위젯은 빈 영역으로 남을 수 있습니다.

---

# 이전 업데이트 (v0.38.0 → v0.39.0)

## (관리자) 계정 관리 — 접속 이력 탭 (NEW)
- **계정 관리** 페이지에 **「접속 이력」 탭**이 생겼습니다(시스템 관리자 전용). 누가 언제·어디서 로그인했는지를 로그로 확인할 수 있습니다.
  - 각 줄에 **접속 시각 · 사용자(이름/이메일) · 구분(로그인/가입/세션) · 결과(성공/실패) · IP · 기기·브라우저**가 표시됩니다.
  - **로그인 실패**(미가입 이메일 포함)도 함께 남아, 이상 접속 시도를 점검할 수 있습니다.
  - **'로그인 유지'로 토큰만 들고 재방문**하는 경우도 **'세션' 접속**으로 남습니다(짧은 시간 내 반복은 묶어 도배 방지). 비밀번호로 직접 로그인하는 것과 구분돼 표시됩니다.
  - **성공/실패 필터**, 최근 **100·200·500건** 선택, 이름/이메일/IP **검색**, 정렬·페이지 이동을 지원합니다.
- IP는 역방향 프록시(nginx) 뒤에서도 실제 접속 IP가 기록됩니다(X-Forwarded-For). 외부 토큰(MCP)·페이지 단순 열람은 기록하지 않으며, 감사 로그 오류가 로그인 자체를 막지 않도록 안전하게 처리했습니다.

---

# 이전 업데이트 (v0.37.0 → v0.38.0)

## 위젯 복사 — 모든 위젯으로 확대 (NEW)
- 지난 업데이트의 **위젯 복사 버튼**(보고서를 볼 때 위젯에 마우스를 올리면 오른쪽 위에 표시)을 **거의 모든 위젯**으로 넓혔습니다. 위젯 성격에 따라 자동으로 알맞은 방식이 적용됩니다.
  - **글 위젯** → 글자로 복사: 긴 글, **제목**, **항목 리스트**, **키-값**.
  - **표 위젯** → PowerPoint·Word엔 표 모양 그대로, 엑셀엔 셀로: 표, **비교표**, **RACI 매트릭스**.
  - **시각 위젯** → **PNG 이미지로 저장**(다운로드): 차트·산점도뿐 아니라 **3D 산점도·히트맵·등고선·트리맵·원배치·관계도·네트워크·마인드맵·원형·와플·박스·밀도·레이더·수식·진행 바·마일스톤·순서도·사분면·Sankey·CAD 3D·이미지**까지.
- 복사 품질도 다듬었습니다.
  - **PNG 저장 시 "그림 N / 표 N" 캡션이 이미지에 함께 들어갑니다.**
  - **표 복사** — 셀 안의 **글자 색·굵기·기울임·밑줄·배경색**까지 그대로 붙습니다. 표는 **복사 버튼 2개**가 뜹니다: ① **표 복사**(엑셀엔 셀, Word엔 표로) ② **이미지(PNG) 저장**(PPT엔 화면 그대로). PowerPoint는 붙여넣은 표의 글자를 줄이는 특성이 있어, **PPT엔 이미지 저장**을 권장합니다.
  - **긴 글** — 줄 앞 **글머리 기호(■ · -)** 와 **줄바꿈**까지 보이는 그대로 복사됩니다.
- 평문 HTTP 운영 환경에서도 동작하도록 기존 방식을 그대로 사용합니다. (동영상·HTML 임베드·첨부처럼 복사 의미가 약한 위젯은 버튼이 뜨지 않습니다.)

---

# 이전 업데이트 (v0.36.0 → v0.37.0)

## 보고서 위젯 복사 — 글·표·차트 (NEW)
- 보고서를 **볼 때** 위젯에 마우스를 올리면 오른쪽 위에 **복사 버튼**이 뜹니다.
  - **긴 글** → 글자를 그대로 복사합니다.
  - **표** → PowerPoint·Word엔 **표 모양 그대로**(색·테두리 포함), 엑셀엔 셀로 붙여넣어집니다.
  - **차트·산점도** → **PNG 이미지로 저장**(다운로드)합니다.
- 운영 환경(평문 HTTP)에서도 복사가 되도록 방식을 맞췄습니다. 기존 **MCP 토큰/명령 복사, 계정 이메일 복사** 등도 HTTP에서 안정적으로 동작합니다.

## 보고서 위젯 크기 조절
- 위젯을 **아래·왼쪽·오른쪽 가장자리 선**과 **아래 두 모서리**로 잡아 크기를 조절할 수 있습니다. 가장자리 가운데의 **＋ 추가 버튼**과 자리를 나눠 쓰며, 위젯이 너무 얇아지지 않도록 **세로 최소 높이**가 생겼습니다.

## 보고서 화면 정리 — 헤더·상태
- 제목 아래가 깔끔해졌습니다. **템플릿 종류·페이지 수·보고서 종류·작성/수정 정보**는 제목 옆 **ⓘ 버튼**을 누르면 보입니다.
- 상단의 **"리뷰 중 / 발행됨" 상태 띠를 없애고**, 제목 아래 **상태 뱃지를 클릭**하면 설명이 뜨도록 바꿨습니다. (편집 시 보고서 종류·관련 정보·연결된 보고서 칩은 같은 자리에 그대로 유지됩니다.)
- 창을 좁히면 버튼이 통째로 아래로 내려가지 않고, "종합보고 제출"이 "제출"로 줄고 버튼이 두 줄로 정리됩니다.

## 같은 폴더 보고서 — 좌측 사이드바
- 보고서 보기 화면 **왼쪽 끝의 폴더 버튼**을 누르면, 같은 폴더의 보고서 목록이 **세로 사이드바**로 펼쳐져 바로 이동할 수 있습니다. (기존 상단의 이전/다음 목록을 대체.)

## 보기 모드(페이지별/전체) — 보고서별 기억
- 보고서마다 기본 보기 모드(**페이지별 / 전체**)를 저장합니다. **편집 모드에서** 토글을 바꾸면, 그 보고서를 다시 열 때 같은 모드로 뜹니다.

## 전체화면 — 설정한 폭 존중
- 전체화면(발표)으로 볼 때, 보고서에 설정한 **폭을 그대로 유지**하고 가운데 정렬합니다(예전엔 화면 전체로 늘어났습니다).

## 공유 버튼 위치 이동
- 보고서 상단의 **"공유"** 버튼을 **"더보기" 메뉴 안**으로 옮겨 툴바를 정리했습니다.

## (관리자) 비밀번호 분실 요청 — 반려
- '비밀번호 찾기'로 들어온 요청 중 **오타·미가입 이메일**처럼 처리할 수 없는 건을 **반려**로 정리할 수 있습니다(계정 관리 화면). 가입된 계정이 없는 이메일이라는 안내도 함께 표시됩니다.

---

# 이전 업데이트 (v0.35.0 → v0.36.0)

## Claude(AI)로 보고서 작성 — 여러 페이지 + 단락 구분
- Claude(MCP)로 보고서를 만들 때 **여러 페이지**로 나눠 작성할 수 있습니다(예: 배경·분석·결론을 페이지별로). 페이지 strip으로 탐색됩니다.
- 각 블록에 **단락 구분**(배경·분석·의사결정·일정 등)을 달 수 있습니다 — 보고서에서 단락 색상 칩으로 표시됩니다. (등록된 단락 코드만 적용되고, 적절한 코드가 없으면 생략.)

---

> 더 이전 버전(v0.34.1 이하)의 업데이트 내역은 **[업데이트내용_보관.md](업데이트내용_보관.md)**에서 볼 수 있습니다.
