# 백그라운드 워커 (Job Queue) — MVP 설계

무거운/지연 가능한 작업을 웹 요청 경로에서 떼어내, **별도 워커 프로세스**가
Postgres 기반 큐에서 꺼내 처리하도록 하는 설계. 새 인프라(Redis/Celery) 없이
**이미 있는 자산(Postgres · systemd · apptainer SIF)** 만 재사용한다.

> 배경 분석: `부족한부분.md`(운영 인프라 공백 1순위), 배포 토폴로지: `배포구조.md`.

---

## 1. 한 줄 요약

> 웹은 "주문서 한 줄(`jobs` INSERT)"만 남기고 즉시 응답하고, **MCP 서버와 똑같이
> 생긴 두 번째 systemd 유닛**(`reportarchive-worker.service`)이 같은 `app.sif`를
> `apptainer exec` 로 돌려 큐를 처리한다. 기존 34개 모듈은 거의 그대로.

---

## 2. 왜 필요한가 (문제)

현재 모든 작업이 uvicorn 워커(운영 기본 4개) **안에서 인라인**으로 돈다. 사용자가
결과를 기다릴 필요가 없는데도 응답 스레드를 통째로 점유하는 작업들:

| 작업 | 현재 | 증상 |
|---|---|---|
| PPT/Word 일괄 내보내기 | 요청 핸들러가 직접 생성 | 한 건이 워커 스레드를 수 초~수십 초 점유 → 같은 프로세스에 줄 선 다른 사람 버벅임 |
| 알림 메일 다이제스트 | (미구현) | SMTP 붙이면 발송 동안 요청 블록 |
| 종합보고 롤업 | 동기 집계 | 큰 종합보고일수록 응답 지연 |
| AI 임베딩 / 자동요약 | (미구현, RAG 선결) | 모델 호출은 수 초 단위 — 인라인 불가 |

추가로, 운영은 **uvicorn 멀티워커(4)** 라 "FastAPI startup hook 에 루프를
넣는" 식의 인-프로세스 방식은 **루프가 4개 떠서 같은 작업을 중복 실행**한다.
→ 단일 책임의 **독립 워커 프로세스**가 정답.

---

## 3. 설계 원칙

1. **Postgres 가 곧 큐다.** 별도 브로커(Redis) 도입 안 함. 폐쇄망 단일 서버에서
   호스트 서비스를 늘리는 건 운영·백업·장애면만 키운다. 동시성은
   `SELECT … FOR UPDATE SKIP LOCKED` 로 해결.
2. **Additive, not rewrite.** 새 모듈 `app/jobs/` 하나 + 호출부에 `enqueue()` 한 줄.
   기존 핸들러의 도메인 로직은 **워커 핸들러로 옮길 뿐** 새로 쓰지 않는다.
3. **기존 배포 패턴 재사용.** `reportarchive-mcp.service` 가 이미 "같은 설치 dir,
   별도 프로세스, 별도 systemd 유닛"의 선례. 워커도 동일한 꼴.
4. **격리가 목적.** 코어를 칸막이로 나누지 않는다. 무거운 일을 **다른 프로세스로**
   빼는 것만으로 웹이 안 멈춘다. 필요하면 `worker_concurrency` / `nice` 로 조절.

---

## 4. 토폴로지

```
┌─ 호스트 (운영 서버) ────────────────────────────────────────────┐
│                                                                  │
│  reportarchive.service        reportarchive-worker.service ★신규 │
│   apptainer run app.sif        apptainer exec app.sif worker.py  │
│   └ uvicorn :3000 (워커 4)      └ worker loop (스레드 N)          │
│        │  enqueue()                    ▲  claim (SKIP LOCKED)     │
│        ▼                               │                          │
│   ┌──────────── PostgreSQL :5433 ──────────────┐                 │
│   │  jobs 테이블 (주문서 목록 = 큐)            │                 │
│   └─────────────────────────────────────────────┘                │
│                                                                  │
│  (reportarchive-mcp.service — 기존 별도 유닛, 동일 패턴)          │
└──────────────────────────────────────────────────────────────────┘
```

웹과 워커는 **같은 SIF·같은 코드·같은 `.env`·같은 DB** 를 쓴다. 명령만 다르다
(`run.py` vs `worker.py`).

---

## 5. 데이터 모델 — `jobs` 테이블

alembic 마이그레이션 1개. (현재 77개 운영 중 — 일상 작업)

```sql
CREATE TABLE jobs (
    id            BIGSERIAL PRIMARY KEY,
    type          TEXT        NOT NULL,                 -- 'export_pptx' 등 핸들러 키
    payload       JSONB       NOT NULL DEFAULT '{}',    -- 입력 인자
    status        TEXT        NOT NULL DEFAULT 'pending',-- pending|running|done|failed|canceled
    priority      INT         NOT NULL DEFAULT 0,       -- 높을수록 먼저
    attempts      INT         NOT NULL DEFAULT 0,
    max_attempts  INT         NOT NULL DEFAULT 5,
    run_after     TIMESTAMPTZ NOT NULL DEFAULT now(),   -- 지연/백오프 재시도
    dedup_key     TEXT,                                  -- 선택: 중복 enqueue 방지
    result        JSONB,                                 -- 산출물 참조 (예: {"file_id": "..."})
    last_error    TEXT,
    locked_at     TIMESTAMPTZ,                           -- 워커가 집은 시각 (reaper)
    worker_id     TEXT,                                  -- 처리 중인 워커 식별
    created_by    INT REFERENCES users(id),             -- 추적/“내 작업” UI (nullable)
    created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- claim 핫패스: 대기 중인 것 중 우선순위/시각 순
CREATE INDEX ix_jobs_claim   ON jobs (priority DESC, run_after, id) WHERE status='pending';
-- reaper: 죽은 워커가 'running' 으로 남긴 좀비 회수
CREATE INDEX ix_jobs_running ON jobs (locked_at) WHERE status='running';
-- 중복 방지: 같은 (type, dedup_key) 가 동시에 두 번 안 들어가게
CREATE UNIQUE INDEX uq_jobs_dedup ON jobs (type, dedup_key)
    WHERE dedup_key IS NOT NULL AND status IN ('pending','running');
-- “내 작업” 조회
CREATE INDEX ix_jobs_mine    ON jobs (created_by, created_at DESC);
```

**상태 전이**: `pending → running → done`
실패 시 `running → (attempts<max) pending(run_after=백오프) | (소진) failed`. 취소는
`pending → canceled`.

---

## 6. 큐 핵심 로직

### 6.1 집기(claim) — 원자적, 동시 워커 안전
```sql
UPDATE jobs SET status='running', locked_at=now(), worker_id=:wid, updated_at=now()
WHERE id = (
    SELECT id FROM jobs
    WHERE status='pending' AND run_after <= now()
    ORDER BY priority DESC, run_after, id
    FOR UPDATE SKIP LOCKED          -- ← 다른 워커/스레드가 같은 행 못 집음
    LIMIT 1
)
RETURNING *;
```
`SKIP LOCKED` 덕분에 워커 스레드 수를 늘려도 락 경합 없이 안전하게 분산된다.

### 6.2 처리 + 재시도(지수 백오프)
- 핸들러 성공 → `status='done', result=…`.
- 핸들러 예외 → `attempts+1`. `attempts < max_attempts` 면
  `run_after = now() + backoff(attempts)` (예: 5s·30s·2m·10m·1h) 로 되돌림,
  아니면 `status='failed', last_error=…`.
- **멱등성 권장**: 핸들러는 재실행돼도 안전하게(예: 같은 export 두 번 = 같은 파일).
  외부 부작용(메일 발송)은 `dedup_key` 또는 result 체크로 중복 방지.

### 6.3 좀비 회수(reaper)
워커가 처리 중 죽으면 행이 `running` 으로 갇힌다. 워커 시작 시 + 주기적으로:
```sql
UPDATE jobs SET status='pending', locked_at=NULL, worker_id=NULL
WHERE status='running' AND locked_at < now() - INTERVAL '15 minutes';
```
(타임아웃은 가장 긴 작업보다 넉넉히. 향후 heartbeat 로 정밀화 가능.)

---

## 7. 코드 구조

```
backend/
├── worker.py                      ★ 워커 진입점 (run.py 와 대칭)
└── app/jobs/
    ├── models.py                  ★ Job ORM (Base 재사용)
    ├── queue.py                   ★ enqueue() / claim() / mark_done|failed()
    ├── registry.py                ★ JOB_HANDLERS = {"export_pptx": fn, ...}
    ├── runner.py                  ★ 스레드풀 루프 + reaper + graceful shutdown
    ├── routes.py                  ★ GET /api/jobs/{id}, GET /api/jobs?mine (폴링)
    └── handlers/
        ├── export.py              ★ 첫 핸들러 (PPT/Word 생성 → result.file_id)
        └── ...                    (이후 digest.py, embed.py)
```

### enqueue (호출부에서 쓰는 한 줄)
```python
# app/jobs/queue.py
def enqueue(session, type: str, payload: dict, *, dedup_key=None,
            priority=0, run_after=None, created_by=None) -> int:
    job = Job(type=type, payload=payload, dedup_key=dedup_key,
              priority=priority, run_after=run_after or func.now(),
              created_by=created_by)
    session.add(job); session.flush()      # 같은 요청 트랜잭션에 묶임
    return job.id
```

### 핸들러 시그니처 (도메인 로직은 여기로 이사)
```python
# app/jobs/registry.py
JOB_HANDLERS: dict[str, Callable[[Session, dict], dict]] = {}

def handler(name):                          # 데코레이터 등록
    def deco(fn): JOB_HANDLERS[name] = fn; return fn
    return deco

# app/jobs/handlers/export.py
@handler("export_pptx")
def export_pptx(session, payload) -> dict:  # 예외 throw → 자동 재시도
    report = get_report(session, payload["report_id"])
    file_id = build_pptx_file(session, report)   # ← 기존 내보내기 로직 그대로
    return {"file_id": file_id}                  # result 에 저장 → 사용자 다운로드
```

### worker.py (run.py 와 대칭)
```python
# .env 로드 → SessionLocal 재사용 → ThreadPool(worker_concurrency) →
# 루프: claim 1건 → dispatch → mark. SIGTERM 시 신규 claim 중단·in-flight 마무리·종료.
```
SQLAlchemy 가 **sync 모드**라, 동시성은 워커 한 프로세스 안의 **스레드풀**로 낸다
(각 스레드가 자기 `SessionLocal` 세션을 들고 claim→처리). 멀티프로세스가 더
필요해지면 유닛을 `@N` 인스턴스로 띄우면 되지만 MVP 는 단일 프로세스로 충분.

---

## 8. 호출부 변경 — before/after

```python
# Before: 요청 핸들러가 직접 생성 (응답까지 사용자가 대기, 스레드 점유)
@router.post("/reports/{rid}/export/pptx")
def export(rid, db=Depends(get_db), user=Depends(current_user)):
    file_id = build_pptx_file(db, get_report(db, rid))   # 수 초~수십 초
    return {"file_id": file_id}

# After: 주문서만 남기고 즉시 응답. 워커가 처리, 프론트는 job_id 로 폴링.
@router.post("/reports/{rid}/export/pptx")
def export(rid, db=Depends(get_db), user=Depends(current_user)):
    job_id = enqueue(db, "export_pptx", {"report_id": rid}, created_by=user.id)
    return {"job_id": job_id, "status": "pending"}        # 202 Accepted
```
프론트: `POST … → job_id` 받고 `GET /api/jobs/{id}` 를 1~2초 간격 폴링,
`status=='done'` 이면 `result.file_id` 로 다운로드. (기존 알림 폴링 패턴과 동일.)

> 주의: **권한**. `GET /api/jobs/{id}` 는 `created_by == 요청자`(또는 admin)만
> 조회 허용. result 의 file_id 다운로드도 기존 grant 검사를 통과해야 함.

---

## 9. 배포

### systemd 유닛 (mcp 유닛 복사 → 명령만 교체)
```ini
# deploy/reportarchive-worker.service.template
[Unit]
Description=Report Archive Background Worker
After=network-online.target postgresql.service reportarchive.service
Requires=postgresql.service

[Service]
Type=simple
User=@@USER@@
Group=@@USER@@
WorkingDirectory=@@INSTALL_DIR@@
ExecStart=/usr/bin/apptainer exec \
    --bind @@INSTALL_DIR@@/.env:/opt/app/backend/.env:ro \
    --bind @@INSTALL_DIR@@/uploads:/data/uploads \
    --bind @@INSTALL_DIR@@/logs:/data/logs \
    @@INSTALL_DIR@@/app.sif /opt/app/venv/bin/python worker.py
Restart=on-failure
RestartSec=10
TimeoutStopSec=60          # in-flight 작업 마무리 여유 (graceful shutdown)
NoNewPrivileges=true
ProtectSystem=full
ProtectHome=false
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```
- `deploy.sh` 의 유닛 렌더링(@@VAR@@ 치환)에 한 줄 추가 + `update` 경로에서
  웹 재시작과 함께 `systemctl restart reportarchive-worker`.
- 마이그레이션은 기존 `deploy.sh update` 의 alembic 단계가 그대로 `jobs` 생성.

### 설정 (runtime_tuning 확장)
`app/shared/runtime_tuning.py` 의 `_TUNABLES` 에 추가 → 관리자-서버 UI 슬라이더로 노출:
```python
"worker_concurrency":   ("WORKER_CONCURRENCY", 2),    # 동시 처리 스레드 수
"worker_poll_ms":       ("WORKER_POLL_MS", 1000),     # 유휴 시 폴링 간격
```
"워커가 서버를 다 먹지 않게" = `worker_concurrency` 를 2~4 로 두는 것으로 충분.

---

## 10. MVP 범위 (1차 출시)

**목표**: 큐 레일을 끝까지(enqueue→처리→result→폴링→다운로드) 한 번 관통시키는 것.
가장 가시적인 통증이자 산출물이 명확한 **단일/일괄 내보내기**를 첫 핸들러로.

- [x] `jobs` 테이블 + 마이그레이션
- [x] `app/jobs/` (queue·registry·runner·routes) + `worker.py`
- [x] 핸들러 1종: `export_pptx` (+ `export_docx`) — 기존 내보내기 로직 이전
- [x] 내보내기 라우트를 enqueue 방식으로 전환 + 프론트 폴링/다운로드 UI
- [x] `reportarchive-worker.service` + deploy.sh 연동
- [x] reaper + 지수 백오프 + graceful shutdown
- [x] `GET /api/jobs/{id}` 권한 검사

**MVP 비포함(다음 단계)**:
- 알림 다이제스트(SMTP 채널 확보 후), 종합보고 롤업 이전
- 스케줄형(주기) 작업 — 아래 11절
- admin 작업 모니터(실패 재시도/취소 UI), LISTEN/NOTIFY 즉시 깨우기 최적화

---

## 11. 스케줄형(주기) 작업 — 두 갈래

| 방식 | 언제 | 비고 |
|---|---|---|
| **systemd 타이머** | 밤마다 다이제스트, 임베딩 스윕 | 큐 없이도 "지난 실행 이후 새 것만 처리"면 충분. 가장 단순. |
| **워커 내부 스케줄러** | 주기 작업도 `jobs` 로 통일하고 싶을 때 | 워커가 tick 마다 due 한 주기작업을 `enqueue`(dedup_key 로 중복 방지) |

> AI 1순위(임베딩/시맨틱 검색)는 **타이머 스윕만으로 시작 가능** — 풀 큐가 없어도 됨.
> 온디맨드 비동기(내보내기·알림)가 풀 큐의 첫 고객.

---

## 12. 관측성 / 운영

- 워커 로그는 기존 `logs/`(bind-mount) 로. 각 작업 시작/종료/실패를 구조적으로 1줄.
- `failed` 작업은 테이블에 `last_error` 와 함께 남아 사후 조회/수동 재시도 가능.
- 후속: 관리자 화면에 "대기/처리중/실패 건수" 카운터 + 실패 재시도 버튼
  (`dashboard`/`admin` 모듈 패턴 재사용).

---

## 13. 테스트 전략 (pytest 재사용, 현재 39파일/220+케이스)

- `enqueue` → 행 생성/필드 확인.
- claim 동시성: 두 세션이 같은 pending 을 동시에 claim 시 정확히 1개만 성공.
- 재시도: 핸들러가 예외 → attempts 증가·run_after 미래·max 소진 시 failed.
- reaper: 오래된 running 회수.
- 멱등성: 같은 dedup_key 두 번 enqueue → 1건.
- e2e: export enqueue → 워커 한 틱 → done + result.file_id + 권한.

---

## 14. 도입 전 결정사항 (열린 질문)

1. **첫 핸들러를 내보내기로 vs 알림으로?** (권장: 내보내기 — SMTP 채널 의존성 없음)
2. **워커 인스턴스 수**: 단일 프로세스+스레드풀로 시작(권장) vs 처음부터 `@N`.
3. **즉시성**: 폴링 1초로 충분한가, 아니면 LISTEN/NOTIFY 까지(MVP 후로 미룸 권장).
4. **보존 정책**: `done`/`failed` 작업을 며칠 후 정리할지(예: 30일, 타이머 정리 잡).

---

## 15. 비목표 (Non-goals)

- Redis/Celery/RQ 등 외부 브로커 도입 — 폐쇄망 단일 서버에 부적합.
- 분산 다중 서버 큐 — 단일 서버 전제.
- 실시간 협업(CRDT) — 별개 주제(`약점보강_로드맵.md`).

---

## 16. 향후 확장 (같은 레일 재사용)

- **알림 전달**: `send_digest`/`send_mail` 핸들러 + `notifications` 를 outbox 로(`부족한부분.md`).
- **AI**: `embed_report`(임베딩) · `summarize_report`(자동요약) 핸들러 → RAG 1순위와 직결.
- **내보내기 예약/일괄**, 종합보고 자동 롤업, 만료 보고서 아카이빙.

> 핵심: 이 워커 레일을 한 번 깔면 알림·AI·내보내기·정리 작업이 **전부 같은 길**을 탄다.
> 그래서 단일 기능이 아니라 **여러 공백을 동시에 여는 기반 투자**다.
</content>
</invoke>
