# 엔티티 메타데이터 관리 개선 설계 — 거버넌스 · 관계 · 태깅 보조

현재의 평면 통제어휘(controlled vocabulary)를 **재설계 없이 확장**해, 세 축으로 더
효율적으로 관리한다: ① 입력 거버넌스(노이즈 원천 차단), ② 엔티티 관계/계층(캐스케이드·
롤업), ③ 태깅 편의(템플릿 바인딩 + AI 보조). `개발계획.md §1-4`의 온톨로지 Phase 2
(그래프 쿼리) 방향과 일치한다.

> 관련 문서: `개발계획.md`(§1-4 온톨로지 단계, §1-5 도메인 어휘 컨트롤),
> `project_rag_foundation`(임베딩/`report_chunks` 운영 반영), `B300_보조AI_설계.md`.

---

## 1. 배경 — 지금 무엇이 비효율인가

현재 구조는 깨끗하다(EAV도 JSON blob도 아닌 정규화된 3테이블 N축 통제어휘):

```
entity_types   (축 7개: model, part, bom, phase, defect, rel_test, sim_type)
  id, slug, label, icon, multi, sort_order, description
entities       (축별 값: "A1234", "P-001" …)
  id, type_id→entity_types, value, code, description, status(active|deprecated),
  created_by_user_id, created_at, updated_at
report_entities(M:N: 어느 보고서가 어느 값을 태깅)
  report_id→reports(CASCADE), entity_id→entities(RESTRICT)
```

구조 결함은 아니다. 비효율은 **운영 부담**과 **표현력 한계**에 있다:

1. **값이 반응적으로 쌓임** — 모든 축이 개방형(아무나 picker에서 즉석 추가). 모델·BOM
   처럼 정형 코드여야 할 축에도 오타·표기흔들림(`A1234`/`a-1234`)·중복이 누적되고,
   admin이 `merge`로 사후 청소하는 두더지잡기.
2. **축이 전부 평면·독립** — 현실의 모델⊃부품⊃BOM 포함관계가 모델에 없어 캐스케이드
   필터·자동제안·롤업이 불가능.
3. **모든 보고서가 7축 전부 노출** — 시험 보고서 picker에 시뮬종류 축까지 다 떠서 번잡.
4. **자유 텍스트 → 엔티티 추출 자동화 없음** — `개발계획.md §1-5`가 "AI가 작성 후 자동으로
   entity 추출"을 적었으나 미구현. 태깅은 100% 수작업.

> 부서별 스코프(`workspace_slug`)는 이번 범위에서 **제외**(현재 전사 단일 어휘로 충분,
> 부서별 용어 분기 수요가 실측될 때 별도 추진). §9 참조.

---

## 2. 핵심 결정 (사용자 확정)

- 3테이블 구조 유지, **additive 확장만**. 기존 행은 전부 동작 보존되게 backfill.
- 개선 방향 = **거버넌스 + 관계/계층 + 태깅 편의** 세 가지(부서 스코프 제외).
- 관계는 단일부모(`parent_id`)가 아니라 **M:N 관계 테이블**(부품이 여러 모델에 속하는
  현실 수용).
- AI 자동태깅은 **결정적 매칭 우선**(regex + 기존 `report_chunks` 임베딩 유사도), LLM
  추출은 후순위 레이어. (메모리 원칙: 생성LLM=보조기능)

### 2.1 템플릿↔축 바인딩 수명주기 (확정)

바인딩은 **"표시 규칙", 태그(`report_entities`)는 "사실 데이터"** — 완전 분리. 바인딩을
언제든 바꿔도(설정 데이터, 마이그레이션 아님) 기존 보고서는 안전하다.

- **빈 바인딩 = 전체 기본** — 어떤 템플릿에 축이 하나도 안 묶이면 7축 전체를 노출(축
  미사용이 아님). 실수로 다 빠져도 안전. 바인딩 테이블은 비어서 시작하므로(backfill
  없음) 기존·신규 템플릿 모두 이 "빈=전체" 경로로 현행 동작을 그대로 유지.
- **축 추가 시** — 기존 보고서는 무영향. 편집하면 새 축이 빈 칸으로 등장(태깅은 선택).
- **축 삭제 시** — 태그는 **DB에 그대로 보존**. 상세 화면은 보고서가 실제 가진 태그를
  **바인딩과 무관하게 전부 표시**. 편집 picker는 **바인딩축 ∪ 이 보고서가 이미 태그를
  가진 축**의 합집합으로 구성 → 안 묶인 축이라도 값이 있으면 picker에 떠서 **여전히
  제거 가능**(유령 태그 방지). 이는 `deprecated` 값을 "보고서가 가지고 있으면 흐리게라도
  표시·제거 가능"하게 다루는 기존 패턴과 동일.
- **`required`** — **새 저장 시 soft 경고**일 뿐. 과거 보고서를 소급 강제 수정하지 않음.

---

## 3. 데이터 모델 변경

마이그레이션 번호는 현재 head(`p51_composite_preset_archived`) 다음부터.
모두 **additive**(기존 데이터 무손상).

### 3.1 ① 입력 거버넌스 — `entity_types` 확장 + 별칭 (p53)

`entity_types` 컬럼 추가:

| 컬럼 | 타입 | 의미 |
|---|---|---|
| `entry_policy` | enum `open`\|`closed`, default `open` | `open`=현행(아무나 즉석 추가). `closed`=admin만 등록, picker는 선택만 |
| `value_pattern` | text, nullable | 값 형식 정규식. 예 BOM `^[0-9]{8}$`. NULL=제약 없음 |

> 기존 7축은 전부 `open`으로 backfill → 동작 100% 보존. admin이 모델·BOM만 `closed`로
> 전환하는 식으로 점진 적용.

별칭 테이블 신규 `entity_aliases`:

```
entity_aliases
  id            PK
  entity_id     FK→entities(CASCADE)
  alias         text          -- 사람이 입력한 표기
  normalized    text          -- lower+trim (resolve 키)
  created_at
  UNIQUE(entity_id, normalized)
  INDEX ix_entity_aliases_normalized (normalized)  -- 입력 → canonical resolve 핫패스
```

지금의 대소문자 dedup(`find_by_value_ci`)을 **명시적·admin관리형 동의어**로 승격.
사용자가 별칭을 입력하면 서버가 canonical entity로 resolve해 그 값을 태깅 → **수동 merge
필요성 거의 소멸**.

### 3.2 ② 엔티티 관계 — `entity_relations` (p54)

```
entity_relations
  id            PK
  src_entity_id FK→entities(CASCADE)   -- 자식 (예: 부품 P-001)
  dst_entity_id FK→entities(CASCADE)   -- 부모 (예: 모델 A1234)
  relation      enum/slug              -- 우선 'part_of' 하나로 시작
  created_by_user_id FK→users(SET NULL)
  created_at
  UNIQUE(src_entity_id, dst_entity_id, relation)
  INDEX ix_entity_relations_dst (dst_entity_id, relation)  -- "A1234의 부품들" 역방향
  INDEX ix_entity_relations_src (src_entity_id, relation)
```

방향 규약: `src --part_of--> dst` (자식→부모). 부품→모델, BOM→부품 등. M:N이라 부품이
여러 모델에 속하는 경우 OK. **자기참조·순환 방지**는 서비스 레이어에서 src≠dst + 깊이
제한(1~2단계)으로 가드.

### 3.3 ③ 템플릿↔축 바인딩 — `template_entity_types` (p52) ✅ **구현 완료**

```
template_entity_types
  template_id        String(64)  -- template_id 단위(버전 무관). templates 에서 유일하지
                                 -- 않아 FK 없음(owner_workspace_slugs 와 같은 이유, 서비스가 정합성)
  entity_type_id     FK→entity_types(CASCADE)
  required           bool default false  -- true면 미태깅 시 작성 화면 soft 경고
  sort_order         int  default 0
  PK(template_id, entity_type_id)
```

> **backfill 없음** — 빈 바인딩 = 전체 축(§2.1)이므로 테이블은 비어서 시작하고, 기존·신규
> 템플릿 모두 전체 축 노출로 현행 동작을 그대로 유지한다. admin이 템플릿별로 노출 축을
> 골라 행을 넣으면 그때부터 그 부분집합만 노출. (빈 바인딩 해석은 §2.1에서 "전체 기본"으로
> 확정.)
>
> 구현: 마이그레이션 `p52_template_entity_types`, 모델 `TemplateEntityType`,
> `GET/PUT /api/templates/{id}/entity-types`, 작성 picker(`EntityTagsSection`)는
> **바인딩축 ∪ 보고서가 이미 가진 축**으로 구성, 템플릿 관리에 "노출 축" 다이얼로그.

### 3.4 ③b AI 자동태깅 — 신규 테이블 불필요(1차)

결정적 매칭은 기존 `entities`(어휘) + `report_chunks`(임베딩, 운영 반영됨)만으로 동작.
제안 수락 로그가 필요해지면 후속에 `entity_tag_suggestions` 추가 검토(§9).

---

## 4. API / 서비스 변경 (`modules/entities`)

### 4.1 거버넌스
- `services.create_entity()`:
  1) 별칭 테이블 우선 resolve → 있으면 canonical 반환(현 case-insensitive dedup 대체/포함).
  2) 축이 `closed`인데 기존 값/별칭에 없으면 **거부**(`409`/`422`, "관리자 등록 값만 선택").
  3) `value_pattern` 있으면 검증 실패 시 거부(형식 안내 메시지 포함).
- `routes`: `PATCH /api/entity-types/{id}`에 `entry_policy`·`value_pattern` 수정 추가
  (admin). `entity_aliases` CRUD: `POST/DELETE /api/entities/{id}/aliases`(admin).

### 4.2 관계
- `GET /api/entities`에 `related_to=<entity_id>` 필터 추가 → 그 부모에 `part_of`로 묶인
  자식만(축 필터와 AND). 캐스케이드 picker의 백엔드.
- 관계 CRUD: `POST/DELETE /api/entities/{id}/relations`(admin). 응답에 src/dst 라벨 동봉.
- `merge_entities()`: 머지 시 양쪽 `entity_relations`·`entity_aliases`도 canonical로
  이관(중복 UNIQUE 충돌은 row-by-row 처리, 기존 merge 패턴과 동일).
- **롤업 쿼리**(`dashboard/services.py`): "모델 A1234 보고서" = A1234 직접태그 ∪
  (A1234의 part_of 자식들을 태그한 보고서). 1~2단계 재귀(또는 펼친 IN절)로 충분.

### 4.3 템플릿 바인딩
- `GET /api/templates/{template_id}/entity-types` → 바인딩된 축 목록(+required).
- `PUT …/entity-types`(admin/매니저) → 축 집합·required·순서 저장.
- 보고서 작성/설정 로딩 시 `listEntityTypes()` 대신 **템플릿 바인딩 축**을 받아 picker 구성.

### 4.4 자동태깅
- `POST /api/reports/{id}/suggest-entities` → 본문 텍스트에서 후보 추출:
  1) **결정적**: `value_pattern`/코드 정규식으로 BOM·부품번호 등 정확 매칭(환각 0).
  2) **유사도**: `report_chunks` 임베딩 ↔ 엔티티 값 임베딩 top-k(임계치 위만).
  반환은 **제안 칩**(자동 태깅 아님) → 사용자가 수락해야 `entity_ids`에 들어감.

---

## 5. 프론트엔드 변경

| 화면/파일 | 변경 |
|---|---|
| `EntitiesAdminPage.jsx` | 축별 `entry_policy`·`value_pattern` 편집, 엔티티별 **별칭** 관리, **관계(부모 연결)** 편집 UI |
| `EntityMultiPicker.jsx` | `closed` 축이면 "+ '{q}' 새로 추가" 숨김 · pattern 힌트 표시 · **캐스케이드**(상위 축 선택 시 하위 picker가 `related_to`로 좁힘, "전체 보기" 탈출구) · 상위 태깅 시 관련 값 **quick-add 칩** |
| `ReportSettingsDialog.jsx` | 템플릿 바인딩 축만 렌더, `required` 미태깅 경고, **"AI 태그 추천"** 버튼(제안 칩 수락) |
| `ReportDetailPage.jsx`(`ReportEntitiesPanel`) | 변경 최소(축 카탈로그 순서 유지). 관계 있는 값은 부모 표기 보조 가능(선택) |
| 템플릿 관리 화면 | 템플릿별 **노출 축 선택**(멀티셀렉트 + required 토글) |
| `shared/api/entities.js`, `…/templates.js` | 신규 엔드포인트 래퍼 |

---

## 6. 단계적 롤아웃 (가치/노력 순)

| 단계 | 내용 | 마이그레이션 | 노력 | 즉효 |
|---|---|---|---|---|
| **A-1** ✅ | ③ 템플릿↔축 바인딩 | p52 | 소 | 모든 보고서 picker 즉시 정리 |
| **A-2** | ① 거버넌스(closed+pattern+별칭) | p53 | 소~중 | merge 부담 즉시↓ |
| **B** | ② 관계/계층 + 캐스케이드 + 롤업 | p54 | 중~대 | 구조적 도약 |
| **C** | ③b 자동태깅(결정적→유사도) | (무) | 중 | 태깅 수고↓ |

- A-1/A-2는 각각 **독립 PR**로 위험 낮게 출시 가능.
- B가 핵심 투자. C는 B의 관계를 활용하면 추천 품질이 올라가 마지막.
- 각 단계는 빈-바인딩=전체 같은 안전 기본값으로 현행 동작을 보존한 채 점진 적용(빅뱅 없음).

---

## 7. 마이그레이션 / 호환성

- 전부 additive. `down_revision` 체인: `p51 → p52(바인딩,완료) → p53(거버넌스) → p54(관계)`
  (롤아웃 순서 = 마이그레이션 생성 순서).
- 동작 보존 규칙:
  - **바인딩(p52)**: backfill 없음 — 빈 바인딩 = 전체 축(§2.1).
  - **거버넌스(p53)**: `entry_policy='open'`, `value_pattern=NULL` 기본 (기존 7축 동작 보존).
- 롤백: 각 마이그레이션 `downgrade()`가 컬럼/테이블 drop. 데이터 손실은 신규 컬럼/링크에
  한정(보고서 본문·기존 태그 무관).

---

## 8. 테스트 관점

- **거버넌스**: closed 축에 신규 값 거부 / open 축은 허용 / pattern 불일치 거부 /
  별칭 입력이 canonical로 resolve / 별칭 충돌(다른 entity에 같은 normalized) 거부.
- **관계**: `related_to` 필터가 자식만 / 자기참조·순환 거부 / merge 시 관계·별칭 이관 /
  롤업이 직접태그∪자식태그 정확.
- **바인딩**: 미바인딩 축은 picker에서 숨김(단 이미 태깅된 축은 합집합으로 유지) /
  required 미태깅 경고 / 빈 바인딩이면 기존
  보고서 picker가 현행과 동일.
- **자동태깅**: 결정적 매칭 정확(오탐 0 지향) / 제안은 수락 전 미저장 / 임계치 아래 미노출.
- **회귀**: 기존 `set_report_entities` replace 동작·deprecated 표시·usage 카운트 불변.

---

## 9. 미결정 / 추후 (v2 후보)

- **부서별 스코프**(`entities.workspace_slug` nullable): 부서별 용어 분기 수요 실측 시.
- **관계 종류 확장**: `part_of` 외 `supersedes`, `variant_of` 등(우선 1종으로 시작).
- **엔티티 이력/스냅샷**: rename 시 과거 보고서 소급 변경 방지용 스냅샷(필요성 낮음).
- **자동태깅 LLM 레이어**: 결정적/유사도로 부족할 때 B300으로 추출(보조기능 원칙).
- **별칭 unique on lower(value)** DB 레벨 강제(PG IMMUTABLE/generated column).

---

## 10. 손대는 곳 요약 (구현 체크리스트)

백엔드:
- ✅ `migrations/versions/p52_template_entity_types.py` (A-1, 완료)
- ✅ `modules/templates/{models,schemas,services,routes}.py` — 바인딩 CRUD (A-1, 완료)
- `migrations/versions/p53_entity_governance.py` (entity_types 2컬럼 + entity_aliases)
- `migrations/versions/p54_entity_relations.py`
- `modules/entities/models.py` — EntityType 2필드, EntityAlias·EntityRelation 모델
- `modules/entities/{schemas,services,routes}.py` — resolve/검증/관계/별칭/related_to
- `modules/reports/routes.py` — `POST …/suggest-entities`
- `modules/dashboard/services.py` — 롤업 쿼리

프론트엔드:
- ✅ `shared/api/templates.js` — get/setTemplateEntityTypes (A-1, 완료)
- ✅ `modules/reports/ReportSettingsDialog.jsx`(EntityTagsSection 바인딩 필터) +
  `ReportDetailPage.jsx`(templateIds 전달) (A-1, 완료)
- ✅ `modules/templates/TemplatesPage.jsx` — "노출 축" 다이얼로그 (A-1, 완료)
- `modules/entities/EntitiesAdminPage.jsx`, `EntityMultiPicker.jsx` (A-2/B)
- `shared/api/entities.js` (A-2/B)
